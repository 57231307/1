import * as utooPack from "@utoo/pack";
export interface BuildOptions {
    projectPath: string;
    rootPath: string | undefined;
    projectOptions: utooPack.WebpackConfig | utooPack.BundleOptions;
}
export declare function resolveFromConfigFile(configDir: string): Promise<Record<string, unknown>>;
export declare function resolveBuildOptions(flags: {
    project?: string;
    root?: string;
    webpack?: boolean;
}): Promise<BuildOptions>;
