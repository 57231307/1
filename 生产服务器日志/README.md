# 生产服务器日志

> **状态说明（2026-06-14）**：本目录之前临时存放从生产服务器导出的日志快照
> （`应用日志/`、`错误日志/`、`系统日志/`、`审计日志/`、`性能日志/`、`安全日志/`）。
> 鉴于日志中可能包含服务器与数据库的敏感信息（IP、端口、服务名等），**所有日志
> 文件已从仓库中删除**，仅保留本说明文件作为归档说明。
>
> **批次 28 v7 P0-2 修复（2026-06-29）**：原 README 暴露生产服务器与数据库的真实
> IP 地址，攻击者扫描 GitHub 即可定位生产环境进行攻击。已将真实 IP 替换为占位符，
> 实际地址请通过受控的内部文档或密钥管理服务获取。

## 原始导出记录

- 导出时间：2026-06-13
- 服务器 IP：已脱敏（见内部文档）
- 数据库 IP：已脱敏（见内部文档）
- 服务端口：8082
- 服务名称：bingxi-backend

## 原始目录结构（已清空）

```
生产服务器日志/
├── 应用日志/          # 后端应用日志
│   ├── bingxi_backend.log.2026-06-11
│   ├── bingxi_backend.log.2026-06-12
│   └── bingxi_backend.log.2026-06-13
├── 错误日志/          # 错误日志
│   ├── error.log.2026-06-11
│   ├── error.log.2026-06-12
│   └── error.log.2026-06-13
├── 系统日志/          # systemd 服务日志
│   └── systemd_日志.txt
├── 审计日志/          # 审计日志
│   ├── business_audit.log.*      # 业务审计
│   ├── database_audit.log.*      # 数据库审计
│   ├── financial_audit.log.*     # 财务审计
│   └── permission_audit.log.*    # 权限审计
├── 性能日志/          # 性能监控日志
│   ├── performance_audit.log.*   # 性能审计
│   └── system_health.log.*       # 系统健康
├── 安全日志/          # 安全审计日志
│   └── security_audit.log.*
└── README.md          # 本文件
```

## 后续约束

- 仓库根 `.gitignore` 已包含 `*.log`，新日志文件不会被纳入版本控制。
- 如确需在仓库中临时保留生产日志，请先脱敏处理（IP、端口、密钥、token 等），
  并仅作为短期排查归档使用，问题解决后立即清理。
