# P1/P2/P3 批次修复规划

**规划日期**：2026-07-02
**基线**：main HEAD `2ecec6bd`（P0×36 已全部修复）
**审计报告**：[2026-07-02-p1-p2-p3-audit.md](./2026-07-02-p1-p2-p3-audit.md)
**总问题数**：149 项（P1×50 / P2×68 / P3×31），跨维度去重后约 137 项

## 修复策略

1. **P1 优先**：50 项（去重约 38 项），按主题/模块聚合，每批 5-10 项
2. **批次工作流**：创建分支 → 修改 → commit → push → 监控 CI → squash merge → 删除分支 → 同步 main → 下一批
3. **CI/CD Only**：禁止本地编译/构建/测试，全部依赖 CI 验证（13 job，12/13 全绿即合格，E2E continue-on-error）
4. **P2 跟进**：68 项，P1 清零后按模块聚合
5. **P3 收尾**：31 项，代码风格与文档类，最后批量处理
6. **复审机制**：所有 P1/P2/P3 修复完成后进行全项目复审，循环直到无问题

---

## 第一阶段：P1 修复（批次 57-67，共 11 批）

### 批次 57：安全认证链路修复（7-1/7-2/7-3/7-4/7-5）✅ 已修复（代码已有修复注释，无需变更）

**主题**：refresh_token / JWT 吊销 / logout / 限流 / 敏感请求体脱敏
**状态**：经核验，5 项 P1 在代码中均已有修复实现（含 "P1 7-X 修复" 注释），审计报告基于批次 55 HEAD 但修复可能在更早批次已完成或审计基线与修复 commit 时序存在差异。
**核验结果**：
- 7-1：✅ `auth_handler_misc.rs:108-128` 已实现 `generate_refresh_token` JWT 形式 + 旧 JTI 吊销轮换
- 7-2：✅ `init_service.rs:586-598` `reset_password` 成功后已调 `revoke_user_jtis(user_id, "PASSWORD_RESET_BY_ADMIN")`
- 7-3：✅ `auth_handler_session.rs:94-108` logout 已调 `revoke_jti` 写 Redis 分布式黑名单（与进程内黑名单双写）
- 7-4：✅ `main.rs:568-577` 已全局挂载 `rate_limit_by_ip`（180 req/min），挂载在 auth_middleware 之外对所有请求生效
- 7-5：✅ `omni_audit.rs:85-103` 已实现 `is_sensitive_request_body_path` 敏感路径白名单，命中时 request_body 脱敏为 "[REDACTED]"

**包含项**：
- 7-1：refresh_token 流程断裂（改用 JWT 形式 + 轮换）
- 7-2：reset_password 后吊销活跃 JWT
- 7-3：logout 改用 Redis JTI 黑名单
- 7-4：main.rs 全局挂载 rate_limit_by_ip
- 7-5：omni_audit 敏感路径请求体脱敏
**改动文件**：无（代码已修复）
**预估**：0 文件，纯核验

### 批次 58：操作审计覆盖补齐（8-1/8-2/8-3/8-4/8-6/8-7）

**主题**：user/role/batch/import_export 写操作审计落库
**包含项**：
- 8-1：update_user 审计（role_id/status 变更）
- 8-2：create_user 审计
- 8-3：role_handler 全部写操作审计
- 8-4：batch_create/batch_update 审计 + user_id 透传
- 8-6：export_csv/export_excel 审计
- 8-7：delete_user 改用 record_async + before_snapshot
**改动文件**：user_handler.rs / role_handler.rs / batch_service.rs / import_export_handler.rs
**预估**：4 文件，审计覆盖补齐

### 批次 59：审计日志质量修复（8-15/8-8/1-1 部分）✅ 已完成

**主题**：哈希指纹 + before_snapshot + user_id 透传（部分）
**包含项**：
- 8-15：✅ change_password 审计记录哈希指纹（SHA-256 摘要前 8 位替代占位 JSON）
- 8-8：✅ update_with_audit 补 before_snapshot（update 前查 old_model，update 后序列化 new_model 为 after_snapshot；仅 i32 主键支持，i64 主键保持 None）
- 1-1（部分）：✅ ap_payment_service.rs 3 处 Some(0) 改 Some(user_id)（update/confirm/invoice 更新）
**延后项**（拆到批次 59b）：
- 1-1（剩余）：40+ 处 Some(0) 改 Some(user_id)（bom_service/purchase_return/quality_inspection/voucher_service/ar_invoice/sales_return/color_card_crud/inventory_adjustment/ap_verification/crm/supplier 等）— 涉及 13+ 文件，部分方法签名需补 user_id 参数并由 handler 注入 AuthContext
- 8-9：delete_with_audit 增加 audit_ctx 参数 — 涉及 30 文件 35 处调用方更新
- 8-5：search_logs 验证 HMAC-SHA256 签名 — 涉及签名逻辑重构（omni_audit_log 表未存 payload，验签时无法重建签名材料）
**改动文件**：user_handler.rs / ap_payment_service.rs / audit_log_service.rs
**预估**：3 文件，已完成

### 批次 59b：审计日志 user_id 透传（1-1 类别 A+B）✅ 已完成

**主题**：user_id 透传（低风险批量替换）
**包含项**：
- 1-1（类别 A，29 处）：✅ 方法签名已有 `user_id` 参数，直接将 `Some(0)` 改为 `Some(user_id)`（特殊：inventory_adjustment_service.rs:194 改为 `Some(approved_by)`）
- 1-1（类别 B，16 处）：✅ 方法签名已有 `_user_id` 参数，去下划线前缀改为 `user_id`，再将 `Some(0)` 改为 `Some(user_id)`
**延后项**（拆到批次 59c/59d/67.5+）：
- 1-1（类别 C，62 处）：方法签名无 `user_id` 参数，需补签名 + 改调用链 — 涉及内部辅助方法（update_return_totals / calculate_*_total_txn / clear_primary_contacts / update_account_balances 等）需从调用方透传 user_id，风险较高，拆分到批次 67.5+ 与其他 P1 修复交错进行
- 8-9：delete_with_audit 增加 audit_ctx 参数 — 涉及 30 文件 35 处调用方更新，拆到批次 59c 单独处理
- 8-5：search_logs 验证 HMAC-SHA256 签名 — 签名材料含 `msg.action` 和 `msg.payload` 但两者均未持久化到 omni_audit_logs 表，验签时无法重建签名材料，需先做设计（新增 payload 列或改签名材料），拆到批次 59d 单独处理
**改动文件**：23 个 service 文件（类别 A 17 文件 + 类别 B 9 文件，部分重叠）
**预估**：45 处机械替换，已完成

### 批次 60：单号生成器统一（3-6/3-8/3-13/5-9/5-10/5-11）✅ 已完成

**主题**：DocumentNumberGenerator 统一 + 事务边界修复
**包含项**：
- 3-6：✅ bpm_service instance_no/task_no 改用 DocumentNumberGenerator（3 处：start_process instance_no + start_process task_no + approve_task task_no）
- 3-8：✅ so/delivery delivery_no 改用 DocumentNumberGenerator（2 处：ship_order 传 &txn + create_delivery 包裹事务）
- 3-13：✅ crm/lead lead_no 改用 DocumentNumberGenerator（保留 req.lead_no 用户可覆盖逻辑，包裹事务）
- 5-9：✅ number_generator 新增 generate_no_with_txn / generate_no_with_width_txn 变体（直接在传入 txn 上执行 advisory_xact_lock + COUNT，不开子事务）
- 5-10：✅ crud_macro 宏 txn 变体改调 generate_no_with_txn（影响 po/order generate_order_no_with_txn + ap_verification generate_verification_no）
- 5-11：✅ ap_verification 单号生成改用宏的 txn 变体（auto_verify + manual_verify 传 &txn）
**改动文件**：utils/number_generator.rs / utils/crud_macro.rs / bpm_service.rs / so/delivery.rs / crm/lead.rs / ap_verification_service.rs
**预估**：6 文件，基础设施改造

### 批次 61：状态机 lock_exclusive 补齐（3-1/3-2/3-3/3-5/5-20/5-21）

**主题**：ar/recon + bpm + voucher + budget 行锁补齐
**包含项**：
- 3-1：ar/recon update 加 txn + lock_exclusive
- 3-2：ar/recon update_status 加状态白名单 + lock
- 3-3：ar/recon send 加 txn + lock_exclusive
- 3-5：bpm approve_task 加 lock_exclusive
- 5-20：voucher_service.update 状态门移入 txn + lock
- 5-21：budget_management update_item 加 txn + lock + audit
**改动文件**：ar/recon.rs / bpm_service.rs / voucher_service.rs / budget_management_service.rs
**预估**：4 文件，并发安全修复

### 批次 62：业务流断点修复（3-7/3-11/5-1/5-2/5-5/1-3）✅ 已完成

**主题**：ship→AR / BPM 启动 / sales_return 红字 AR 链路修复
**状态**：已合并 main HEAD `f46760ab`，PR #306，CI 12/13 全绿
**包含项**：
- 3-7/5-1：ship_order commit 后调用 create_receivable
- 5-2：event_bus SalesOrderShipped 补监听
- 3-11：submit_order BPM 启动失败回滚/补偿
- 5-5/1-3：sales_return approve_return AR 生成移入 txn 或补偿
**改动文件**：so/delivery.rs / so/order_workflow.rs / event_bus.rs / sales_return_service.rs / ar_invoice_service.rs
**预估**：5 文件，业务链路修复

### 批次 63：事务边界原子性（3-4/3-12/5-3/5-4/5-6）✅ 已完成

**主题**：状态门 + 除零 + 事务包裹
**状态**：已合并 main HEAD `50989f4a`，PR #307，CI 12/13 全绿
**包含项**：
- 3-4：ar_invoice cancel 状态门拒绝 PAID
- 3-12/5-6：ap_payment 分摊除零校验
- 5-3：quality_inspection create_record 整体包裹 txn
- 5-4：finance_payment create_payment 整体包裹 txn
**改动文件**：ar_invoice_service.rs / ap_payment_service.rs / quality_inspection_service.rs / finance_payment_service.rs
**预估**：4 文件

### 批次 64：接口越权防护（2-1/2-2/2-3/2-4/2-5/4-1/4-2）✅ 已完成

**主题**：批量删除越权 + 系统角色保护 + cookie 名 + 侧边栏权限
**状态**：已合并 main HEAD `5d20ff58`，PR #308，CI 12/13 全绿
**包含项**：
- 2-1：批量删除改 DELETE 或校验 products:delete
- 2-2：批量端点补充 AuthContext
- 2-3：delete_role 检查 is_system
- 2-4：delete_user 保护最后一个 admin
- 2-5：request_validator cookie 名改 access_token
- 4-1：MainLayout canAccessMenu 改 return false
- 4-2：StockTab 权限码统一两段式
**改动文件**：routes/catalog.rs / bulk_product_handler.rs / role_handler.rs / user_handler.rs / middleware/request_validator.rs / MainLayout.vue / StockTab.vue
**预估**：7 文件

### 批次 65：测试资产伪测试清理（6-1/6-2/6-3/6-4/6-5/6-6）✅ 已完成

**主题**：删除/重写伪测试
**状态**：已合并 main HEAD `ef47106f`，PR #309，CI 12/13 全绿
**包含项**：
- 6-1：quotation_e2e.rs 删除伪测试，改真实链路
- 6-2：test_utils_response.rs 删除标准库测试
- 6-3：test_inventory_count.rs 改调真实方法
- 6-4：test_quality_standard.rs 改调真实方法
- 6-5：frontend utils.test.ts 改真实导入
- 6-6：frontend request.test.ts 改真实导入
**改动文件**：backend/tests/quotation_e2e.rs / test_utils_response.rs / test_inventory_count.rs / test_quality_standard.rs / frontend/tests/unit/utils.test.ts / request.test.ts
**预估**：6 文件

### 批次 66：E2E 环境补齐（6-7，原 6-6 延后项）✅ 已完成

**主题**：14 个 purchase/sales E2E spec 补 auth mock
**状态**：已合并 main HEAD `ea69d747`，PR #310，CI 14/14 全绿（E2E 也 success）
**包含项**：
- 6-7：✅ 各 spec beforeEach 加 applyAuthMocks + mock 业务 API
  - `_helpers.ts` 新增 `mockBusinessApi`（通配 `/api/v1/erp/*` 返回空分页，对 auth/me + init/status 调 `route.fallback()`）
  - `applyAuthMocks` 末尾调用 `mockBusinessApi`
  - 14 个 spec beforeEach 改为解构 `{ page, context }` + 调用 `applyAuthMocks(context)`
- 移除 ci-e2e 的 continue-on-error（可选，待 E2E 稳定后）— 未做，保持 continue-on-error
**改动文件**：frontend/e2e/smoke/_helpers.ts + 14 个 spec（purchase 7 + sales 7）
**预估**：15 文件

### 批次 67：BPM 定义占位实现（1-2）✅ 已完成

**主题**：bpm_service_stub 10 个方法真实实现
**包含项**：
- 1-2：create/get/update/delete_process_definition / create_process_version / activate / save_as_template / create_from_template 等真实实现
**改动文件**：bpm_service_stub.rs / bpm_definition_handler.rs / routes/system.rs / bpm_service.rs
**实现方案**：
- service 层：实现 9 个方法真实 CRUD 逻辑（create_process_version 因签名缺陷已删除，handler 层改用 create_process_definition 完成版本创建）
- 模板标识：通过 `category` 字段特殊值 `__TEMPLATE__` 标识模板记录，无需新增表/字段
- 版本管理：基于同一 `code` 的多记录实现，`activate_process_version` 将同 code 其他记录置为 INACTIVE
- handler 层：添加字段映射（code→process_key, name→process_name, config.nodes→nodes, PageResponse.data→list），适配前端 API 契约
- 路由注册：在 `routes/system.rs` 的 `bpm()` 函数中注册 8 条新路由（definitions CRUD + versions + templates）
- 移除所有 `#[allow(dead_code)]` 标注
- NULL 安全：所有 `category != __TEMPLATE__` 过滤都显式包含 `IS NULL` 条件
- BpmService.db 字段改为 pub(crate)，允许 bpm_service_stub.rs 访问
**预估**：3-5 文件，较大改造
**实际**：4 文件（bpm_service_stub.rs / bpm_definition_handler.rs / routes/system.rs / bpm_service.rs）
**main HEAD**：`739b500b`（PR #311，CI 13/13 全绿含 E2E success）

---

## 第二阶段：P2 修复（批次 68-75，共 8 批）

### 批次 68：N+1 查询与批量优化（5-12/5-13/5-14/5-22/5-23/3-18/3-23/3-24/3-25）✅ 已完成

**主题**：inventory_finance_bridge / purchase_receipt / delivery.reservations / ap_payment / crm N+1 修复
**状态**：已合并 main HEAD `edd257a3`，PR #312，CI 12/13 全绿（E2E continue-on-error）
**包含项**：5-12/5-13/5-14/5-22/5-23 + 3-18/3-23/3-24/3-25
**改动文件**：inventory_finance_bridge_service.rs / purchase_receipt_service.rs / so/delivery.rs / ap_payment_service.rs / crm/cust.rs / inventory_stock_service.rs
**实现方案**：
- 5-12：合并 get_product_name + get_product_cost_price 为单次 get_product_info 查询（5 个调用点，每条事件由 2 次 product 查询降为 1 次）
- 5-13/3-18：create_receipt 明细改用 insert_many 批量插入（N 次 INSERT→1 次）
- 5-14：release_reservations 按 (product_id, warehouse_id) 聚合后批量 update_many（N 次 UPDATE→G 次）
- 5-22：移除预算核销链中 _request 死查询（4 次串行查询→3 次）
- 5-23：create_stock / create_stock_fabric 新增仓库与产品存在性校验
- 3-23：compute_rfm_score 3 次查询合并为 1 次（内存计算 R/F/M）
- 3-24：Decimal→String→f64 改为 Decimal::to_f64（避免精度丢失）
- 3-25：get_customer_relation_summary 改用数据库聚合 count()+sum()+max()

### 批次 69：缓存语义与事件修复（5-15/5-16/5-17/5-18/5-19/3-21）✅ 已完成

**主题**：cache invalidate_prefix/set_with_ttl + event_bus user_id + 通知角色过滤
**包含项**：5-15/5-16/5-17/5-18/5-19 + 3-21
**改动文件**：cache_service.rs / event_bus.rs / inventory_stock_service.rs / bpm_service.rs / event_kafka_payload.rs / event_kafka.rs / test_event_bus.rs
**完成详情**：
- main HEAD: `50de3cf6`
- PR #313
- CI: 12/13 全绿（E2E continue-on-error）
- 修复内容：
  - 5-15/3-21：check_low_stock 改为 txn 内查询，commit 后批量 publish（需补充 TransactionTrait 导入）
  - 5-16：invalidate_prefix 实现真实前缀失效（维护 key_index HashSet）
  - 5-17：set_with_ttl 实现真实 per-entry TTL（维护 custom_expirations HashMap<String, Instant>）
  - 5-18：BpmProcessFinished 事件添加 approver_id 字段，4 个文件同步修改
  - 5-19：LowStockAlert 通知按角色过滤（admin/manager，系统无 user_role 表，使用 user.role_id + role.code 替代）

### 批次 70：超长函数拆分（1-4/1-5/1-6/1-7/1-8）✅ 已完成

**主题**：production_order / sales_return / voucher / import_export 函数拆分 + 正则预编译
**包含项**：1-4/1-5/1-6/1-7/1-8
**改动文件**：production_order_service.rs / sales_return_service.rs / voucher_service.rs / import_export_service.rs / supplier_service.rs
**状态**：✅ 已完成（PR #314，CI 12/13 全绿，main HEAD `38f7963f`）
**完成详情**：
- 1-4：✅ `production_order_service.rs` `handle_production_completion_inventory_txn`（275 行）拆为 `fetch_default_warehouse_txn` / `deduct_raw_materials_txn` / `increase_finished_goods_txn` 3 个私有方法，主函数保留事件收集与日志
- 1-5：✅ `sales_return_service.rs` `approve_return`（167 行）拆为 `validate_and_lock_submitted_txn` / `apply_stock_inbound_txn`（&self 方法以访问 self.db）/ `mark_approved_txn` / `generate_red_ar_txn` 4 个方法
- 1-6：✅ `voucher_service.rs` `create`（138 行）抽取 `validate_voucher_create_req` / `precheck_subjects_exist_txn` / `insert_voucher_items_txn` 3 个私有方法
- 1-7：✅ `import_export_service.rs` 抽取 `record_import_result` 静态方法消除 products/customers 分支重复代码（原泛型闭包方案过于复杂，改为更简单的方案）
- 1-8：✅ `supplier_service.rs` `validate_mobile_phone` 正则改为模块级 `static MOBILE_PHONE_RE: LazyLock<Regex>`，全局编译一次
**设计说明**：所有拆分仅做职责分离，保持原有业务语义不变；1-5 `apply_stock_inbound_txn` 因 `record_transaction` 内部使用 `self.db`（批次 27 v7 P1 修复提及的遗留问题），改为 `&self` 方法以保留原行为

### 批次 71：前端类型契约与生产代码清理（1-9/1-10/1-11/1-12）✅ 已完成

**主题**：as any 清理 + testData 迁移 + admin 权限码 + real_name 修复
**包含项**：1-9/1-10/1-11/1-12
**改动文件**：missing_handlers.rs / auth_handler.rs / router/index.ts / permission.ts / MainLayout.vue / request.ts / quotation.ts / inventoryTestData.ts
**状态**：✅ 已完成（PR #315，CI 12/13 全绿，main HEAD `a108edbe`）
**完成详情**：
- 1-9：✅ `missing_handlers.rs` `real_name: Some(u.username)` → `None`（User 模型无 real_name 字段）
- 1-10：✅ `testData.ts` 经 `git mv` 迁至 `frontend/tests/fixtures/inventoryTestData.ts`，更新测试 import 路径
- 1-11：✅ `request.ts` 拦截器 `res as any` → `res as unknown as AxiosResponse`；5 个方法签名统一 `data?: unknown` + `res as unknown as T`；`quotation.ts` 16 处 `as any` 改为显式泛型
- 1-12：✅ 权限模型重构 — 后端 `build_with_permissions` 为 `is_system=true` 角色注入 `*:*` 通配权限；前端 `hasRoutePermission` 支持 `*:*`；删除 3 处 `role_name === 'admin'` 硬编码绕过（router/permission/MainLayout）

### 批次 72：接口响应规范（2-6/2-7/2-8/2-9/2-10/2-11/2-12）✅ 已完成

**主题**：CORS PATCH + ApiResponse 包装 + 权限字典 + 批量事务 + 分页统一
**包含项**：2-6/2-7/2-8/2-9/2-10/2-11/2-12
**改动文件**：main.rs / init_handler.rs / role_handler.rs / bulk_product_handler.rs / batch_service.rs / utils/response.rs / fixed_asset_handler.rs / sales.rs / purchase.rs / finance.rs
**状态**：✅ 已完成（PR #316，CI 12/13 全绿，main HEAD `c1c5f4a3`）
**完成详情**：
- 2-6：✅ `main.rs` CORS `allow_methods` 补齐 `Method::PATCH`
- 2-7：✅ `init_handler.rs` `get_task_status` 定义 `TaskStatusResponse` DTO，改用 `ApiResponse::success`
- 2-8：✅ `role_handler.rs` `list_permissions` 从 `role_permission` 表 distinct 查询替代 24 项硬编码（按 (resource_type, action) 去重）
- 2-9：✅ `bulk_product_handler.rs` 3 个 DTO 加 `#[validate(length(max=500))]`；`batch_service.rs` 批量创建/更新用 `db.begin()` 包裹事务，任一失败回滚全部；审计日志记录在主连接（非事务）确保回滚后可追溯
- 2-10：✅ `response.rs` 移除 `PaginatedResponse<T>` 的 `IntoResponse` 实现（原实现丢失结构化分页信息），强制新代码使用 `ApiResponse::success_paginated`
- 2-11：✅ `sales.rs`/`purchase.rs`/`finance.rs` 路由模块文档标注"动作端点"语义（POST /resource/:id/{action} 短期保留以兼容前端，长期计划重构为 PATCH）
- 2-12：✅ `fixed_asset_handler.rs` 分页默认值 `unwrap_or_default()`（0）改为 `unwrap_or(1).clamp(1,10000)`
**CI 修复历程**：3 次 push — ① `json!` 宏内 `[] as Vec<usize>` 语法错误改为 `errors.iter().map(|e| e.index).collect::<Vec<_>>()`；② `BatchCreateProductRequest`/`BatchUpdateProductRequest` 补 `Serialize` derive 满足 `validator::Validate` 的 `add_param<T: Serialize>` 约束

### 批次 73：审计日志完善（8-10/8-11/8-12/8-13/8-14）

**主题**：search_logs 过滤 + dashboard 统计 + 字段裁剪 + 保留策略 + role_permission 审计
**包含项**：8-10/8-11/8-12/8-13/8-14
**改动文件**：omni_audit_handler.rs / omni_audit_service.rs / audit_cleanup_service.rs / role_handler.rs

### 批次 74：安全加固（7-6/7-7/7-8/7-9/7-10/7-11/7-12/7-13）

**主题**：JWT 算法固定 + 密码策略 + CSRF + refresh 绑定 + leeway + IP 提取 + 截断 + track_event
**包含项**：7-6/7-7/7-8/7-9/7-10/7-11/7-12/7-13
**改动文件**：auth_service.rs / auth_handler.rs / auth_handler_misc.rs / middleware/omni_audit.rs / omni_audit_handler.rs

### 批次 75：业务逻辑剩余 P2（3-14/3-15/3-16/3-17/3-19/3-20/3-22 + 侧边栏 4-3/4-4/4-5）

**主题**：ap_verification 状态 + ap_invoice 退货 + 成本价守卫 + 权限模型 + 信用检查 + 期间锁定 + 侧边栏动态化
**包含项**：3-14/3-15/3-16/3-17/3-19/3-20/3-22 + 4-3/4-4/4-5
**改动文件**：ap_verification_service.rs / ap_invoice_service.rs / inventory_finance_bridge_service.rs / purchase_receipt_service.rs / so/order_workflow.rs / ar_collection_service.rs / MainLayout.vue / StockTab.vue / router/index.ts

---

## 第三阶段：P3 修复（批次 76-77，共 2 批）

### 批次 76：代码风格清理（1-13/1-14/1-15/1-16/1-17 + 2-13/2-14/2-15 + 3-26/3-27/3-28/3-29/3-30/3-31 + 4-6/4-7 + 5-26/5-27/5-28/5-29）

**主题**：i18n 迁移 + 死代码清理 + console.error + unwrap + Decimal 转换 + 审计补齐 + 路由 meta
**包含项**：所有代码风格类 P3
**改动文件**：多文件批量

### 批次 77：测试边界与审计清理（6-8/6-9/6-10/6-11/6-12/6-13 + 7-14/7-15/7-16/7-17 + 8-16/8-17/8-18/8-19/8-20）

**主题**：冒烟测试强断言 + 核心 service 单元测试补充 + HSTS + 密码策略接入 + 审计死代码清理
**包含项**：所有测试与审计类 P3
**改动文件**：frontend/e2e/smoke/*.spec.ts / backend/tests/ / 多 service 补测试 / main.rs / password_policy_service.rs / audit_log_service.rs

---

## 进度跟踪

| 批次 | 主题 | 级别 | 项数 | 状态 |
|------|------|------|------|------|
| 57 | 安全认证链路 | P1 | 5 | ✅ 已完成（代码已修复，纯核验） |
| 58 | 操作审计覆盖 | P1 | 6 | ✅ 已完成 |
| 59 | 审计日志质量 | P1 | 5 | ✅ 已完成（含 59b user_id 透传） |
| 60 | 单号生成器统一 | P1 | 6 | ✅ 已完成 |
| 61 | 状态机 lock_exclusive | P1 | 6 | ✅ 已完成 |
| 62 | 业务流断点 | P1 | 6 | ✅ 已完成 |
| 63 | 事务边界原子性 | P1 | 5 | ✅ 已完成 |
| 64 | 接口越权防护 | P1 | 7 | ✅ 已完成 |
| 65 | 测试资产伪测试清理 | P1 | 6 | ✅ 已完成 |
| 66 | E2E 环境补齐 | P1 | 1 | ✅ 已完成 |
| 67 | BPM 定义占位 | P1 | 1 | ✅ 已完成 |
| 68 | N+1 查询优化 | P2 | 9 | ✅ 已完成 |
| 69 | 缓存语义与事件 | P2 | 6 | ✅ 已完成 |
| 70 | 超长函数拆分 | P2 | 5 | ✅ 已完成 |
| 71 | 前端类型契约 | P2 | 4 | ✅ 已完成 |
| 72 | 接口响应规范 | P2 | 7 | ✅ 已完成 |
| 73 | 审计日志完善 | P2 | 5 | 待启动 |
| 74 | 安全加固 | P2 | 8 | 待启动 |
| 75 | 业务逻辑剩余 P2 | P2 | 10 | 待启动 |
| 76 | 代码风格清理 | P3 | 21 | 待启动 |
| 77 | 测试边界与审计清理 | P3 | 15 | 待启动 |

**P1 完成后**：全项目复审（按八维度重新审计）
**P2 完成后**：全项目复审
**P3 完成后**：全项目复审，循环直到无问题

---

## 验证标准

每个批次必须满足：
1. CI 13 job 中 12 个关键 job 全绿（E2E continue-on-error 不阻塞）
2. Rust 构建 / Clippy / 单元测试 / 格式 全部 success
3. 前端构建 / 测试 / lint / 类型检查 全部 success
4. 修复内容与审计报告项一一对应
5. commit message 使用中文，描述"做了什么"和"为什么"
6. 死代码按项目规范第六节处理（项级 `#[allow(dead_code)]` + TODO，禁止文件级全局抑制）
