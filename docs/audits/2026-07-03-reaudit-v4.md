# v4 第四轮复审报告

**审计日期**：2026-07-03
**审计基线**：main HEAD `ff88f81`（批次 89-92 全部合并后）
**审计范围**：三维度并行复审 — 修复质量验证 + 占位符/TODO 扫描 + 安全/并发/事务边界
**复审方法**：3 个 search agent 并行，覆盖 service/handler/model/migration/route/cli/frontend 全链路

## 一、审计结果总览

| 级别 | 数量 | 说明 |
|------|------|------|
| P0 | 0 | 无致命问题 |
| P1 | 9 | 事务边界 TOCTOU（delete 方法漏修）+ id:Set(0) 推广未完成 |
| P2 | 15 | LIKE 模式注入 + 缺失审计 + N+1 查询 + 前端占位 + 客户端 user_id |
| P3 | 20 | 分页 clamp 推广 + panic! 修复对齐 + dead_code 清理 + 前端占位 |
| **合计** | **44** | |

## 二、批次 89-92 修复质量验证

| 批次 | 项数 | ✅ 通过 | ⚠️ 有问题 | ❌ 未修复 |
|------|------|--------|----------|----------|
| 89 P1 | 3 项 | 3 | 0 | 0 |
| 90 P2 | 4 项 | 4 | 0 | 0 |
| 90b P2-12 | 1 项 | 1 | 0 | 0 |
| 91 P0-1 | 1 项 | 1 | 0 | 0 |
| 92 P3 A 死代码 | 5 项 | 5 | 0 | 0 |
| 92 P3 B panic/吞错 | 2 项 | 1 | 1 | 0 |
| 92 P3 C 业务逻辑 | 6 项 | 6 | 0 | 0 |
| **合计** | **22 项** | **21** | **1** | **0** |

**结论**：批次 89-92 修复质量优秀，22 项中 21 项完整通过。主要遗漏是修复未推广到同类问题（id:Set(0) 仅修 2 处，另有 13 处未修；delete 方法 lock_exclusive 仅修部分 service）。

## 三、P1 问题清单（9 项）

### P1-1：ar/recon.rs delete 方法 TOCTOU 未修复
- **位置**：`backend/src/services/ar/recon.rs:150-170`
- **问题**：批次 92 P3-9 仅修复 user_id 注入，但状态门（draft）+ delete 仍在 `&*self.db` 上执行，无 txn 无 lock_exclusive。同文件 update/send/dispute/close 都已修复 lock_exclusive，唯独 delete 漏修
- **修复**：参考 update 方法，begin txn + lock_exclusive + 状态门 + delete_with_audit(&txn)

### P1-2：sales_return_service.rs delete_return TOCTOU + 缺失审计
- **位置**：`backend/src/services/sales_return_service.rs:569-578`
- **问题**：状态门（DRAFT）+ delete 全在 self.db，无 txn 无 lock_exclusive；handler 用 `_auth` 未传 user_id，无审计
- **修复**：补 txn + lock_exclusive + user_id 注入 + delete_with_audit

### P1-3：sales_return_service.rs delete_return_item TOCTOU
- **位置**：`backend/src/services/sales_return_service.rs:735-744`
- **问题**：find + 状态门在 self.db，delete_by_id 在 txn，TOCTOU 窗口
- **修复**：状态门移入 txn + lock_exclusive

### P1-4：production_order_service.rs delete TOCTOU + 缺失审计
- **位置**：`backend/src/services/production_order_service.rs:333-348`
- **问题**：软删除（设 CANCELLED）无 txn 无 lock_exclusive，签名不接收 user_id，无审计
- **修复**：补 txn + lock_exclusive + user_id + update_with_audit

### P1-5：supplier_service.rs delete_supplier TOCTOU + 缺失审计
- **位置**：`backend/src/services/supplier_service.rs:340-352`
- **问题**：can_delete_supplier + get_supplier + delete 三步分离，无 txn 无 lock_exclusive，无审计，不接收 user_id
- **修复**：补 txn + lock_exclusive + user_id + delete_with_audit

### P1-6：supplier_service.rs delete_supplier_contact 缺失审计
- **位置**：`backend/src/services/supplier_service.rs:524-527`
- **问题**：get_contact + delete 无 txn 无 lock_exclusive，无审计
- **修复**：补 txn + lock_exclusive + user_id + delete_with_audit

### P1-7：voucher_service.rs delete TOCTOU + 缺失审计
- **位置**：`backend/src/services/voucher_service.rs:426-447`
- **问题**：状态门（draft）+ delete 全在 self.db，无 txn 无 lock_exclusive；同文件 submit/post/audit 都已加 lock_exclusive，唯独 delete 漏修；签名不接收 user_id
- **修复**：补 txn + lock_exclusive + user_id + delete_with_audit

### P1-8：inventory_adjustment_service.rs delete_adjustment_item TOCTOU
- **位置**：`backend/src/services/inventory_adjustment_service.rs:595-625`
- **问题**：状态门（pending）在 txn 之外，并发 approve 可绕过门控
- **修复**：状态门移入 txn + lock_exclusive

### P1-9：id: Set(0) 主键冲突推广修复（13 处）
- **位置**：13 处
  - `ar/recon.rs:37`、`ar/vfy.rs:133,172,191,227,249,274,296,539,565,586`（10 处）
  - `batch_service.rs:110`、`finance_payment_service.rs:72`
  - `inventory_adjustment_service.rs:82,130,515`
  - `po/receipt.rs:343`、`po/price.rs:182,290`、`po/order.rs:310`
  - `product_service.rs:369`、`user_service.rs:143`
  - `inventory_stock_query.rs:43`、`operation_log_service.rs:53`
  - `business_trace_service.rs:163`、`purchase_contract_service.rs:192`
- **问题**：批次 89 P1-1 仅修复 fixed_asset_service 2 处，上述 13 处仍用 `id: Set(0)`，第二次插入触发主键冲突
- **修复**：全部改为 `id: Default::default()` 或 `..Default::default()`

## 四、P2 问题清单（15 项）

### P2-1：audit_cleanup_service.rs format! 拼接 SQL
- **位置**：`backend/src/services/audit_cleanup_service.rs:52-55, 74-77`
- **修复**：改用 `Statement::from_sql_and_values` 参数化绑定

### P2-2：LIKE 模式注入 - services 层 5 处
- **位置**：`color_card_crud_service.rs:109`、`quotation_service.rs:191`、`import_export_service.rs:610,673,733`、`custom_order_crud_service.rs:144`、`mrp_engine_service.rs:746`
- **修复**：改用 `crate::utils::sql_escape::safe_like_pattern`

### P2-3：LIKE 模式注入 - handlers 层 3 处
- **位置**：`omni_audit_handler.rs:226`、`audit_log_handler.rs:172`、`slow_query_handler.rs:111`
- **修复**：同 P2-2

### P2-4：batch_service.rs N+1 查询
- **位置**：`backend/src/services/batch_service.rs:249-253`
- **修复**：循环内 find_by_id 改为 is_in 批量查询 + HashMap

### P2-5：color_price_seasonal_service.rs delete 缺失审计
- **位置**：`backend/src/services/color_price_seasonal_service.rs:168-172`
- **修复**：补 user_id + delete_with_audit

### P2-6：sales_return_handler.rs delete_sales_return `_auth` 未用
- **位置**：`backend/src/handlers/sales_return_handler.rs:126-137`
- **修复**：`_auth` → `auth`，注入 `auth.user_id`

### P2-7：ar_report_handler.rs 4 个接口 `_auth` 未用
- **位置**：`backend/src/handlers/ar_report_handler.rs:23,40,57,74`
- **修复**：评估数据权限过滤需求

### P2-8：material_shortage_handler.rs `_auth` 未用
- **位置**：`backend/src/handlers/material_shortage_handler.rs:61`

### P2-9：fixed_asset_handler batch_depreciate 客户端 user_id
- **位置**：`backend/src/handlers/fixed_asset_handler.rs:314,325`
- **修复**：DTO 移除 user_id 字段，handler 使用 auth.user_id

### P2-10：审计日志 Some(0) 占位 user_id（约 40 处）
- **位置**：bom_service、finance_invoice_service、role_permission_service、sales_return_service、crm/lead、crm/opp、crm/recycle_rule、purchase_receipt_private、po/receipt、ap_payment_request_service、product_service、product_category_service、color_price_tier_service、inventory_adjustment_service、inv/inventory_move、supplier_service、sales_fabric_order_handler、warehouse_handler、inventory_batch_handler、logistics_handler 等
- **修复**：service 方法签名补 user_id，handler 注入 auth.user_id

### P2-11：事件发布/审计 let _ = 吞错（约 20 处）
- **位置**：purchase_price_service、sales_price_service、sales_contract_service、bpm_service、sales_order_handler、inventory_stock_handler 等
- **修复**：改为 `?` 传播或记录 warn 日志

### P2-12：前端 P1 占位功能
- `quality/index.vue:456` 更新功能未实现
- `inventory/tabs/StockTab.vue` 4 处占位（编辑/删除/批量调整/批量删除）
- `purchase-contract/composables/usePcProc.ts:74` 导出假成功
- `crm/leads/index.vue:366` 导出假成功
- `security/two-factor/composables/useTfaProc.ts:79,110` 恢复码客户端生成
- `custom-orders/list.vue:202` 操作人 ID 占位为 1

### P2-13：quotation_handler/quotation_service 审计 user_id 占位
- **位置**：`quotation_handler.rs:199` `let _ = auth;`、`quotation_service.rs:406` `let _ = user_id;`
- **修复**：接入 AuthContext 注入 user_id

### P2-14：custom_order_crud_service cancel reason 占位
- **位置**：`custom_order_crud_service.rs:255` `let _ = dto.reason;`
- **修复**：记录到 process_log

### P2-15：custom_order_quality_service operator_id 占位
- **位置**：`custom_order_quality_service.rs:129` `let _ = dto.operator_id;`
- **修复**：记录到 audit_log

## 五、P3 问题清单（20 项）

### P3-1：failover_service.rs panic! 修复与计划不符
- **位置**：`backend/src/services/failover_service.rs:134,140`
- **问题**：批次 92 P3-7 计划"改为 Result/日志"，实际仍 panic!，仅加 tracing::error!

### P3-2：finance_report_service.rs 生产代码 .unwrap()
- **位置**：`backend/src/services/finance_report_service.rs:632,636`

### P3-3~P3-8：分页参数未 clamp 上限（30+ 处）
- **位置**：30+ handler 仅 `.unwrap_or(1)` 未做 `.clamp(1, 100)`
- **修复**：统一加 `.clamp(1, 100)`

### P3-9：api_gateway_handler.rs key_to_json created_by 占位 0
- **位置**：`backend/src/handlers/api_gateway_handler.rs:153-154`

### P3-10：api_gateway_handler.rs create_api_endpoint 唯一性检查 TOCTOU
- **位置**：`backend/src/handlers/api_gateway_handler.rs:230-237`

### P3-11：fixed_asset_service.rs P2-1 唯一约束匹配宽泛
- **位置**：`backend/src/services/fixed_asset_service.rs:313-316`

### P3-12：cli/util/service.rs cmd_logs 仍吞错
- **位置**：`backend/src/cli/util/service.rs:125,137,147`

### P3-13：cli/util/upgrade.rs 关键路径失败不中止
- **位置**：`backend/src/cli/util/upgrade.rs:126-137,191-216,225-237`

### P3-14：.env.example ENV 变量重复定义
- **位置**：`backend/.env.example:40,47`

### P3-15：bpm_service_stub.rs 文件名误导
- **位置**：`backend/src/services/bpm_service_stub.rs`

### P3-16：routes/v1.rs 占位路由
- **位置**：`backend/src/routes/v1.rs:1-23`

### P3-17~P3-20：前端 P2/P3 占位功能
- `sales-price/composables/useSpProc.ts:61` 价格策略占位
- `sales-analysis/composables/useSaProc.ts:9` 编辑目标占位
- `crm/opportunities/index.vue:311` 查看详情空函数
- `sales-analysis/components/SaTrend.vue:22,36` 图表占位

## 六、已修复确认（验证通过）

以下批次 89-92 修复项经复核确实修复到位：

1. ✅ fixed_asset_service.rs `depreciate`/`dispose` 状态门 + lock_exclusive + txn + 纯计算函数复用
2. ✅ fixed_asset_service.rs `id: Set(0)` → `Default::default()`（2 处）
3. ✅ fixed_asset_service.rs 已足额折旧短路 + 封顶 + 幂等返回
4. ✅ fixed_asset_service.rs batch_calculate_depreciation 长度校验 + paginate 流式
5. ✅ ar/recon.rs `update/send/dispute/close/update_status` lock_exclusive + user_id 注入
6. ✅ color_card_crud_service.rs `update/archive/mark_lost` lock_exclusive + user_id 注入
7. ✅ omni_audit_service.rs panic! → match + return Err（闭包 return 陷阱修复）
8. ✅ cli/util 36 处 `let _ = run_cmd(...)` 改为带日志错误处理
9. ✅ m0037 迁移：外键 ON DELETE RESTRICT + DROP 冗余索引
10. ✅ 中间件挂载完整、迁移注册完整（m0001-m0037）

## 七、修复建议优先级

### 批次 93：P1 修复（9 项）
1. P1-9 id:Set(0) 推广修复（13 处，机械修复低风险）
2. P1-1 ar/recon.rs delete 补 lock_exclusive
3. P1-7 voucher_service.rs delete 补 lock_exclusive + user_id
4. P1-4 production_order_service.rs delete 补 lock_exclusive + user_id
5. P1-5 supplier_service.rs delete_supplier 补 txn + lock_exclusive + user_id
6. P1-2 sales_return_service.rs delete_return 补 lock_exclusive + user_id
7. P1-3 sales_return_service.rs delete_return_item 补 lock_exclusive
8. P1-6 supplier_service.rs delete_supplier_contact 补 txn + user_id
9. P1-8 inventory_adjustment_service.rs delete_adjustment_item 补 lock_exclusive

### 批次 94：P2 修复（15 项）
- LIKE 模式 safe_like_pattern 推广（8 处）
- audit_cleanup_service format! SQL 改参数化
- batch_service N+1 改 is_in 批量查询
- 审计日志 Some(0) 占位 user_id 推广（40 处）
- 前端 P1 占位功能实现

### 批次 95：P3 修复（20 项）
- 分页 clamp 推广（30+ 处）
- failover_service panic! 真正改 Result
- dead_code 清理
- 前端 P2/P3 占位功能
