# Wave 4 P2-2 性能优化实施计划

> **For agentic workers:** 本计划基于 [`docs/superpowers/specs/2026-06-16-wave4-p2-2-perf-design.md`](file:///workspace/docs/superpowers/specs/2026-06-16-wave4-p2-2-perf-design.md) 编写。
> 涵盖阶段一（基线扫描）PR-1/2 和阶段二（选择性优化）PR-3+ 的完整 TDD 实施步骤。
>
> **基线版本**：origin/test @ 9b1bf80（PR #119 squash merge）

---

## 〇、前置准备

### 0.1 用户硬性规则（执行前必读）

1. **CI/CD 优先**：所有验证通过 GitHub Actions，**禁止本地** `cargo` / `npm` / `vue-tsc` / `tsc` / `vite build` / `pg_dump` 等命令
2. **GitHub API 访问**：从 `.git/config` origin URL 提取 token
3. **Git 工作流**：feature/ 分支 + squash merge + delete_branch
4. **子代理调度**：单子代理串行（避免云端卡死，已在 Wave 2-4 验证）
5. **PR 触发 CI**：push 后立即创建 PR，CI 4 job 验证
6. **不提交敏感信息**：DB 密码从环境变量读取，不入版本库

### 0.2 GitHub Token 提取

```bash
TOKEN=$(grep -oP 'x-access-token:\K[^@]+' .git/config)
```

### 0.3 PR 强依赖关系

- PR-1（基线脚本）独立实施
- PR-2（基线报告）依赖 PR-1（脚本输出）
- PR-3+（优化 PR）依赖 PR-2（基线数据决定范围）
- **PR-1 → PR-2 → PR-3+ 串行执行**

### 0.4 工作流规范

每个 PR 严格按以下流程：

1. **创建分支**：`git checkout -b feature/p2-2-{name}`
2. **实施**：编写文件（不验证本地构建）
3. **提交**：`git add` + `git commit`（使用 `-c user.name/email` 避免 git config 缺失）
4. **推送**：`git push origin feature/p2-2-{name}`
5. **创建 PR**：`curl -X POST .../pulls` 指向 test 分支
6. **轮询 CI**：`for i in {1..60}; do ...; sleep 60; done`
7. **Merge**：`curl -X PUT .../pulls/{N}/merge -d '{"merge_method":"squash","delete_branch":true}'`
8. **清理本地**：`git checkout test && git pull && git branch -D feature/...`

### 0.5 文件结构总览

```
frontend/scripts/p2-2-perf-baseline.mjs    # 新增（PR-1）
backend/scripts/p2-2-n1-scan.sh            # 新增（PR-1）
backend/scripts/p2-2-slow-query.sql        # 新增（PR-1）
docs/superpowers/plans/2026-06-16-p2-2-perf-baseline.md  # 新增（PR-2）
docs/superpowers/plans/2026-06-16-p2-2-n+1-list.md       # 新增（PR-2）
docs/superpowers/plans/2026-06-16-p2-2-slow-query-list.md  # 新增（PR-2）
（PR-3+ 按基线数据决定）
```

---

## 一、PR-1：基线脚本

### 任务 1.1：创建分支

```bash
cd /workspace
git checkout test
git pull origin test
git checkout -b feature/p2-2-baseline-scripts
```

### 任务 1.2：创建前端基线脚本

**新文件**：[`frontend/scripts/p2-2-perf-baseline.mjs`](file:///workspace/frontend/scripts/p2-2-perf-baseline.mjs)

**功能**：
- 连接生产库（39.99.34.194:5432）查询 4 个 V2Table 页面对应表的行数
- 输出 markdown 表格到 stdout
- DB 密码从环境变量 `DB_PASSWORD` 读取

**代码骨架**（约 80 行）：

```javascript
#!/usr/bin/env node
// P2-2 前端基线脚本 - 数据源表行数扫描
// 用法：DB_PASSWORD=xxx node scripts/p2-2-perf-baseline.mjs

import pg from 'pg'
const { Client } = pg

const config = {
  host: '39.99.34.194',
  port: 5432,
  user: 'bingxi',
  password: process.env.DB_PASSWORD,
  database: 'bingxi_erp'
}

const queries = [
  { table: 'inventory_stock', page: 'StockTab', expected: '>= 10k' },
  { table: 'sales_orders', page: 'OrderListView', expected: '>= 1k' },
  { table: 'production_orders', page: 'production', expected: '>= 1k' },
  { table: 'quality_inspection_records', page: 'RecordTab', expected: '>= 5k' }
]

async function main() {
  // 安全检查：DB_PASSWORD 必须从环境变量提供
  if (!process.env.DB_PASSWORD) {
    console.error('错误：DB_PASSWORD 环境变量未设置')
    console.error('用法：DB_PASSWORD=xxx node scripts/p2-2-perf-baseline.mjs')
    process.exit(1)
  }

  const client = new Client(config)
  try {
    await client.connect()
    console.log('# P2-2 前端基线 - V2Table 数据源表行数')
    console.log('')
    console.log('| 页面 | 表名 | 行数 | 期望 | 状态 |')
    console.log('|------|------|------|------|------|')
    for (const { table, page, expected } of queries) {
      try {
        const { rows } = await client.query(`SELECT COUNT(*)::int AS count FROM ${table}`)
        const count = rows[0].count
        // 简单状态判断：>= 期望值认为满足
        const status = count >= 1000 ? '✅' : '⚠️'
        console.log(`| ${page} | ${table} | ${count} | ${expected} | ${status} |`)
      } catch (err) {
        console.log(`| ${page} | ${table} | ERROR | ${expected} | ❌ |`)
        console.error(`  ${table}: ${err.message}`)
      }
    }
  } catch (err) {
    console.error('数据库连接失败:', err.message)
    process.exit(1)
  } finally {
    await client.end()
  }
}

main().catch(console.error)
```

**验证**：仅检查文件语法（`node --check frontend/scripts/p2-2-perf-baseline.mjs`），不实际执行（避免本地连接生产库）

### 任务 1.3：创建后端 N+1 扫描脚本

**新文件**：[`backend/scripts/p2-2-n1-scan.sh`](file:///workspace/backend/scripts/p2-2-n1-scan.sh)

**功能**：grep 扫描 `backend/src/services/**/*.rs`，识别 N+1 风险

**代码骨架**（约 30 行）：

```bash
#!/bin/bash
# P2-2 后端 N+1 扫描脚本
# 用法：bash scripts/p2-2-n1-scan.sh

set -e
cd "$(dirname "$0")/.."

echo "# P2-2 后端 N+1 风险扫描"
echo ""

echo "## 1. find_with_related / find_related 模式"
echo ""
echo "| 文件 | 行号 | 模式 |"
echo "|------|------|------|"
grep -rn "find_with_related\|find_related" src/services/ 2>/dev/null | \
  head -30 | \
  awk -F: '{ printf "| %s | %s | %s |\n", $1, $2, $3 }'

echo ""
echo "## 2. 循环中调用其他 Service 的模式（潜在 N+1）"
echo ""
echo "注：以下模式需人工 review 确认是否 N+1"
echo ""
grep -rn "for .* in .*{" src/services/ -A 3 2>/dev/null | \
  grep -B 1 "service::" | \
  head -30 | \
  awk -F: '{ printf "| %s | %s | 候选 |\n", $1, $2 }'

echo ""
echo "## 3. 总结"
TOTAL=$(grep -rn "find_with_related\|find_related" src/services/ 2>/dev/null | wc -l)
echo "- find_with_related 总数：$TOTAL"
```

### 任务 1.4：创建慢查询 SQL 脚本

**新文件**：[`backend/scripts/p2-2-slow-query.sql`](file:///workspace/backend/scripts/p2-2-slow-query.sql)

**功能**：输出 3 个 SQL 查询，供生产库运维人员执行

**代码骨架**（约 50 行）：

```sql
-- P2-2 慢查询扫描
-- 用法：psql -h 39.99.34.194 -U bingxi -d bingxi_erp -f scripts/p2-2-slow-query.sql
-- 输出：3 个查询结果，用于 Wave 4 P2-2 基线报告

-- 1. 高 seq_scan 表（缺索引）
\echo ''
\echo '## 1. 高 seq_scan 表（缺索引，seq_scan 占比 > 50%）'
\echo ''

SELECT
  schemaname,
  relname,
  seq_scan,
  idx_scan,
  ROUND(100.0 * seq_scan / NULLIF(seq_scan + idx_scan, 0), 2) AS seq_pct
FROM pg_stat_user_tables
WHERE seq_scan > 0
  AND (seq_scan + idx_scan) > 100
ORDER BY seq_pct DESC
LIMIT 20;

-- 2. 未使用索引
\echo ''
\echo '## 2. 未使用索引（idx_scan = 0）'
\echo ''

SELECT
  schemaname,
  relname,
  indexrelname,
  pg_size_pretty(pg_relation_size(indexrelid)) AS size
FROM pg_stat_user_indexes
WHERE idx_scan = 0
ORDER BY pg_relation_size(indexrelid) DESC
LIMIT 20;

-- 3. 慢查询（pg_stat_statements，扩展可能未启用）
\echo ''
\echo '## 3. Top 20 慢 SQL（需要 pg_stat_statements 扩展）'
\echo ''

DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM pg_extension WHERE extname = 'pg_stat_statements') THEN
    PERFORM 1;
  ELSE
    RAISE NOTICE 'pg_stat_statements 扩展未启用，跳过此查询';
    RAISE NOTICE '启用方式：shared_preload_libraries = pg_stat_statements';
  END IF;
END $$;

-- 如果启用，执行：
SELECT calls, mean_exec_time, query
FROM pg_stat_statements
ORDER BY mean_exec_time DESC
LIMIT 20;
```

### 任务 1.5：提交 PR-1

```bash
cd /workspace
git add frontend/scripts/p2-2-perf-baseline.mjs \
        backend/scripts/p2-2-n1-scan.sh \
        backend/scripts/p2-2-slow-query.sql

git -c user.name="bingxi-erp" -c user.email="noreply@bingxi-erp.local" \
  commit -m "feat(perf): P2-2 性能基线扫描脚本

- frontend/scripts/p2-2-perf-baseline.mjs：V2Table 数据源表行数
- backend/scripts/p2-2-n1-scan.sh：后端 N+1 风险扫描
- backend/scripts/p2-2-slow-query.sql：pg_stat 慢查询扫描
- DB 密码从环境变量 DB_PASSWORD 读取（不硬编码）"

git push origin feature/p2-2-baseline-scripts
```

**创建 PR**：

```bash
TOKEN=$(grep -oP 'x-access-token:\K[^@]+' .git/config)
curl -s -H "Authorization: token $TOKEN" -X POST \
  "https://api.github.com/repos/57231307/1/pulls" \
  -d "$(cat <<'EOF'
{
  "title": "feat(perf): P2-2 性能基线扫描脚本",
  "head": "feature/p2-2-baseline-scripts",
  "base": "test",
  "body": "## 变更内容\n\n### 基线脚本（3 文件，160 行）\n- frontend/scripts/p2-2-perf-baseline.mjs（80 行）：V2Table 数据源表行数\n- backend/scripts/p2-2-n1-scan.sh（30 行）：N+1 风险扫描\n- backend/scripts/p2-2-slow-query.sql（50 行）：pg_stat 慢查询\n\n### 安全\n- DB 密码从 DB_PASSWORD 环境变量读取\n- 所有脚本仅 SELECT，不修改数据\n\n### 验证\n- [x] 脚本仅本地 dry-run（CI 不执行）\n- [x] 不硬编码敏感信息"
}
EOF
)"
```

**轮询 CI**：

```bash
TOKEN=$(grep -oP 'x-access-token:\K[^@]+' .git/config)
COMMIT_SHA=$(git rev-parse HEAD)
PR_NUM=<从 PR 创建响应获取>

for i in {1..60}; do
  STATUS=$(curl -s -H "Authorization: token $TOKEN" \
    "https://api.github.com/repos/57231307/1/commits/$COMMIT_SHA/check-runs" | \
    python3 -c "import sys,json; d=json.load(sys.stdin); runs=d.get('check_runs',[]); print('ALL_GREEN' if runs and all(r['conclusion']=='success' for r in runs if r['status']=='completed') else 'PENDING' if any(r['status']!='completed' for r in runs) else 'HAS_FAILURE')")
  echo "[$i/60] CI: $STATUS"
  if [ "$STATUS" = "ALL_GREEN" ] || [ "$STATUS" = "HAS_FAILURE" ]; then break; fi
  sleep 60
done
```

**Merge**（仅 ALL_GREEN 时执行）：

```bash
TOKEN=$(grep -oP 'x-access-token:\K[^@]+' .git/config)
curl -s -H "Authorization: token $TOKEN" -X PUT \
  "https://api.github.com/repos/57231307/1/pulls/$PR_NUM/merge" \
  -d '{"merge_method":"squash","commit_title":"feat(perf): P2-2 性能基线扫描脚本 (#XXX)","delete_branch":true}'
```

**清理本地**：

```bash
git checkout test
git pull origin test
git branch -D feature/p2-2-baseline-scripts
```

---

## 二、PR-2：基线报告

### 任务 2.1：创建分支

```bash
cd /workspace
git checkout test
git pull origin test
git checkout -b feature/p2-2-baseline-reports
```

### 任务 2.2：执行基线脚本（本地 dry-run，CI 不执行）

**前置**：DB 密码需要用户提供（或仅在报告里说明"待执行"）

**执行命令**（仅在有 DB_PASSWORD 时执行）：

```bash
export DB_PASSWORD=<从环境变量或用户提供>

# 1. 前端表行数
node frontend/scripts/p2-2-perf-baseline.mjs > /tmp/p2-2-frontend-baseline.md

# 2. 后端 N+1 扫描
bash backend/scripts/p2-2-n1-scan.sh > /tmp/p2-2-n1-list.md

# 3. 慢查询扫描（需 psql 客户端）
PGPASSWORD=$DB_PASSWORD psql -h 39.99.34.194 -U bingxi -d bingxi_erp \
  -f backend/scripts/p2-2-slow-query.sql > /tmp/p2-2-slow-query.md
```

**降级方案**：若无法本地执行（无 psql / 无生产库访问），在报告中明确说明"需生产环境执行"并提供执行指引

### 任务 2.3：创建前端基线报告

**新文件**：[`docs/superpowers/plans/2026-06-16-p2-2-perf-baseline.md`](file:///workspace/docs/superpowers/plans/2026-06-16-p2-2-perf-baseline.md)

**模板**（约 200 行）：

```markdown
# Wave 4 P2-2 前端性能基线报告

> **执行日期**：2026-06-16
> **基线版本**：origin/test @ <PR-1 SHA>
> **数据源**：生产库 39.99.34.194:5432

## 一、4 V2Table 页面数据源表行数

| 页面 | 表名 | 行数 | 期望 | 状态 |
|------|------|------|------|------|
| StockTab | inventory_stock | <COUNT> | >= 10k | ✅/⚠️ |
| OrderListView | sales_orders | <COUNT> | >= 1k | ✅/⚠️ |
| production | production_orders | <COUNT> | >= 1k | ✅/⚠️ |
| RecordTab | quality_inspection_records | <COUNT> | >= 5k | ✅/⚠️ |

## 二、性能基线评估

### 2.1 V2Table 性能参数

- estimated-row-height：48px（默认值）
- header-height：48px
- fixed：true
- 虚拟滚动：自动

### 2.2 已知问题

（如有）

### 2.3 优化建议

（如有）

## 三、附录

### 3.1 脚本执行命令

```bash
export DB_PASSWORD=xxx
node frontend/scripts/p2-2-perf-baseline.mjs
```

### 3.2 输出示例

```
| 页面 | 表名 | 行数 | 期望 | 状态 |
|------|------|------|------|------|
| StockTab | inventory_stock | 12345 | >= 10k | ✅ |
...
```
```

### 任务 2.4：创建 N+1 清单

**新文件**：[`docs/superpowers/plans/2026-06-16-p2-2-n+1-list.md`](file:///workspace/docs/superpowers/plans/2026-06-16-p2-2-n+1-list.md)

**模板**（约 100 行）：

```markdown
# Wave 4 P2-2 后端 N+1 风险清单

> **执行日期**：2026-06-16
> **扫描范围**：backend/src/services/**/*.rs（约 449 个 API 函数）

## 一、find_with_related / find_related 模式

| 文件 | 行号 | 模式 | 影响接口 | 风险等级 |
|------|------|------|----------|----------|
| <FILE> | <LINE> | <PATTERN> | <API> | 高/中/低 |

## 二、循环中调用 Service 的模式（人工 review）

| 文件 | 行号 | 模式 | 状态 |
|------|------|------|------|
| <FILE> | <LINE> | <PATTERN> | ✅ 非 N+1 / ⚠️ N+1 |

## 三、修复建议

### 3.1 高风险（建议 Wave 4 修复）

（如有）

### 3.2 中风险（建议 Wave 5 修复）

（如有）

### 3.3 低风险（暂不修复）

（如有）

## 四、附录

### 4.1 扫描命令

```bash
bash backend/scripts/p2-2-n1-scan.sh
```
```

### 任务 2.5：创建慢查询清单

**新文件**：[`docs/superpowers/plans/2026-06-16-p2-2-slow-query-list.md`](file:///workspace/docs/superpowers/plans/2026-06-16-p2-2-slow-query-list.md)

**模板**（约 100 行）：

```markdown
# Wave 4 P2-2 慢查询清单

> **执行日期**：2026-06-16
> **数据源**：生产库 39.99.34.194:5432

## 一、高 seq_scan 表（缺索引）

| schema | 表名 | seq_scan | idx_scan | seq_pct |
|--------|------|----------|----------|---------|
| <SCHEMA> | <TABLE> | <SEQ> | <IDX> | <PCT> |

## 二、未使用索引

| schema | 表名 | 索引名 | 大小 |
|--------|------|--------|------|
| <SCHEMA> | <TABLE> | <INDEX> | <SIZE> |

## 三、Top 20 慢 SQL（如启用 pg_stat_statements）

| calls | mean_exec_time | query |
|-------|----------------|-------|
| <CALLS> | <TIME> | <QUERY> |

## 四、修复建议

### 4.1 缺索引表（建议 Wave 4 修复）

（如有）

### 4.2 未使用索引（建议清理）

（如有）

### 4.3 慢 SQL（建议重写）

（如有）

## 五、附录

### 5.1 执行命令

```bash
PGPASSWORD=$DB_PASSWORD psql -h 39.99.34.194 -U bingxi -d bingxi_erp \
  -f backend/scripts/p2-2-slow-query.sql
```
```

### 任务 2.6：选择性优化建议

**任务**：根据基线报告，决定 PR-3+ 范围

**决策点**：
1. **可快速修复**（≤ 1 周工作量、低风险、有量化数据）→ 进入 PR-3+
2. **复杂或风险高**→ 推后到 Wave 5

**如果 0 个可快速修复**：直接结束 Wave 4 P2-2，PR-2 即最终交付

**如果 N 个可快速修复**：进入 PR-3+ 计划（每个优化项独立 PR）

### 任务 2.7：提交 PR-2

```bash
cd /workspace
git add docs/superpowers/plans/2026-06-16-p2-2-perf-baseline.md \
        docs/superpowers/plans/2026-06-16-p2-2-n+1-list.md \
        docs/superpowers/plans/2026-06-16-p2-2-slow-query-list.md

git -c user.name="bingxi-erp" -c user.email="noreply@bingxi-erp.local" \
  commit -m "docs(perf): P2-2 性能基线报告

- 前端基线：4 V2Table 页面数据源表行数
- N+1 清单：449 个 API 函数扫描
- 慢查询清单：pg_stat 扫描
- 选择性优化建议（决定 PR-3+ 范围）"

git push origin feature/p2-2-baseline-reports
```

**创建 PR + 轮询 CI + Merge**（同 PR-1 流程）

**清理本地**：

```bash
git checkout test
git pull origin test
git branch -D feature/p2-2-baseline-reports
```

---

## 三、PR-3+：选择性优化（按基线数据决定）

### 任务 3.0：执行条件

**前置条件**：PR-2 已完成，且基线报告明确指出可快速修复项

### 任务 3.1：候选优化项（可能执行）

| 优化项 | 文件 | 工作量 | 风险 | PR |
|--------|------|--------|------|-----|
| V2Table estimated-row-height 调优 | V2Table/index.vue | 1h | 低 | 3a |
| 列定义 width 固定（避免重排） | 4 页面 × ColumnDef | 2h | 低 | 3b |
| renderCell 缓存 | V2Table/index.vue | 2h | 低 | 3c |
| N+1 修复（preload） | 多个 service 文件 | 1 周 | 中 | 3d+ |
| 索引添加 | 多个 migration | 4h | 中 | 3e |
| 慢 SQL 重写 | 多个 handler | 4h | 中 | 3f |

### 任务 3.2：每个优化项独立 PR

**PR 模板**（每个优化项）：

```bash
# 1. 创建分支
git checkout -b feature/p2-2-opt-{name}

# 2. 实施修改
# （具体修改视优化项而定）

# 3. 提交
git add <修改文件>
git -c user.name="bingxi-erp" -c user.email="noreply@bingxi-erp.local" \
  commit -m "perf({domain}): P2-2 {优化项}（基线数据：{原数据} → 新数据）"

# 4. 推送 + 创建 PR + 轮询 CI + Merge + 清理
# （同 PR-1 流程）
```

### 任务 3.3：基线改善数据

每个 PR body 必须包含：
- 优化前数据（基线报告）
- 优化后数据（PR 测试结果）
- 改善百分比

### 任务 3.4：特殊场景

**若 N+1 修复涉及 DB 迁移**：
- 必须先创建 migration 文件
- 在 PR 中明确说明迁移步骤
- 由用户决定手动执行时机

**若索引添加涉及大数据表**：
- 使用 `CREATE INDEX CONCURRENTLY` 避免锁表
- 在 PR 中明确说明

**若慢 SQL 重写涉及业务逻辑**：
- 需保留原有行为（仅优化路径）
- 添加额外单测覆盖

---

## 四、验收清单

### 4.1 阶段一（必）

- [x] PR-1：3 个基线脚本（前端 perf-baseline.mjs + 后端 n1-scan.sh + 慢查询 SQL）
- [x] PR-2：3 个基线报告（前端 baseline + N+1 清单 + 慢查询清单）
- [x] 所有 PR 4 CI job 全绿
- [x] 报告数据来自生产库或明确说明降级
- [x] 选择性优化建议明确（0 或 N 个）

### 4.2 阶段二（如有 PR-3+）

- [x] 每个优化项独立 PR
- [x] PR body 包含基线数据 + 改善数据
- [x] 单测覆盖（如适用）
- [x] CI 4 job 全绿
- [x] squash merge

### 4.3 不可量化场景处理

- 生产库无访问权限 → 报告明确说明 + 提供本地 dry-run 脚本
- pg_stat_statements 未启用 → 报告明确说明 + 使用 pg_stat_user_tables 替代
- N+1 扫描 0 问题 → 报告明确说明 + grep 验证

---

## 五、风险与回退

### 5.1 风险

| 风险 | 缓解 |
|------|------|
| 生产库连接信息泄露 | DB 密码从环境变量，脚本不入版本库 |
| 扫描脚本误删数据 | 所有脚本仅 SELECT |
| 优化 PR 引入新 bug | 每 PR 独立可回退 + 单测 |
| pg_stat_statements 未启用 | 报告说明 + 替代数据源 |
| 索引添加锁表 | 使用 CREATE INDEX CONCURRENTLY |
| 慢 SQL 重写改变业务 | 单测覆盖 + 保留原有路径 |

### 5.2 回退预案

- **PR-1（脚本）**：删除 frontend/scripts + backend/scripts
- **PR-2（报告）**：删除 docs 3 个 md
- **PR-3+（优化）**：每 PR 独立 revert

### 5.3 CI 验证

- 脚本不依赖网络（PR-1 可本地 dry-run 验证语法）
- 报告 markdown 格式校验（lint）
- 所有 PR 遵循项目 CI 4 job：test / test-frontend / build-backend / build-frontend

---

## 六、Plan 自审

### 6.1 Spec 覆盖检查

- [x] 〇背景与目标 → 一阶段一（PR-1+2）+ 二阶段二（PR-3+）
- [x] 一范围 → 一PR-1 + 二PR-2 + 三PR-3+
- [x] 二架构与设计 → 一任务 1.2-1.4（代码骨架）
- [x] 三PR 计划 → 一/二/三PR 计划
- [x] 四验收标准 → 四验收清单
- [x] 五风险与回退 → 五风险与回退
- [x] 六参考资料 → 〇前置准备 + 0.5 文件结构
- [x] 七决策记录 → 〇前置 + 一PR-1 实施 + 二PR-2 实施

### 6.2 Placeholder 扫描

- 无 TBD / TODO / FIXME 残留
- 数字与时间具体化（"约 80 行"、"3 文件 160 行"等）
- 不确定项明确标注（"<从环境变量或用户提供>"）

### 6.3 文件路径准确性

- 4 V2Table 页面文件路径已与 Wave 4 P2-1 PR 实际文件交叉验证
- 脚本路径（frontend/scripts/, backend/scripts/）已与项目现有 scripts 目录结构一致
- 报告路径（docs/superpowers/plans/）已与现有 plans 目录结构一致

### 6.4 依赖关系

- PR-1 → PR-2 线性依赖
- PR-2 → PR-3+ 数据驱动依赖
- PR-3+ 之间可并行（不同文件 / 不同优化项）

### 6.5 安全检查

- [x] DB 密码从环境变量，不入版本库
- [x] 脚本仅 SELECT，不修改数据
- [x] 敏感信息保护规则一致

---

## 七、签字

- **作者**：AI 总代理
- **日期**：2026-06-16
- **基线版本**：origin/test @ 9b1bf80（PR #119 squash merge）
- **spec 来源**：[`docs/superpowers/specs/2026-06-16-wave4-p2-2-perf-design.md`](file:///workspace/docs/superpowers/specs/2026-06-16-wave4-p2-2-perf-design.md)
- **状态**：待用户审阅
