# 应付管理模块 - 技术方案

## 一、总体架构

### 1.1 技术栈
- **后端框架**: Axum 0.7 + Tokio
- **ORM 框架**: SeaORM 1.0
- **数据库**: PostgreSQL 18.0
- **通信协议**: RESTful API (/api/v1/erp/accounts-payable/)

### 1.2 模块划分
```
应付管理模块
├── 应付单管理 (ap_invoice)
├── 付款申请 (ap_payment_request)
├── 付款执行 (ap_payment)
├── 应付核销 (ap_verification)
├── 账龄分析 (ap_aging_analysis)
└── 供应商对账 (ap_reconciliation)
```

## 二、数据库设计

### 2.1 应付单表 (ap_invoice)

```sql
CREATE TABLE ap_invoice (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    invoice_no VARCHAR(50) NOT NULL UNIQUE,             -- 应付单号（AP20260315001）
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    invoice_type VARCHAR(20) NOT NULL,                  -- 应付类型：PURCHASE=采购应付，EXPENSE=费用应付，OTHER=其他应付
    source_type VARCHAR(20),                            -- 源单类型：PURCHASE_RECEIPT=采购入库，RETURN=退货，MANUAL=手工
    source_id INTEGER,                                  -- 源单 ID
    invoice_date DATE NOT NULL,                         -- 应付日期
    due_date DATE NOT NULL,                             -- 到期日期
    payment_terms INTEGER DEFAULT 30,                   -- 账期天数
    currency VARCHAR(10) DEFAULT 'CNY',                 -- 币种
    exchange_rate DECIMAL(18,6) DEFAULT 1.000000,       -- 汇率
    amount DECIMAL(18,2) NOT NULL,                      -- 应付金额
    tax_amount DECIMAL(18,2) DEFAULT 0.00,              -- 税额
    paid_amount DECIMAL(18,2) DEFAULT 0.00,             -- 已付金额
    unpaid_amount DECIMAL(18,2) DEFAULT 0.00,           -- 未付金额
    invoice_status VARCHAR(20) DEFAULT 'DRAFT',         -- 状态：DRAFT=草稿，AUDITED=已审核，PARTIAL_PAID=部分核销，PAID=已核销，CANCELLED=已取消
    department_id INTEGER,                              -- 部门 ID
    employee_id INTEGER,                                -- 业务员 ID
    notes TEXT,                                         -- 备注
    created_by INTEGER NOT NULL,                        -- 创建人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    audited_by INTEGER,                                 -- 审核人 ID
    audited_at TIMESTAMP,                               -- 审核时间
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (department_id) REFERENCES departments(id),
    FOREIGN KEY (employee_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (audited_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_ap_inv_no ON ap_invoice(invoice_no);
CREATE INDEX idx_ap_inv_supplier ON ap_invoice(supplier_id);
CREATE INDEX idx_ap_inv_date ON ap_invoice(invoice_date);
CREATE INDEX idx_ap_inv_due_date ON ap_invoice(due_date);
CREATE INDEX idx_ap_inv_status ON ap_invoice(invoice_status);
```

### 2.2 付款申请表 (ap_payment_request)

```sql
CREATE TABLE ap_payment_request (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    request_no VARCHAR(50) NOT NULL UNIQUE,             -- 申请单号（PR20260315001）
    request_date DATE NOT NULL,                         -- 申请日期
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    payment_type VARCHAR(20) NOT NULL,                  -- 付款类型：PREPAYMENT=预付款，PROGRESS=进度款，FINAL=尾款，WARRANTY=质保金
    payment_method VARCHAR(20) NOT NULL,                -- 付款方式：TT=电汇，LC=信用证，DP=付款交单，DA=承兑汇票，CHECK=支票，CASH=现金
    currency VARCHAR(10) DEFAULT 'CNY',                 -- 币种
    exchange_rate DECIMAL(18,6) DEFAULT 1.000000,       -- 汇率
    request_amount DECIMAL(18,2) NOT NULL,              -- 申请金额
    bank_account_name VARCHAR(200),                     -- 收款账户名称
    bank_account_no VARCHAR(50),                        -- 收款账号
    bank_name VARCHAR(200),                             -- 开户银行
    approval_status VARCHAR(20) DEFAULT 'DRAFT',        -- 审批状态：DRAFT=草稿，APPROVING=审批中，APPROVED=已批准，REJECTED=已拒绝
    applicant_id INTEGER NOT NULL,                      -- 申请人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    approved_by INTEGER,                                -- 批准人 ID
    approved_at TIMESTAMP,                              -- 批准时间
    rejected_reason TEXT,                               -- 拒绝原因
    notes TEXT,                                         -- 备注
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (applicant_id) REFERENCES users(id),
    FOREIGN KEY (approved_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_ap_req_no ON ap_payment_request(request_no);
CREATE INDEX idx_ap_req_supplier ON ap_payment_request(supplier_id);
CREATE INDEX idx_ap_req_date ON ap_payment_request(request_date);
CREATE INDEX idx_ap_req_status ON ap_payment_request(approval_status);

-- 付款申请明细表（关联应付单）
CREATE TABLE ap_payment_request_item (
    id SERIAL PRIMARY KEY,
    request_id INTEGER NOT NULL,
    invoice_id INTEGER NOT NULL,
    invoice_no VARCHAR(50) NOT NULL,
    invoice_amount DECIMAL(18,2) NOT NULL,
    unpaid_amount DECIMAL(18,2) NOT NULL,
    apply_amount DECIMAL(18,2) NOT NULL,
    FOREIGN KEY (request_id) REFERENCES ap_payment_request(id),
    FOREIGN KEY (invoice_id) REFERENCES ap_invoice(id)
);
```

### 2.3 付款单表 (ap_payment)

```sql
CREATE TABLE ap_payment (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    payment_no VARCHAR(50) NOT NULL UNIQUE,             -- 付款单号（PAY20260315001）
    payment_date DATE NOT NULL,                         -- 付款日期
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    request_id INTEGER,                                 -- 付款申请 ID
    payment_method VARCHAR(20) NOT NULL,                -- 付款方式
    currency VARCHAR(10) DEFAULT 'CNY',                 -- 币种
    exchange_rate DECIMAL(18,6) DEFAULT 1.000000,       -- 汇率
    payment_amount DECIMAL(18,2) NOT NULL,              -- 付款金额
    fee_amount DECIMAL(18,2) DEFAULT 0.00,              -- 手续费
    actual_amount DECIMAL(18,2) NOT NULL,               -- 实际付款金额
    bank_account_no VARCHAR(50),                        -- 付款账号
    ticket_no VARCHAR(50),                              -- 票据号
    payment_status VARCHAR(20) DEFAULT 'REGISTERED',    -- 状态：REGISTERED=已登记，CONFIRMED=已确认
    cashier_id INTEGER NOT NULL,                        -- 出纳 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    confirmed_by INTEGER,                               -- 确认人 ID
    confirmed_at TIMESTAMP,                             -- 确认时间
    notes TEXT,                                         -- 备注
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (request_id) REFERENCES ap_payment_request(id),
    FOREIGN KEY (cashier_id) REFERENCES users(id),
    FOREIGN KEY (confirmed_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_ap_pay_no ON ap_payment(payment_no);
CREATE INDEX idx_ap_pay_supplier ON ap_payment(supplier_id);
CREATE INDEX idx_ap_pay_date ON ap_payment(payment_date);
```

### 2.4 应付核销表 (ap_verification)

```sql
CREATE TABLE ap_verification (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    verification_no VARCHAR(50) NOT NULL UNIQUE,        -- 核销单号（VER20260315001）
    verification_date DATE NOT NULL,                    -- 核销日期
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    verification_type VARCHAR(20) NOT NULL,             -- 核销类型：AUTO=自动核销，MANUAL=手工核销
    total_amount DECIMAL(18,2) NOT NULL,                -- 核销总金额
    verification_status VARCHAR(20) DEFAULT 'COMPLETED',-- 状态：COMPLETED=已完成，CANCELLED=已取消
    created_by INTEGER NOT NULL,                        -- 创建人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    notes TEXT,                                         -- 备注
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

-- 核销明细表
CREATE TABLE ap_verification_item (
    id SERIAL PRIMARY KEY,
    verification_id INTEGER NOT NULL,
    invoice_id INTEGER NOT NULL,                        -- 应付单 ID
    payment_id INTEGER NOT NULL,                        -- 付款单 ID
    invoice_no VARCHAR(50) NOT NULL,
    payment_no VARCHAR(50) NOT NULL,
    verify_amount DECIMAL(18,2) NOT NULL,               -- 核销金额
    FOREIGN KEY (verification_id) REFERENCES ap_verification(id),
    FOREIGN KEY (invoice_id) REFERENCES ap_invoice(id),
    FOREIGN KEY (payment_id) REFERENCES ap_payment(id)
);

-- 索引
CREATE INDEX idx_ap_ver_no ON ap_verification(verification_no);
CREATE INDEX idx_ap_ver_supplier ON ap_verification(supplier_id);
CREATE INDEX idx_ap_ver_date ON ap_verification(verification_date);
```

### 2.5 供应商对账表 (ap_reconciliation)

```sql
CREATE TABLE ap_reconciliation (
    id SERIAL PRIMARY KEY,
    reconciliation_no VARCHAR(50) NOT NULL UNIQUE,      -- 对账单号（REC20260315001）
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    start_date DATE NOT NULL,                           -- 对账开始日期
    end_date DATE NOT NULL,                             -- 对账结束日期
    opening_balance DECIMAL(18,2) DEFAULT 0.00,         -- 期初余额
    total_invoice DECIMAL(18,2) DEFAULT 0.00,           -- 本期应付总额
    total_payment DECIMAL(18,2) DEFAULT 0.00,           -- 本期付款总额
    closing_balance DECIMAL(18,2) DEFAULT 0.00,         -- 期末余额
    reconciliation_status VARCHAR(20) DEFAULT 'PENDING',-- 状态：PENDING=待确认，CONFIRMED=已确认，DIFFERENT=有差异
    confirmed_by_supplier BOOLEAN DEFAULT FALSE,        -- 供应商是否确认
    confirmed_at TIMESTAMP,                             -- 确认时间
    created_by INTEGER NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    notes TEXT,
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (created_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_ap_rec_no ON ap_reconciliation(reconciliation_no);
CREATE INDEX idx_ap_rec_supplier ON ap_reconciliation(supplier_id);
CREATE INDEX idx_ap_rec_date ON ap_reconciliation(end_date);
```

## 三、API 接口设计

### 3.1 应付单管理（10 个接口）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/erp/accounts-payable/invoices | 获取应付单列表 |
| POST | /api/v1/erp/accounts-payable/invoices | 创建应付单 |
| GET | /api/v1/erp/accounts-payable/invoices/:id | 获取应付单详情 |
| PUT | /api/v1/erp/accounts-payable/invoices/:id | 更新应付单 |
| POST | /api/v1/erp/accounts-payable/invoices/:id/audit | 审核应付单 |
| POST | /api/v1/erp/accounts-payable/invoices/auto-generate | 自动生成应付单 |
| GET | /api/v1/erp/accounts-payable/invoices/aging | 应付账龄分析 |
| GET | /api/v1/erp/accounts-payable/invoices/balance | 应付余额表 |
| GET | /api/v1/erp/accounts-payable/invoices/statistics | 应付统计报表 |
| POST | /api/v1/erp/accounts-payable/invoices/:id/cancel | 取消应付单 |

### 3.2 付款申请管理（8 个接口）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/erp/accounts-payable/payment-requests | 获取付款申请列表 |
| POST | /api/v1/erp/accounts-payable/payment-requests | 创建付款申请 |
| GET | /api/v1/erp/accounts-payable/payment-requests/:id | 获取付款申请详情 |
| PUT | /api/v1/erp/accounts-payable/payment-requests/:id | 更新付款申请 |
| POST | /api/v1/erp/accounts-payable/payment-requests/:id/submit | 提交付款申请 |
| POST | /api/v1/erp/accounts-payable/payment-requests/:id/approve | 审批付款申请 |
| POST | /api/v1/erp/accounts-payable/payment-requests/:id/reject | 拒绝付款申请 |
| DELETE | /api/v1/erp/accounts-payable/payment-requests/:id | 删除付款申请 |

### 3.3 付款执行管理（6 个接口）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/erp/accounts-payable/payments | 获取付款单列表 |
| POST | /api/v1/erp/accounts-payable/payments | 创建付款单 |
| GET | /api/v1/erp/accounts-payable/payments/:id | 获取付款单详情 |
| PUT | /api/v1/erp/accounts-payable/payments/:id | 更新付款单 |
| POST | /api/v1/erp/accounts-payable/payments/:id/confirm | 确认付款 |
| GET | /api/v1/erp/accounts-payable/payments/plan | 付款计划表 |

### 3.4 应付核销管理（5 个接口）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/erp/accounts-payable/verifications | 获取核销记录列表 |
| POST | /api/v1/erp/accounts-payable/verifications/auto | 自动核销 |
| POST | /api/v1/erp/accounts-payable/verifications/manual | 手工核销 |
| GET | /api/v1/erp/accounts-payable/verifications/:id | 获取核销详情 |
| POST | /api/v1/erp/accounts-payable/verifications/:id/cancel | 取消核销 |

### 3.5 供应商对账（4 个接口）

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/v1/erp/accounts-payable/reconciliations | 获取对账单列表 |
| POST | /api/v1/erp/accounts-payable/reconciliations/generate | 生成对账单 |
| GET | /api/v1/erp/accounts-payable/reconciliations/:id | 获取对账单详情 |
| POST | /api/v1/erp/accounts-payable/reconciliations/:id/confirm | 确认对账单 |

**总计：33 个 API 接口**

## 四、Service 层设计

### 4.1 应付单服务 (ApInvoiceService)

```rust
pub struct ApInvoiceService {
    db: Arc<DatabaseConnection>,
}

impl ApInvoiceService {
    /// 自动生成应付单（从采购入库单）
    pub async fn auto_generate_from_receipt(
        &self,
        receipt_id: i32,
        user_id: i32,
    ) -> Result<ap_invoice::Model, AppError>;
    
    /// 创建应付单（手工）
    pub async fn create_invoice(
        &self,
        req: CreateApInvoiceRequest,
        user_id: i32,
    ) -> Result<ap_invoice::Model, AppError>;
    
    /// 审核应付单
    pub async fn audit_invoice(
        &self,
        invoice_id: i32,
        user_id: i32,
    ) -> Result<ap_invoice::Model, AppError>;
    
    /// 更新已付金额
    pub async fn update_paid_amount(
        &self,
        invoice_id: i32,
        paid_amount: Decimal,
    ) -> Result<(), AppError>;
    
    /// 获取应付单列表
    pub async fn list_invoices(...) -> Result<(Vec<ap_invoice::Model>, u64), AppError>;
    
    /// 应付账龄分析
    pub async fn aging_analysis(
        &self,
        supplier_id: Option<i32>,
        as_of_date: NaiveDate,
    ) -> Result<Vec<AgingAnalysisVO>, AppError>;
}
```

### 4.2 付款申请服务 (ApPaymentRequestService)

```rust
pub struct ApPaymentRequestService {
    db: Arc<DatabaseConnection>,
}

impl ApPaymentRequestService {
    /// 创建付款申请
    pub async fn create_request(...) -> Result<ap_payment_request::Model, AppError>;
    
    /// 提交付款申请
    pub async fn submit_request(...) -> Result<ap_payment_request::Model, AppError>;
    
    /// 审批付款申请
    pub async fn approve_request(...) -> Result<ap_payment_request::Model, AppError>;
    
    /// 拒绝付款申请
    pub async fn reject_request(...) -> Result<ap_payment_request::Model, AppError>;
}
```

### 4.3 付款服务 (ApPaymentService)

```rust
pub struct ApPaymentService {
    db: Arc<DatabaseConnection>,
}

impl ApPaymentService {
    /// 创建付款单
    pub async fn create_payment(...) -> Result<ap_payment::Model, AppError>;
    
    /// 确认付款
    pub async fn confirm_payment(...) -> Result<ap_payment::Model, AppError>;
    
    /// 获取付款计划
    pub async fn get_payment_plan(...) -> Result<Vec<PaymentPlanVO>, AppError>;
}
```

### 4.4 核销服务 (ApVerificationService)

```rust
pub struct ApVerificationService {
    db: Arc<DatabaseConnection>,
}

impl ApVerificationService {
    /// 自动核销
    pub async fn auto_verify(...) -> Result<ap_verification::Model, AppError>;
    
    /// 手工核销
    pub async fn manual_verify(...) -> Result<ap_verification::Model, AppError>;
    
    /// 取消核销
    pub async fn cancel_verification(...) -> Result<(), AppError>;
}
```

## 五、核心业务逻辑

### 5.1 应付单自动生成逻辑

```rust
pub async fn auto_generate_from_receipt(
    &self,
    receipt_id: i32,
    user_id: i32,
) -> Result<ap_invoice::Model, AppError> {
    let txn = self.db.begin().await?;
    
    // 1. 查询采购入库单
    let receipt = purchase_receipt::Entity::find_by_id(receipt_id)
        .one(&txn).await?
        .ok_or(AppError::ResourceNotFound(...))?;
    
    // 2. 检查是否已生成应付
    let exists = ap_invoice::Entity::find()
        .filter(ap_invoice::Column::SourceType.eq("PURCHASE_RECEIPT"))
        .filter(ap_invoice::Column::SourceId.eq(receipt_id))
        .one(&txn).await?;
    
    if exists.is_some() {
        return Err(AppError::BusinessError("该入库单已生成应付单".to_string()));
    }
    
    // 3. 获取供应商账期
    let supplier = supplier::Entity::find_by_id(receipt.supplier_id)
        .one(&txn).await?
        .ok_or(AppError::ResourceNotFound(...))?;
    
    let payment_terms = supplier.payment_terms.unwrap_or(30);
    
    // 4. 生成应付单
    let invoice_no = self.generate_invoice_no().await?;
    let invoice_date = receipt.receipt_date;
    let due_date = invoice_date + Duration::days(payment_terms as i64);
    
    let invoice = ap_invoice::ActiveModel {
        invoice_no: Set(invoice_no),
        supplier_id: Set(receipt.supplier_id),
        invoice_type: Set("PURCHASE".to_string()),
        source_type: Set("PURCHASE_RECEIPT".to_string()),
        source_id: Set(Some(receipt_id)),
        invoice_date: Set(invoice_date),
        due_date: Set(due_date),
        payment_terms: Set(payment_terms),
        amount: Set(receipt.total_amount),
        invoice_status: Set("AUDITED".to_string()), // 自动生成直接审核
        created_by: Set(user_id),
        ..Default::default()
    }.insert(&txn).await?;
    
    txn.commit().await?;
    
    Ok(invoice)
}
```

### 5.2 自动核销逻辑

```rust
pub async fn auto_verify(
    &self,
    supplier_id: i32,
    user_id: i32,
) -> Result<ap_verification::Model, AppError> {
    let txn = self.db.begin().await?;
    
    // 1. 查询该供应商未核销的应付单（按到期日排序）
    let invoices = ap_invoice::Entity::find()
        .filter(ap_invoice::Column::SupplierId.eq(supplier_id))
        .filter(ap_invoice::Column::InvoiceStatus.eq("AUDITED"))
        .filter(ap_invoice::Column::UnpaidAmount.gt(Decimal::new(0, 2)))
        .order_by_asc(ap_invoice::Column::DueDate)
        .all(&txn).await?;
    
    // 2. 查询该供应商已确认未核销的付款单
    let payments = ap_payment::Entity::find()
        .filter(ap_payment::Column::SupplierId.eq(supplier_id))
        .filter(ap_payment::Column::PaymentStatus.eq("CONFIRMED"))
        .all(&txn).await?;
    
    // 3. 逐个匹配核销
    let mut verification_items = Vec::new();
    let mut total_amount = Decimal::new(0, 2);
    
    for mut payment in payments {
        let mut remaining = payment.payment_amount;
        
        for mut invoice in &invoices {
            if remaining <= Decimal::new(0, 2) {
                break;
            }
            
            if invoice.unpaid_amount > Decimal::new(0, 2) {
                let verify_amount = remaining.min(invoice.unpaid_amount);
                
                // 创建核销明细
                verification_items.push(ApVerificationItem {
                    invoice_id: invoice.id,
                    payment_id: payment.id,
                    verify_amount,
                });
                
                // 更新应付单已付金额
                invoice.paid_amount += verify_amount;
                invoice.unpaid_amount -= verify_amount;
                if invoice.unpaid_amount == Decimal::new(0, 2) {
                    invoice.invoice_status = "PAID".to_string();
                } else {
                    invoice.invoice_status = "PARTIAL_PAID".to_string();
                }
                
                remaining -= verify_amount;
                total_amount += verify_amount;
            }
        }
    }
    
    // 4. 创建核销单
    let verification_no = self.generate_verification_no().await?;
    let verification = ap_verification::ActiveModel {
        verification_no: Set(verification_no),
        supplier_id: Set(supplier_id),
        verification_type: Set("AUTO".to_string()),
        total_amount: Set(total_amount),
        created_by: Set(user_id),
        ..Default::default()
    }.insert(&txn).await?;
    
    // 5. 保存核销明细
    for item in verification_items {
        ap_verification_item::ActiveModel {
            verification_id: Set(verification.id),
            invoice_id: Set(item.invoice_id),
            payment_id: Set(item.payment_id),
            verify_amount: Set(item.verify_amount),
        }.insert(&txn).await?;
    }
    
    txn.commit().await?;
    
    Ok(verification)
}
```

## 六、异常处理

### 6.1 业务异常

```rust
pub enum ApError {
    /// 应付单已存在
    #[error("应付单已存在：{0}")]
    InvoiceAlreadyExists(i32),
    
    /// 付款金额超过未付金额
    #[error("付款金额 ({0}) 超过未付金额 ({1})")]
    ExceedUnpaidAmount(Decimal, Decimal),
    
    /// 应付单已完全核销
    #[error("应付单已完全核销")]
    InvoiceFullyPaid,
    
    /// 付款申请金额超过限额
    #[error("付款申请金额超过审批限额")]
    ExceedApprovalLimit,
    
    /// 核销金额不匹配
    #[error("核销金额不匹配")]
    VerificationAmountMismatch,
}
```

## 七、性能优化

### 7.1 数据库优化

```sql
-- 1. 物化视图 - 供应商应付汇总
CREATE MATERIALIZED VIEW mv_supplier_ap_summary AS
SELECT 
    supplier_id,
    COUNT(*) as total_invoices,
    SUM(amount) as total_amount,
    SUM(paid_amount) as paid_amount,
    SUM(unpaid_amount) as unpaid_amount,
    MAX(due_date) as latest_due_date
FROM ap_invoice
WHERE invoice_status IN ('AUDITED', 'PARTIAL_PAID')
GROUP BY supplier_id;

-- 2. 定期清理已核销数据
CREATE OR REPLACE FUNCTION archive_old_ap_data()
RETURNS void AS $$
BEGIN
    -- 归档 3 年前的已核销数据
    INSERT INTO ap_invoice_archive
    SELECT * FROM ap_invoice
    WHERE invoice_status = 'PAID' 
    AND audited_at < CURRENT_DATE - INTERVAL '3 years';
    
    DELETE FROM ap_invoice
    WHERE invoice_status = 'PAID' 
    AND audited_at < CURRENT_DATE - INTERVAL '3 years';
END;
$$ LANGUAGE plpgsql;
```

## 八、安全控制

### 8.1 权限控制

```rust
/// 付款申请审批权限检查
pub async fn check_approval_permission(
    amount: Decimal,
    user_id: i32,
) -> Result<(), AppError> {
    let user = user::Entity::find_by_id(user_id)
        .one(&*db).await?
        .ok_or(AppError::UserNotFound(user_id))?;
    
    if amount <= dec!(100000) {
        // 10 万以下：财务经理审批
        if !user.has_role(&["FINANCE_MANAGER"]) {
            return Err(AppError::PermissionDenied(
                "财务经理才能审批 10 万元以下的付款".to_string()
            ));
        }
    } else if amount <= dec!(500000) {
        // 10-50 万：总经理审批
        if !user.has_role(&["GENERAL_MANAGER"]) {
            return Err(AppError::PermissionDenied(
                "总经理才能审批 50 万元以下的付款".to_string()
            ));
        }
    } else {
        // 50 万以上：董事长审批
        if !user.has_role(&["CHAIRMAN"]) {
            return Err(AppError::PermissionDenied(
                "董事长才能审批 50 万元以上的付款".to_string()
            ));
        }
    }
    
    Ok(())
}
```

## 九、总结

本技术方案完整覆盖了应付管理模块的 5 大核心功能：
1. ✅ 应付单管理（10 个 API）
2. ✅ 付款申请管理（8 个 API）
3. ✅ 付款执行管理（6 个 API）
4. ✅ 应付核销管理（5 个 API）
5. ✅ 供应商对账（4 个接口）

**关键特性**:
- 应付单自动生成（从采购入库）
- 多级审批流程（按金额分级）
- 自动核销与手工核销
- 账龄分析报表
- 供应商对账管理
- 完整的权限控制

下一步将根据此技术方案进行任务规划和编码实现。
