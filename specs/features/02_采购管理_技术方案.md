# 采购管理模块 - 技术方案

## 一、总体架构

### 1.1 技术栈
- **后端框架**: Axum 0.7 + Tokio
- **ORM 框架**: SeaORM 1.0
- **数据库**: PostgreSQL 18.0
- **通信协议**: gRPC (Tonic) - 模块间通信
- **HTTP API**: RESTful (/api/v1/erp/purchases/)

### 1.2 模块划分
```
采购管理模块
├── 采购申请管理 (purchase_requisition)
├── 采购订单管理 (purchase_order)
├── 采购价格管理 (purchase_price)
├── 采购入库管理 (purchase_receipt)
├── 采购退货管理 (purchase_return)
└── 采购质检管理 (purchase_inspection)
```

## 二、数据库设计

### 2.1 采购订单表 (purchase_order)

```sql
CREATE TABLE purchase_order (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    order_no VARCHAR(50) NOT NULL UNIQUE,               -- 订单编号（PO20250115001）
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID（外键）
    order_date DATE NOT NULL,                           -- 订单日期
    expected_delivery_date DATE,                        -- 预计交货日期
    actual_delivery_date DATE,                          -- 实际交货日期
    warehouse_id INTEGER NOT NULL,                      -- 入库仓库 ID
    department_id INTEGER NOT NULL,                     -- 采购部门 ID
    purchaser_id INTEGER NOT NULL,                      -- 采购员 ID
    currency VARCHAR(10) DEFAULT 'CNY',                 -- 币种
    exchange_rate DECIMAL(18,6) DEFAULT 1.000000,       -- 汇率
    total_amount DECIMAL(18,2) DEFAULT 0.00,            -- 订单总金额（本位币）
    total_amount_foreign DECIMAL(18,2) DEFAULT 0.00,    -- 订单总金额（外币）
    total_quantity DECIMAL(18,4) DEFAULT 0.0000,        -- 总数量（主单位）
    total_quantity_alt DECIMAL(18,4) DEFAULT 0.0000,    -- 总数量（辅助单位）
    order_status VARCHAR(20) DEFAULT 'DRAFT',           -- 订单状态
    payment_terms TEXT,                                 -- 付款条件
    shipping_terms TEXT,                                -- 运输条款
    notes TEXT,                                         -- 备注
    attachment_urls TEXT[],                             -- 附件 URL 列表
    created_by INTEGER NOT NULL,                        -- 创建人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    updated_by INTEGER,                                 -- 更新人 ID
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 更新时间
    approved_by INTEGER,                                -- 审批人 ID
    approved_at TIMESTAMP,                              -- 审批时间
    rejected_reason TEXT,                               -- 拒绝原因
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouses(id),
    FOREIGN KEY (department_id) REFERENCES departments(id),
    FOREIGN KEY (purchaser_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (updated_by) REFERENCES users(id),
    FOREIGN KEY (approved_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_po_order_no ON purchase_order(order_no);
CREATE INDEX idx_po_supplier_id ON purchase_order(supplier_id);
CREATE INDEX idx_po_order_date ON purchase_order(order_date);
CREATE INDEX idx_po_order_status ON purchase_order(order_status);
CREATE INDEX idx_po_expected_delivery ON purchase_order(expected_delivery_date);
CREATE INDEX idx_po_created_by ON purchase_order(created_by);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_order_updated_at
BEFORE UPDATE ON purchase_order
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

### 2.2 采购订单明细表 (purchase_order_item)

```sql
CREATE TABLE purchase_order_item (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    order_id INTEGER NOT NULL,                          -- 订单 ID（外键）
    line_no INTEGER NOT NULL,                           -- 行号（10, 20, 30...）
    material_id INTEGER NOT NULL,                       -- 物料 ID
    material_code VARCHAR(50) NOT NULL,                 -- 物料编码
    material_name VARCHAR(200) NOT NULL,                -- 物料名称
    specification VARCHAR(200),                         -- 规格型号
    batch_no VARCHAR(50),                               -- 批次号（面料行业）
    color_code VARCHAR(50),                             -- 色号（面料行业）
    lot_no VARCHAR(50),                                 -- 缸号（面料行业）
    grade VARCHAR(10),                                  -- 等级（面料行业）
    gram_weight DECIMAL(8,2),                           -- 克重（g/m²）
    width DECIMAL(8,2),                                 -- 幅宽（cm）
    unit_price DECIMAL(18,6) NOT NULL,                  -- 单价
    currency VARCHAR(10) DEFAULT 'CNY',                 -- 币种
    quantity_ordered DECIMAL(18,4) NOT NULL,            -- 订购数量（主单位）
    quantity_received DECIMAL(18,4) DEFAULT 0.0000,     -- 已入库数量（主单位）
    quantity_returned DECIMAL(18,4) DEFAULT 0.0000,     -- 已退货数量（主单位）
    unit_master VARCHAR(20) NOT NULL,                   -- 主单位（米）
    unit_alt VARCHAR(20),                               -- 辅助单位（公斤）
    conversion_factor DECIMAL(18,6),                    -- 换算系数
    quantity_alt_ordered DECIMAL(18,4),                 -- 订购数量（辅助单位）
    quantity_alt_received DECIMAL(18,4),                -- 已入库数量（辅助单位）
    amount DECIMAL(18,2) NOT NULL,                      -- 金额
    tax_rate DECIMAL(5,2) DEFAULT 13.00,                -- 税率（%）
    tax_amount DECIMAL(18,2) DEFAULT 0.00,              -- 税额
    delivery_date DATE,                                 -- 交货日期
    warehouse_id INTEGER,                               -- 仓库 ID
    notes TEXT,                                         -- 备注
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 更新时间
    FOREIGN KEY (order_id) REFERENCES purchase_order(id) ON DELETE CASCADE,
    FOREIGN KEY (material_id) REFERENCES materials(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouses(id),
    UNIQUE (order_id, line_no)
);

-- 索引
CREATE INDEX idx_poi_order_id ON purchase_order_item(order_id);
CREATE INDEX idx_poi_material_id ON purchase_order_item(material_id);
CREATE INDEX idx_poi_batch_no ON purchase_order_item(batch_no);
CREATE INDEX idx_poi_color_code ON purchase_order_item(color_code);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_order_item_updated_at
BEFORE UPDATE ON purchase_order_item
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

-- 触发器：计算金额和汇总
CREATE TRIGGER calc_purchase_order_item_amount
BEFORE INSERT OR UPDATE ON purchase_order_item
FOR EACH ROW EXECUTE FUNCTION calc_purchase_order_item_amount();
```

### 2.3 采购入库表 (purchase_receipt)

```sql
CREATE TABLE purchase_receipt (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    receipt_no VARCHAR(50) NOT NULL UNIQUE,             -- 入库单号（GR20250115001）
    order_id INTEGER,                                   -- 采购订单 ID（外键）
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    receipt_date DATE NOT NULL,                         -- 入库日期
    warehouse_id INTEGER NOT NULL,                      -- 仓库 ID
    department_id INTEGER,                              -- 收货部门 ID
    receiver_id INTEGER,                                -- 收货人 ID
    inspector_id INTEGER,                               -- 质检员 ID
    inspection_status VARCHAR(20) DEFAULT 'PENDING',    -- 质检状态
    receipt_status VARCHAR(20) DEFAULT 'DRAFT',         -- 入库状态
    total_quantity DECIMAL(18,4) DEFAULT 0.0000,        -- 总入库数量（主单位）
    total_quantity_alt DECIMAL(18,4) DEFAULT 0.0000,    -- 总入库数量（辅助单位）
    total_amount DECIMAL(18,2) DEFAULT 0.00,            -- 总金额
    notes TEXT,                                         -- 备注
    attachment_urls TEXT[],                             -- 附件 URL 列表
    created_by INTEGER NOT NULL,                        -- 创建人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    updated_by INTEGER,                                 -- 更新人 ID
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 更新时间
    FOREIGN KEY (order_id) REFERENCES purchase_order(id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouses(id),
    FOREIGN KEY (department_id) REFERENCES departments(id),
    FOREIGN KEY (receiver_id) REFERENCES users(id),
    FOREIGN KEY (inspector_id) REFERENCES users(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (updated_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_pr_receipt_no ON purchase_receipt(receipt_no);
CREATE INDEX idx_pr_order_id ON purchase_receipt(order_id);
CREATE INDEX idx_pr_supplier_id ON purchase_receipt(supplier_id);
CREATE INDEX idx_pr_receipt_date ON purchase_receipt(receipt_date);
CREATE INDEX idx_pr_warehouse_id ON purchase_receipt(warehouse_id);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_receipt_updated_at
BEFORE UPDATE ON purchase_receipt
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

### 2.4 采购入库明细表 (purchase_receipt_item)

```sql
CREATE TABLE purchase_receipt_item (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    receipt_id INTEGER NOT NULL,                        -- 入库单 ID（外键）
    order_item_id INTEGER,                              -- 订单明细 ID（外键）
    line_no INTEGER NOT NULL,                           -- 行号
    material_id INTEGER NOT NULL,                       -- 物料 ID
    material_code VARCHAR(50) NOT NULL,                 -- 物料编码
    material_name VARCHAR(200) NOT NULL,                -- 物料名称
    batch_no VARCHAR(50),                               -- 批次号
    color_code VARCHAR(50),                             -- 色号
    lot_no VARCHAR(50),                                 -- 缸号
    grade VARCHAR(10),                                  -- 等级
    gram_weight DECIMAL(8,2),                           -- 克重
    width DECIMAL(8,2),                                 -- 幅宽
    quantity DECIMAL(18,4) NOT NULL,                    -- 入库数量（主单位）
    quantity_alt DECIMAL(18,4),                         -- 入库数量（辅助单位）
    unit_master VARCHAR(20) NOT NULL,                   -- 主单位
    unit_alt VARCHAR(20),                               -- 辅助单位
    unit_price DECIMAL(18,6),                           -- 单价
    amount DECIMAL(18,2),                               -- 金额
    location_code VARCHAR(50),                          -- 库位编码
    package_no VARCHAR(50),                             -- 包号
    production_date DATE,                               -- 生产日期
    shelf_life INTEGER,                                 -- 保质期（天）
    notes TEXT,                                         -- 备注
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    FOREIGN KEY (receipt_id) REFERENCES purchase_receipt(id) ON DELETE CASCADE,
    FOREIGN KEY (order_item_id) REFERENCES purchase_order_item(id),
    FOREIGN KEY (material_id) REFERENCES materials(id),
    UNIQUE (receipt_id, line_no)
);

-- 索引
CREATE INDEX idx_pri_receipt_id ON purchase_receipt_item(receipt_id);
CREATE INDEX idx_pri_order_item_id ON purchase_receipt_item(order_item_id);
CREATE INDEX idx_pri_material_id ON purchase_receipt_item(material_id);
CREATE INDEX idx_pri_batch_no ON purchase_receipt_item(batch_no);
CREATE INDEX idx_pri_color_code ON purchase_receipt_item(color_code);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_receipt_item_updated_at
BEFORE UPDATE ON purchase_receipt_item
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

### 2.5 采购退货表 (purchase_return)

```sql
CREATE TABLE purchase_return (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    return_no VARCHAR(50) NOT NULL UNIQUE,              -- 退货单号（RT20250115001）
    receipt_id INTEGER NOT NULL,                        -- 入库单 ID（外键）
    order_id INTEGER,                                   -- 采购订单 ID
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    return_date DATE NOT NULL,                          -- 退货日期
    warehouse_id INTEGER NOT NULL,                      -- 仓库 ID
    department_id INTEGER,                              -- 退货部门 ID
    reason_type VARCHAR(20) NOT NULL,                   -- 退货原因类型
    reason_detail TEXT,                                 -- 退货原因详情
    return_status VARCHAR(20) DEFAULT 'DRAFT',          -- 退货状态
    total_quantity DECIMAL(18,4) DEFAULT 0.0000,        -- 总退货数量（主单位）
    total_quantity_alt DECIMAL(18,4) DEFAULT 0.0000,    -- 总退货数量（辅助单位）
    total_amount DECIMAL(18,2) DEFAULT 0.00,            -- 总退货金额
    notes TEXT,                                         -- 备注
    created_by INTEGER NOT NULL,                        -- 创建人 ID
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    updated_by INTEGER,                                 -- 更新人 ID
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 更新时间
    approved_by INTEGER,                                -- 审批人 ID
    approved_at TIMESTAMP,                              -- 审批时间
    FOREIGN KEY (receipt_id) REFERENCES purchase_receipt(id),
    FOREIGN KEY (order_id) REFERENCES purchase_order(id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (warehouse_id) REFERENCES warehouses(id),
    FOREIGN KEY (department_id) REFERENCES departments(id),
    FOREIGN KEY (created_by) REFERENCES users(id),
    FOREIGN KEY (updated_by) REFERENCES users(id),
    FOREIGN KEY (approved_by) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_pret_return_no ON purchase_return(return_no);
CREATE INDEX idx_pret_receipt_id ON purchase_return(receipt_id);
CREATE INDEX idx_pret_order_id ON purchase_return(order_id);
CREATE INDEX idx_pret_supplier_id ON purchase_return(supplier_id);
CREATE INDEX idx_pret_return_date ON purchase_return(return_date);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_return_updated_at
BEFORE UPDATE ON purchase_return
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

### 2.6 采购质检表 (purchase_inspection)

```sql
CREATE TABLE purchase_inspection (
    id SERIAL PRIMARY KEY,                              -- 主键 ID
    inspection_no VARCHAR(50) NOT NULL UNIQUE,          -- 质检单号（IQ20250115001）
    receipt_id INTEGER NOT NULL,                        -- 入库单 ID（外键）
    order_id INTEGER,                                   -- 采购订单 ID
    supplier_id INTEGER NOT NULL,                       -- 供应商 ID
    inspection_date DATE NOT NULL,                      -- 质检日期
    inspector_id INTEGER NOT NULL,                      -- 质检员 ID
    inspection_type VARCHAR(20) DEFAULT 'NORMAL',       -- 质检类型
    sample_size INTEGER,                                -- 抽样数量
    defect_count INTEGER DEFAULT 0,                     -- 不合格数量
    pass_quantity DECIMAL(18,4),                        -- 合格数量（主单位）
    reject_quantity DECIMAL(18,4),                      -- 不合格数量（主单位）
    inspection_status VARCHAR(20) DEFAULT 'PENDING',    -- 质检状态
    inspection_result VARCHAR(20),                      -- 质检结果
    quality_score DECIMAL(5,2),                         -- 质量得分
    defect_description TEXT,                            -- 缺陷描述
    attachment_urls TEXT[],                             -- 附件 URL 列表
    notes TEXT,                                         -- 备注
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 创建时间
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,     -- 更新时间
    FOREIGN KEY (receipt_id) REFERENCES purchase_receipt(id),
    FOREIGN KEY (order_id) REFERENCES purchase_order(id),
    FOREIGN KEY (supplier_id) REFERENCES suppliers(id),
    FOREIGN KEY (inspector_id) REFERENCES users(id)
);

-- 索引
CREATE INDEX idx_pi_inspection_no ON purchase_inspection(inspection_no);
CREATE INDEX idx_pi_receipt_id ON purchase_inspection(receipt_id);
CREATE INDEX idx_pi_order_id ON purchase_inspection(order_id);
CREATE INDEX idx_pi_inspection_date ON purchase_inspection(inspection_date);
CREATE INDEX idx_pi_inspector_id ON purchase_inspection(inspector_id);

-- 触发器：更新时间
CREATE TRIGGER update_purchase_inspection_updated_at
BEFORE UPDATE ON purchase_inspection
FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
```

## 三、API 接口设计

### 3.1 采购订单管理

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | /api/v1/erp/purchases/orders | 获取采购订单列表 | 采购专员、采购经理 |
| POST | /api/v1/erp/purchases/orders | 创建采购订单 | 采购专员 |
| GET | /api/v1/erp/purchases/orders/:id | 获取采购订单详情 | 采购专员、采购经理 |
| PUT | /api/v1/erp/purchases/orders/:id | 更新采购订单 | 采购专员（草稿状态） |
| DELETE | /api/v1/erp/purchases/orders/:id | 删除采购订单 | 采购专员（草稿状态） |
| POST | /api/v1/erp/purchases/orders/:id/submit | 提交采购订单 | 采购专员 |
| POST | /api/v1/erp/purchases/orders/:id/approve | 审批采购订单 | 采购经理 |
| POST | /api/v1/erp/purchases/orders/:id/reject | 拒绝采购订单 | 采购经理 |
| POST | /api/v1/erp/purchases/orders/:id/close | 关闭采购订单 | 采购经理 |
| GET | /api/v1/erp/purchases/orders/:id/items | 获取订单明细列表 | 采购专员 |
| POST | /api/v1/erp/purchases/orders/:id/items | 添加订单明细 | 采购专员 |
| PUT | /api/v1/erp/purchases/orders/:id/items/:item_id | 更新订单明细 | 采购专员 |
| DELETE | /api/v1/erp/purchases/orders/:id/items/:item_id | 删除订单明细 | 采购专员 |

### 3.2 采购入库管理

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | /api/v1/erp/purchases/receipts | 获取入库单列表 | 仓库管理员 |
| POST | /api/v1/erp/purchases/receipts | 创建入库单 | 仓库管理员 |
| GET | /api/v1/erp/purchases/receipts/:id | 获取入库单详情 | 仓库管理员 |
| PUT | /api/v1/erp/purchases/receipts/:id | 更新入库单 | 仓库管理员 |
| POST | /api/v1/erp/purchases/receipts/:id/confirm | 确认入库单 | 仓库管理员 |
| GET | /api/v1/erp/purchases/receipts/:id/items | 获取入库明细列表 | 仓库管理员 |
| POST | /api/v1/erp/purchases/receipts/:id/items | 添加入库明细 | 仓库管理员 |

### 3.3 采购退货管理

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | /api/v1/erp/purchases/returns | 获取退货单列表 | 仓库管理员、采购专员 |
| POST | /api/v1/erp/purchases/returns | 创建退货单 | 仓库管理员 |
| GET | /api/v1/erp/purchases/returns/:id | 获取退货单详情 | 仓库管理员、采购专员 |
| PUT | /api/v1/erp/purchases/returns/:id | 更新退货单 | 仓库管理员 |
| POST | /api/v1/erp/purchases/returns/:id/submit | 提交退货单 | 仓库管理员 |
| POST | /api/v1/erp/purchases/returns/:id/approve | 审批退货单 | 采购经理 |

### 3.4 采购质检管理

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | /api/v1/erp/purchases/inspections | 获取质检单列表 | 质量管理员 |
| POST | /api/v1/erp/purchases/inspections | 创建质检单 | 质量管理员 |
| GET | /api/v1/erp/purchases/inspections/:id | 获取质检单详情 | 质量管理员 |
| PUT | /api/v1/erp/purchases/inspections/:id | 更新质检单 | 质量管理员 |
| POST | /api/v1/erp/purchases/inspections/:id/complete | 完成质检单 | 质量管理员 |

### 3.5 采购价格管理

| 方法 | 路径 | 描述 | 权限 |
|------|------|------|------|
| GET | /api/v1/erp/purchases/prices | 获取采购价格列表 | 采购专员、采购经理 |
| POST | /api/v1/erp/purchases/prices | 创建采购价格 | 采购经理 |
| PUT | /api/v1/erp/purchases/prices/:id | 更新采购价格 | 采购经理 |
| DELETE | /api/v1/erp/purchases/prices/:id | 删除采购价格 | 采购经理 |
| POST | /api/v1/erp/purchases/prices/import | 导入采购价格 | 采购经理 |
| POST | /api/v1/erp/purchases/prices/export | 导出采购价格 | 采购经理 |

## 四、Service 层设计

### 4.1 采购订单服务 (PurchaseOrderService)

```rust
pub struct PurchaseOrderService {
    db: Arc<DbConnection>,
}

impl PurchaseOrderService {
    /// 创建采购订单
    pub async fn create_order(
        &self,
        req: CreatePurchaseOrderRequest,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 更新采购订单
    pub async fn update_order(
        &self,
        order_id: i32,
        req: UpdatePurchaseOrderRequest,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 删除采购订单
    pub async fn delete_order(
        &self,
        order_id: i32,
        user_id: i32,
    ) -> Result<(), AppError>;

    /// 提交采购订单
    pub async fn submit_order(
        &self,
        order_id: i32,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 审批采购订单
    pub async fn approve_order(
        &self,
        order_id: i32,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 拒绝采购订单
    pub async fn reject_order(
        &self,
        order_id: i32,
        reason: String,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 关闭采购订单
    pub async fn close_order(
        &self,
        order_id: i32,
        user_id: i32,
    ) -> Result<purchase_order::Model, AppError>;

    /// 添加订单明细
    pub async fn add_order_item(
        &self,
        order_id: i32,
        req: CreateOrderItemRequest,
        user_id: i32,
    ) -> Result<purchase_order_item::Model, AppError>;

    /// 更新订单明细
    pub async fn update_order_item(
        &self,
        item_id: i32,
        req: UpdateOrderItemRequest,
        user_id: i32,
    ) -> Result<purchase_order_item::Model, AppError>;

    /// 删除订单明细
    pub async fn delete_order_item(
        &self,
        item_id: i32,
        user_id: i32,
    ) -> Result<(), AppError>;

    /// 计算订单总金额
    pub async fn calculate_order_total(
        &self,
        order_id: i32,
    ) -> Result<OrderTotal, AppError>;

    /// 检查订单状态
    pub async fn check_order_status(
        &self,
        order_id: i32,
    ) -> Result<OrderStatusInfo, AppError>;
}
```

### 4.2 采购入库服务 (PurchaseReceiptService)

```rust
pub struct PurchaseReceiptService {
    db: Arc<DbConnection>,
}

impl PurchaseReceiptService {
    /// 创建入库单
    pub async fn create_receipt(
        &self,
        req: CreatePurchaseReceiptRequest,
        user_id: i32,
    ) -> Result<purchase_receipt::Model, AppError>;

    /// 更新入库单
    pub async fn update_receipt(
        &self,
        receipt_id: i32,
        req: UpdatePurchaseReceiptRequest,
        user_id: i32,
    ) -> Result<purchase_receipt::Model, AppError>;

    /// 确认入库单
    pub async fn confirm_receipt(
        &self,
        receipt_id: i32,
        user_id: i32,
    ) -> Result<purchase_receipt::Model, AppError>;

    /// 添加入库明细
    pub async fn add_receipt_item(
        &self,
        receipt_id: i32,
        req: CreateReceiptItemRequest,
        user_id: i32,
    ) -> Result<purchase_receipt_item::Model, AppError>;

    /// 更新库存
    async fn update_inventory(
        &self,
        receipt_id: i32,
    ) -> Result<(), AppError>;
}
```

### 4.3 采购退货服务 (PurchaseReturnService)

```rust
pub struct PurchaseReturnService {
    db: Arc<DbConnection>,
}

impl PurchaseReturnService {
    /// 创建退货单
    pub async fn create_return(
        &self,
        req: CreatePurchaseReturnRequest,
        user_id: i32,
    ) -> Result<purchase_return::Model, AppError>;

    /// 更新退货单
    pub async fn update_return(
        &self,
        return_id: i32,
        req: UpdatePurchaseReturnRequest,
        user_id: i32,
    ) -> Result<purchase_return::Model, AppError>;

    /// 提交退货单
    pub async fn submit_return(
        &self,
        return_id: i32,
        user_id: i32,
    ) -> Result<purchase_return::Model, AppError>;

    /// 审批退货单
    pub async fn approve_return(
        &self,
        return_id: i32,
        user_id: i32,
    ) -> Result<purchase_return::Model, AppError>;

    /// 更新库存
    async fn update_inventory(
        &self,
        return_id: i32,
    ) -> Result<(), AppError>;
}
```

### 4.4 采购质检服务 (PurchaseInspectionService)

```rust
pub struct PurchaseInspectionService {
    db: Arc<DbConnection>,
}

impl PurchaseInspectionService {
    /// 创建质检单
    pub async fn create_inspection(
        &self,
        req: CreatePurchaseInspectionRequest,
        user_id: i32,
    ) -> Result<purchase_inspection::Model, AppError>;

    /// 更新质检单
    pub async fn update_inspection(
        &self,
        inspection_id: i32,
        req: UpdatePurchaseInspectionRequest,
        user_id: i32,
    ) -> Result<purchase_inspection::Model, AppError>;

    /// 完成质检单
    pub async fn complete_inspection(
        &self,
        inspection_id: i32,
        req: CompleteInspectionRequest,
        user_id: i32,
    ) -> Result<purchase_inspection::Model, AppError>;

    /// 计算质量得分
    pub async fn calculate_quality_score(
        &self,
        inspection_id: i32,
    ) -> Result<Decimal, AppError>;
}
```

### 4.5 采购价格服务 (PurchasePriceService)

```rust
pub struct PurchasePriceService {
    db: Arc<DbConnection>,
}

impl PurchasePriceService {
    /// 创建采购价格
    pub async fn create_price(
        &self,
        req: CreatePurchasePriceRequest,
        user_id: i32,
    ) -> Result<purchase_price::Model, AppError>;

    /// 更新采购价格
    pub async fn update_price(
        &self,
        price_id: i32,
        req: UpdatePurchasePriceRequest,
        user_id: i32,
    ) -> Result<purchase_price::Model, AppError>;

    /// 删除采购价格
    pub async fn delete_price(
        &self,
        price_id: i32,
        user_id: i32,
    ) -> Result<(), AppError>;

    /// 获取最新采购价格
    pub async fn get_latest_price(
        &self,
        material_id: i32,
        supplier_id: i32,
    ) -> Result<purchase_price::Model, AppError>;

    /// 导入采购价格
    pub async fn import_prices(
        &self,
        prices: Vec<CreatePurchasePriceRequest>,
        user_id: i32,
    ) -> Result<usize, AppError>;

    /// 导出采购价格
    pub async fn export_prices(
        &self,
        filter: PriceFilter,
    ) -> Result<Vec<PriceExportVO>, AppError>;
}
```

## 五、异常处理

### 5.1 业务异常

```rust
pub enum PurchaseError {
    /// 订单不存在
    #[error("采购订单不存在：{0}")]
    OrderNotFound(i32),
    
    /// 订单状态不允许操作
    #[error("订单状态不允许此操作：当前状态={0}")]
    InvalidOrderStatus(String),
    
    /// 订单已被完全入库
    #[error("订单已被完全入库，无法继续操作")]
    OrderFullyReceived,
    
    /// 入库数量超过订单数量
    #[error("入库数量 ({0}) 超过订单剩余数量 ({1})")]
    ExceedOrderQuantity(Decimal, Decimal),
    
    /// 退货数量超过入库数量
    #[error("退货数量超过可退货数量")]
    ExceedReturnableQuantity,
    
    /// 物料价格不存在
    #[error("物料 {0} 的采购价格不存在")]
    PriceNotFound(i32),
    
    /// 双计量单位换算失败
    #[error("双计量单位换算失败：{0}")]
    ConversionError(String),
    
    /// 库存不足
    #[error("库存不足：物料={0}, 可用={1}, 需求={2}")]
    InsufficientInventory(String, Decimal, Decimal),
}
```

### 5.2 事务处理

```rust
/// 创建采购订单（事务）
pub async fn create_order_with_items(
    &self,
    req: CreatePurchaseOrderWithItemsRequest,
    user_id: i32,
) -> Result<purchase_order::Model, AppError> {
    let txn = self.db.begin().await?;
    
    // 1. 创建采购订单
    let order = purchase_order::ActiveModel {
        order_no: Set(self.generate_order_no().await?),
        supplier_id: Set(req.supplier_id),
        // ... 其他字段
        created_by: Set(user_id),
        ..Default::default()
    }.insert(&txn).await?;
    
    // 2. 创建订单明细
    for item_req in req.items {
        purchase_order_item::ActiveModel {
            order_id: Set(order.id),
            line_no: Set(item_req.line_no),
            material_id: Set(item_req.material_id),
            // ... 其他字段
        }.insert(&txn).await?;
    }
    
    // 3. 计算订单总金额
    self.calculate_order_total_internal(order.id, &txn).await?;
    
    // 4. 提交事务
    txn.commit().await?;
    
    Ok(order)
}
```

## 六、性能优化

### 6.1 数据库优化

```sql
-- 1. 分区表（按日期分区）
-- 采购订单表按月分区
CREATE TABLE purchase_order_2025_01 PARTITION OF purchase_order
    FOR VALUES FROM ('2025-01-01') TO ('2025-02-01');

-- 2. 物化视图（统计信息）
CREATE MATERIALIZED VIEW mv_purchase_order_stats AS
SELECT 
    supplier_id,
    COUNT(*) as total_orders,
    SUM(total_amount) as total_amount,
    AVG(total_amount) as avg_order_amount,
    MAX(order_date) as last_order_date
FROM purchase_order
WHERE order_status IN ('COMPLETED', 'CLOSED')
GROUP BY supplier_id;

-- 3. 定期清理历史数据
CREATE OR REPLACE FUNCTION archive_old_purchase_orders()
RETURNS void AS $$
BEGIN
    -- 归档 3 年前的订单
    INSERT INTO purchase_order_archive
    SELECT * FROM purchase_order
    WHERE order_date < CURRENT_DATE - INTERVAL '3 years';
    
    DELETE FROM purchase_order
    WHERE order_date < CURRENT_DATE - INTERVAL '3 years';
END;
$$ LANGUAGE plpgsql;
```

### 6.2 缓存策略

```rust
// 使用 Redis 缓存采购价格
pub async fn get_material_price(
    &self,
    material_id: i32,
    supplier_id: i32,
) -> Result<Decimal, AppError> {
    let cache_key = format!("price:{}:{}", material_id, supplier_id);
    
    // 1. 尝试从缓存获取
    if let Some(price) = self.redis.get(&cache_key).await? {
        return Ok(price);
    }
    
    // 2. 从数据库查询
    let price = self.query_price_from_db(material_id, supplier_id).await?;
    
    // 3. 写入缓存（过期时间 24 小时）
    self.redis.set_ex(&cache_key, price, 86400).await?;
    
    Ok(price)
}
```

### 6.3 批量操作

```rust
/// 批量创建订单明细
pub async fn create_order_items_batch(
    &self,
    order_id: i32,
    items: Vec<CreateOrderItemRequest>,
    user_id: i32,
) -> Result<Vec<purchase_order_item::Model>, AppError> {
    let txn = self.db.begin().await?;
    
    let mut result = Vec::with_capacity(items.len());
    
    for item_req in items {
        let item = purchase_order_item::ActiveModel {
            order_id: Set(order_id),
            line_no: Set(item_req.line_no),
            // ... 其他字段
        }.insert(&txn).await?;
        
        result.push(item);
    }
    
    txn.commit().await?;
    
    Ok(result)
}
```

## 七、安全控制

### 7.1 权限控制

```rust
/// 采购订单权限检查
pub async fn check_order_permission(
    &self,
    order_id: i32,
    user_id: i32,
    action: &str,
) -> Result<(), AppError> {
    let order = self.get_order(order_id).await?;
    
    let user = user::Entity::find_by_id(user_id)
        .one(&*self.db)
        .await?
        .ok_or(AppError::UserNotFound(user_id))?;
    
    match action {
        "view" => {
            // 采购专员、采购经理、财务人员可查看
            if !user.has_role(&["PURCHASER", "PURCHASE_MANAGER", "FINANCE"]) {
                return Err(AppError::PermissionDenied(
                    "无权查看采购订单".to_string()
                ));
            }
        }
        "edit" => {
            // 只有创建者可编辑草稿订单
            if order.created_by != user_id {
                return Err(AppError::PermissionDenied(
                    "只能编辑自己创建的订单".to_string()
                ));
            }
            if order.order_status != "DRAFT" {
                return Err(AppError::InvalidOrderStatus(
                    "只有草稿订单可编辑".to_string()
                ));
            }
        }
        "approve" => {
            // 只有采购经理可审批
            if !user.has_role(&["PURCHASE_MANAGER"]) {
                return Err(AppError::PermissionDenied(
                    "只有采购经理可审批订单".to_string()
                ));
            }
        }
        _ => {}
    }
    
    Ok(())
}
```

### 7.2 数据验证

```rust
#[derive(Debug, Validate, Deserialize)]
pub struct CreatePurchaseOrderRequest {
    #[validate(required)]
    pub supplier_id: i32,
    
    #[validate(required)]
    pub warehouse_id: i32,
    
    #[validate(required)]
    pub department_id: i32,
    
    #[validate(required)]
    pub order_date: NaiveDate,
    
    pub expected_delivery_date: Option<NaiveDate>,
    
    #[validate(length(min = 1, message = "订单至少需要一行明细"))]
    pub items: Vec<CreateOrderItemRequest>,
    
    pub payment_terms: Option<String>,
    pub shipping_terms: Option<String>,
    pub notes: Option<String>,
}

#[derive(Debug, Validate, Deserialize)]
pub struct CreateOrderItemRequest {
    pub line_no: i32,
    
    #[validate(required)]
    pub material_id: i32,
    
    #[validate(range(min = 0.0001, message = "数量必须大于 0"))]
    pub quantity_ordered: Decimal,
    
    #[validate(range(min = 0.01, message = "单价必须大于 0"))]
    pub unit_price: Decimal,
    
    pub batch_no: Option<String>,
    pub color_code: Option<String>,
    pub lot_no: Option<String>,
    pub grade: Option<String>,
    pub gram_weight: Option<Decimal>,
    pub width: Option<Decimal>,
}
```

## 八、日志记录

### 8.1 操作日志

```rust
/// 记录采购订单操作日志
pub async fn log_order_action(
    &self,
    order_id: i32,
    action: &str,
    user_id: i32,
    details: Option<String>,
) -> Result<(), AppError> {
    let log = operation_log::ActiveModel {
        module: Set("PURCHASE".to_string()),
        business_type: Set("PURCHASE_ORDER".to_string()),
        business_id: Set(order_id.to_string()),
        action: Set(action.to_string()),
        operator_id: Set(user_id),
        details: Set(details),
        ..Default::default()
    };
    
    log.insert(&*self.db).await?;
    
    Ok(())
}
```

### 8.2 审计日志

```sql
-- 创建审计日志表
CREATE TABLE purchase_order_audit (
    id SERIAL PRIMARY KEY,
    order_id INTEGER NOT NULL,
    action VARCHAR(50) NOT NULL,
    old_data JSONB,
    new_data JSONB,
    changed_by INTEGER NOT NULL,
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 创建审计触发器
CREATE OR REPLACE FUNCTION audit_purchase_order_changes()
RETURNS TRIGGER AS $$
BEGIN
    IF TG_OP = 'UPDATE' THEN
        INSERT INTO purchase_order_audit (order_id, action, old_data, new_data, changed_by)
        VALUES (NEW.id, 'UPDATE', to_jsonb(OLD), to_jsonb(NEW), NEW.updated_by);
    ELSIF TG_OP = 'DELETE' THEN
        INSERT INTO purchase_order_audit (order_id, action, old_data, changed_by)
        VALUES (OLD.id, 'DELETE', to_jsonb(OLD), OLD.updated_by);
    END IF;
    RETURN NULL;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER audit_purchase_order
AFTER UPDATE OR DELETE ON purchase_order
FOR EACH ROW EXECUTE FUNCTION audit_purchase_order_changes();
```

## 九、集成测试

### 9.1 采购订单创建测试

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::services::purchase_order_service::PurchaseOrderService;
    
    #[tokio::test]
    async fn test_create_purchase_order() {
        let db = setup_test_db().await;
        let service = PurchaseOrderService::new(db);
        
        let req = CreatePurchaseOrderRequest {
            supplier_id: 1,
            warehouse_id: 1,
            department_id: 1,
            order_date: chrono::Utc::now().naive_utc().date(),
            expected_delivery_date: None,
            items: vec![
                CreateOrderItemRequest {
                    line_no: 10,
                    material_id: 1,
                    quantity_ordered: dec!(1000.0000),
                    unit_price: dec!(10.500000),
                    batch_no: Some("B20250115".to_string()),
                    color_code: Some("RED001".to_string()),
                    // ... 其他字段
                }
            ],
            payment_terms: None,
            shipping_terms: None,
            notes: None,
        };
        
        let order = service.create_order(req, 1).await.unwrap();
        
        assert_eq!(order.supplier_id, 1);
        assert_eq!(order.order_status, "DRAFT");
        assert!(order.total_amount > dec!(0.00));
    }
}
```

## 十、部署配置

### 10.1 数据库迁移

```bash
# 执行数据库迁移
sea-orm-migrate up -d database/migration

# 回滚迁移
sea-orm-migrate down -d database/migration

# 查看迁移状态
sea-orm-migrate status -d database/migration
```

### 10.2 配置文件

```toml
# config/purchase.toml
[module.purchase]
enabled = true

[module.purchase.order]
auto_generate_no = true
no_format = "PO{YYYYMMDD}{NNN}"
allow_negative_inventory = false
require_inspection = true

[module.purchase.price]
enable_history = true
max_history_days = 365
cache_enabled = true
cache_ttl_seconds = 86400

[module.purchase.approval]
enable_workflow = true
auto_approve_limit = 1000.00  # 1000 元以下自动审批
require_dual_approval = true  # 大额订单需要双重审批
```

## 十一、总结

本技术方案完整覆盖了采购管理模块的 6 大核心功能：
1. ✅ 采购申请管理
2. ✅ 采购订单管理（13 个 API）
3. ✅ 采购入库管理（7 个 API）
4. ✅ 采购退货管理（6 个 API）
5. ✅ 采购质检管理（5 个 API）
6. ✅ 采购价格管理（6 个 API）

**关键特性**:
- 支持面料行业五维管理（批次、色号、缸号、等级、克重、幅宽）
- 支持双计量单位自动换算
- 完整的事务处理和异常处理
- 全面的权限控制和安全验证
- 高性能优化（分区表、缓存、批量操作）
- 完整的审计日志和操作日志

下一步将根据此技术方案进行任务规划和编码实现。
