# P0-D15：nginx upstream 活跃实例配置 - green
# 用法：ln -sf /etc/nginx/bingxi-upstream-green.conf /etc/nginx/bingxi-upstream.active.conf
# 切换后执行 nginx -s reload 实现零停机流量切换
upstream bingxi_backend {
    server 127.0.0.1:8083;
    server 127.0.0.1:8082 backup;
}
