[advisories]
ignore = [
    # rsa 0.9.10 存在 Marvin Attack 时序侧信道漏洞，但 sqlx-mysql 依赖此版本且暂无修复版本
    "RUSTSEC-2023-0071",
    # idna 0.4.0 被 validator 0.16.1 锁定，validator 尚未发布新版本
    "RUSTSEC-2024-0421",
    # protobuf 2.28.0 被 prometheus 0.13.4 锁定，prometheus 尚未升级
    "RUSTSEC-2024-0437",
    # 批次 24（2026-06-29）：lopdf 0.31.0 存在栈溢出漏洞（RUSTSEC-2026-0187，CVSS 7.5），
    # 被 printpdf 0.7.0 锁定为传递依赖；printpdf 暂未发布兼容 lopdf 0.42+ 的新版本。
    # 影响：未受信任 PDF 文件可触发 SIGABRT DoS。本项目仅使用 printpdf 生成内部 PDF（无外部输入），
    # 风险可接受；待 printpdf 发布兼容版本后立即移除 ignore。
    "RUSTSEC-2026-0187",
]
