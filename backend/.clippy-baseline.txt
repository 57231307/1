
#
# 1. 逐步为预留服务编写路由 handler + 注册路由
# 2. 每修复一批，CI 自动刷新 baseline（main 分支 + 无新警告 + 已修复 > 0）
# 3. 当 baseline 警告数 < 50 时，切换为严格模式（零容忍）
# Clippy Baseline（2026-08-12 重置）
# 清空 baseline，让 CI 重新生成准确的警告列表
# 之前的 baseline 已过时，很多代码已被使用但警告未删除
