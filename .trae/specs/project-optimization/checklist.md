# 秉羲面料管理优化与功能扩展 - 验证清单

## 阶段一：基础设施层 - 安全强化与后端访问控制

### [ ] 检查点 1.1：前端路由保护验证
- 未登录用户访问受保护路由时重定向到登录页
- Token过期用户访问受保护路由时重定向到登录页
- 已登录用户可以正常访问所有授权页面
- 登录页面本身不受路由守卫影响

### [ ] 检查点 1.2：后端CORS配置验证
- 非允许域名的跨域请求被拒绝
- 允许域名的跨域请求正常处理
- 响应头包含正确的CORS配置
- 响应头包含安全相关头部（X-Frame-Options, X-Content-Type-Options等）

### [ ] 检查点 1.3：请求头验证验证
- 缺少X-Requested-With请求头的请求返回403
- 包含正确请求头的请求正常处理
- 公开路径（登录、健康检查等）不需要验证请求头
- 错误请求有明确的错误信息

### [ ] 检查点 1.4：认证中间件验证
- 公开路径无需认证即可访问
- 无效Token返回401状态码
- 过期Token返回401状态码
- 认证失败记录到日志
- 认证成功后用户信息正确注入到请求上下文

### [ ] 检查点 1.5：Nginx配置验证
- 直接访问后端端口被拒绝
- 通过Nginx代理访问正常
- 响应头包含安全相关头部
- 静态资源正确代理

## 阶段二：数据模型层 - 权限体系与供应商产品映射

### [ ] 检查点 2.1：权限数据模型验证
- 权限表结构正确创建
- 角色权限关联表结构正确创建
- 数据权限配置表结构正确创建
- 外键约束正确设置

### [ ] 检查点 2.2：权限中间件验证
- 无功能权限的用户访问被拒绝并返回403
- 有功能权限的用户访问正常
- 数据权限验证正确执行（全部数据/本部门数据/本人数据）
- 权限验证失败有明确的错误信息

### [ ] 检查点 2.3：供应商产品映射数据模型验证
- 供应商产品表结构正确创建
- 供应商产品色号表结构正确创建
- 产品色号映射表结构正确创建
- 外键约束正确设置

### [ ] 检查点 2.4：供应商产品映射模型文件验证
- 后端模型文件编译通过
- 模型与数据库表结构一致
- 模型关系正确定义（一对多、多对多）

### [ ] 检查点 2.5：供应商产品映射前端模型验证
- 前端模型编译通过
- 前端模型与后端模型字段一致
- 序列化/反序列化正常工作

## 阶段三：业务逻辑层 - 功能实现

### [ ] 检查点 3.1：供应商产品管理服务验证
- 创建供应商产品成功并返回正确数据
- 查询供应商产品列表返回正确数据
- 更新供应商产品成功
- 删除供应商产品成功
- 创建产品色号映射成功
- 查询产品色号映射返回正确数据

### [ ] 检查点 3.2：供应商产品管理API接口验证
- POST /api/v1/erp/supplier-products 创建成功返回201
- GET /api/v1/erp/supplier-products 查询成功返回200
- PUT /api/v1/erp/supplier-products/:id 更新成功返回200
- DELETE /api/v1/erp/supplier-products/:id 删除成功返回204
- 无权限用户访问返回403

### [ ] 检查点 3.3：供应商产品管理前端服务验证
- 前端页面正常渲染
- 创建供应商产品表单提交成功
- 产品色号映射功能正常
- 列表分页功能正常
- 搜索过滤功能正常

### [ ] 检查点 3.4：销售转采购转换服务验证
- 销售订单审批后自动生成采购订单
- 采购订单按供应商正确拆分
- 产品色号正确转换为供应商产品色号
- 默认供应商选择正确
- 转换日志正确记录

### [ ] 检查点 3.5：权限管理服务验证
- 创建权限成功
- 查询权限列表成功
- 分配角色权限成功
- 配置数据权限成功
- 权限变更立即生效

### [ ] 检查点 3.6：权限管理API接口验证
- 权限CRUD接口正常工作
- 角色权限分配接口正常工作
- 数据权限配置接口正常工作
- 权限验证中间件正确拦截未授权请求

## 阶段四：代码优化层 - 性能与质量提升

### [ ] 检查点 4.1：数据库查询优化验证
- 所有查询使用索引（通过EXPLAIN验证）
- 无N+1查询问题
- 查询响应时间符合要求（< 200ms）
- 数据库连接池配置合理

### [ ] 检查点 4.2：缓存策略验证
- 缓存命中时响应时间明显减少
- 缓存失效后数据正确更新
- 缓存命中率统计正常
- 内存使用在合理范围内

### [ ] 检查点 4.3：前端性能优化验证
- 首屏加载时间 < 3秒
- 页面交互流畅，无明显卡顿
- 大列表渲染正常
- 组件懒加载正常工作

### [ ] 检查点 4.4：代码重复消除验证
- 代码审查确认无重复代码块
- 公共函数和组件正确提取
- 编译无警告
- cargo clippy检测通过

### [ ] 检查点 4.5：未使用代码清理验证
- cargo clippy无dead_code警告
- 编译成功
- 所有测试通过
- 功能无回归

### [ ] 检查点 4.6：API响应格式统一验证
- 所有API响应格式一致为 { code, message, data }
- 错误响应格式一致
- 前端API调用适配完成
- API文档更新

### [ ] 检查点 4.7：缺失CRUD操作补全验证
- 所有模块都有完整的CRUD操作
- 前后端接口一一对应
- 操作日志正确记录
- 数据一致性保持

### [ ] 检查点 4.8：重复功能接口消除验证
- 无功能重复的API接口
- 前端调用正常工作
- API文档更新
- 路由配置清晰

### [ ] 检查点 4.9：代码注释完善验证
- 代码注释覆盖率 > 30%
- 公共API都有文档注释
- 复杂逻辑有说明注释
- 注释内容准确有用

### [ ] 检查点 4.10：集成测试与验证
- 所有功能测试通过
- 安全测试无漏洞
- 性能指标达标
- 无功能回归

## 最终验收

### [ ] 安全验收
- 未登录只能访问登录页
- 后端不能直接访问
- 所有API需要认证
- 权限控制正确执行
- 无安全漏洞

### [ ] 功能验收
- 供应商产品管理功能完整
- 销售转采购自动转换正确
- 权限管理功能完整
- 所有CRUD操作完整

### [ ] 性能验收
- API响应时间 < 200ms（95%请求）
- 首屏加载时间 < 3s
- 数据库查询优化
- 缓存策略有效

### [ ] 代码质量验收
- 代码无重复
- 代码无未使用部分
- API响应格式统一
- 代码注释完善
- 编译无警告
