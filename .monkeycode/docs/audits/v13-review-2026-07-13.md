# v13 复审报告（2026-07-13，规则 15 启动）

> v12 复审全部完成后启动，新增"业务场景闭环""财务场景闭环""运行逻辑环流程闭环"等复审维度。
> 核心目标：baseline 213 条摘要行（~993 个警告）全部清零 + 业务/财务/逻辑闭环问题修复。
> 执行策略：规则 13+14+15 联动，复审完成后自动连续修复，每批 5-6 文件，CI 全绿后自动进入下一批，无需用户确认。

---

## 进度总览

| 维度 | 总数 | P0 | P1 | P2 | P3 | 状态 |
|------|------|----|----|----|----|------|
| 🟢 baseline 警告清零 | 213 摘要 / 89 位置 / 135 文件 | - | - | - | - | ⏳ 待修复 |
| 🟢 业务场景闭环 | 21 | 6 | 9 | 6 | 0 | ⏳ 待修复 |
| 🟢 财务场景闭环 | 16 | 8 | 4 | 4 | 0 | ⏳ 待修复 |
| 🟢 运行逻辑环流程闭环 | 45 | 0 | 6 | 14 | 25 | ⏳ 待修复 |
| **合计** | **295** | **14** | **19** | **24** | **25** | — |

**修复优先级**：P0（14 项阻塞）→ P1（19 项高）→ P2（24 项中）→ P3（25 项低），每批 5-6 文件，CI 全绿后自动进入下一批。

---

## 一、baseline 警告清零（213 摘要 / 89 位置 / 135 文件）

### 警告类型分布

| 类型 | 数量 | 说明 |
|------|------|------|
| dead_code | ~193 | associated function/struct/enum/variant/field never used/read/constructed |
| unused_import | 15 | 未使用的导入 |
| needless_borrow/asref | 1 | 不必要的借用/引用 |
| too_many_arguments (8/7) | 1 | 函数参数过多 |
| unreachable_code | 1 | 不可达代码 |
| unused_variable | 1 | 未使用的变量 |
| unused_mut | 1 | 不需要的 mut |

### 警告最多的文件（按 `-->` 位置行统计，89 条位置行）

| 文件 | 警告数 |
|------|--------|
| tests/bi_analysis_test.rs | 18 |
| src/services/bi_analysis_service.rs | 11 |
| src/services/auth/password_policy_service.rs | 3 |
| src/search/elastic.rs | 3 |
| src/websocket/notifications.rs | 2 |
| src/utils/failover/cache.rs | 2 |
| src/services/p9_5_sales_extra_tests.rs | 2 |
| src/services/p9_5_inventory_extra_tests.rs | 2 |
| src/services/failover_service.rs | 2 |
| src/services/ap_payment_service.rs | 2 |
| src/middleware/auth_context.rs | 2 |
| src/handlers/ap_payment_handler.rs | 2 |
| 其余 48 个文件 | 各 1 |

---

## 二、业务场景闭环（21 项：6 P0 + 9 P1 + 6 P2）

### 🔴 P0 级（6 项 - 核心业务链路断裂）

| # | 问题描述 | 文件位置 | 影响范围 | 修复方案 |
|---|---------|---------|---------|---------|
| B-P0-1 | 销售订单审批后未触发库存预留/锁定：`approve_order` 仅更新订单状态，不调用 `InventoryReservationService::create_reservation` | [services/so/order_workflow.rs#L206-L247](file:///workspace/backend/src/services/so/order_workflow.rs) | 销售订单→库存锁定链路完全断开；超卖风险；库存预留机制形同虚设 | 在 `approve_order` commit 成功后调用 `InventoryReservationService::create_reservation` |
| B-P0-2 | 销售出库凭证未自动生成：`reduce_inventory` 仅做库存数量扣减，不调用 `record_transaction_txn`，不生成 `SALES_DELIVERY` 类型库存流水 | [services/so/delivery.rs#L521-L595](file:///workspace/backend/src/services/so/delivery.rs) | 销售订单→发货→财务凭证链路断点；主营业务成本/库存商品凭证缺失 | 在 `reduce_inventory` 内扣减库存后调用 `record_transaction_txn`（类型 `SALES_DELIVERY`） |
| B-P0-3 | 生产订单成本核算链路完全断裂：`complete_production_order` 不调用 `CostCollectionService` | [services/production_order_service.rs#L482-L523](file:///workspace/backend/src/services/production_order_service.rs) | 生产成本无法归集；产品成本失真；BI 报表成本数据缺失 | 在 `complete_production_order` commit 成功后调用 `CostCollectionService` 做成本归集 |
| B-P0-4 | 生产订单凭证事件类型不匹配：发布 `PRODUCTION_CONSUMPTION`/`PRODUCTION_OUTPUT`，但订阅方只处理 `PRODUCTION_RECEIPT`/`PRODUCTION_ISSUE` | 发布方：[services/production_order_service.rs#L692-L715](file:///workspace/backend/src/services/production_order_service.rs)<br>订阅方：[services/inventory_finance_bridge_service.rs#L196-L220](file:///workspace/backend/src/services/inventory_finance_bridge_service.rs) | 生产入库/领料凭证全部缺失 | 二选一：统一事件类型 |
| B-P0-5 | 采购退货财务凭证未生成：发布 `PURCHASE_RETURN` 事件，订阅方无此类型处理分支 | 发布方：[services/purchase_return_service.rs#L290-L312](file:///workspace/backend/src/services/purchase_return_service.rs)<br>订阅方：[services/inventory_finance_bridge_service.rs#L196-L220](file:///workspace/backend/src/services/inventory_finance_bridge_service.rs) | 采购退货→财务凭证链路断点；应付红字凭证缺失 | 在 `handle_inventory_transaction` 增加 `PURCHASE_RETURN` 分支 |
| B-P0-6 | 销售退货财务凭证未生成：发布 `SALES_RETURN` 事件，订阅方无此类型处理分支 | 发布方：[services/sales_return_service.rs#L485-L506](file:///workspace/backend/src/services/sales_return_service.rs)<br>订阅方：[services/inventory_finance_bridge_service.rs#L196-L220](file:///workspace/backend/src/services/inventory_finance_bridge_service.rs) | 销售退货→财务凭证链路断点；红字收入凭证缺失 | 在 `handle_inventory_transaction` 增加 `SALES_RETURN` 分支 |

### 🟠 P1 级（9 项 - 跨模块一致性/事件闭环缺失）

| # | 问题描述 | 文件位置 | 修复方案 |
|---|---------|---------|---------|
| B-P1-1 | 销售退货使用非事务版本 `record_transaction` 导致幻事件：事务回滚时事件已发出 | [services/sales_return_service.rs#L416-L506](file:///workspace/backend/src/services/sales_return_service.rs) | 改用 `record_transaction_txn`（事务版本） |
| B-P1-2 | 库存盘点完成未触发任何事件：不发布 `InventoryCountCompleted`，不调用 `record_transaction_txn` | [services/inventory_count_service.rs#L356-L446](file:///workspace/backend/src/services/inventory_count_service.rs) | 循环内对有差异的明细调用 `record_transaction_txn`，commit 后统一 publish |
| B-P1-3 | 客户/供应商主数据变更未同步关联单据：冗余名称字段会变为脏数据 | [services/customer_service.rs](file:///workspace/backend/src/services/customer_service.rs)、[services/supplier_service.rs](file:///workspace/backend/src/services/supplier_service.rs) | 发布 `CustomerUpdated`/`SupplierUpdated` 事件，监听器异步刷新关联单据 |
| B-P1-4 | 销售订单状态变更未发布任何事件：`submit_order`/`approve_order`/`complete_order`/`cancel_order` 均不发布事件 | [services/so/order_workflow.rs#L29-L291](file:///workspace/backend/src/services/so/order_workflow.rs) | 在每个状态变更 commit 后发布对应事件 |
| B-P1-5 | 采购订单状态变更未发布 `PurchaseOrderApproved` 事件 | [services/po/order.rs](file:///workspace/backend/src/services/po/order.rs) | 在 `approve_order` commit 后发布事件 |
| B-P1-6 | 三个孤岛事件（有订阅无发布）：`InventoryAdjusted`/`PurchaseOrderApproved`/`InventoryCountCompleted` | [services/event_bus.rs#L64-L91](file:///workspace/backend/src/services/event_bus.rs) | 删除这些事件变体或在对应业务流程中真实接入 |
| B-P1-7 | 事件处理失败无重试 + 死信队列 + 告警：仅 `tracing::error!` 打日志 | [services/event_bus.rs#L354-L730](file:///workspace/backend/src/services/event_bus.rs) | 引入重试机制（指数退避）+ 死信队列 + 告警 |
| B-P1-8 | `SalesOrderShipped`/`InventoryTransactionCreated` 事件无幂等处理：重复消费会重复触发财务指标计算/重复生成凭证 | [services/event_bus.rs#L374-L388](file:///workspace/backend/src/services/event_bus.rs) | 事件 payload 增加唯一键，监听器消费前检查是否已处理 |
| B-P1-9 | `BpmProcessFinished` 事件处理覆盖不全：仅处理 `purchase_order`/`sales_order`，生产订单 BPM 审批结果无法回写 | [services/event_bus.rs#L452-L531](file:///workspace/backend/src/services/event_bus.rs) | 在 `BpmProcessFinished` match 中增加 `production_order` 分支 |

### 🟡 P2 级（6 项 - 孤岛 service/冗余逻辑）

| # | 问题描述 | 文件位置 | 修复方案 |
|---|---------|---------|---------|
| B-P2-1 | `ar_service::create_payment` 与 `mark_as_paid` 状态更新重复 | [services/ar_service.rs#L264-L279](file:///workspace/backend/src/services/ar_service.rs) | 移除 `mark_as_paid` 监听分支或 `create_payment` 不在事务内更新状态 |
| B-P2-2 | `customer_credit_evaluate.rs` 是孤岛 service：全代码库无任何调用 | [services/customer_credit_evaluate.rs](file:///workspace/backend/src/services/customer_credit_evaluate.rs) | 评估后删除或接入业务 |
| B-P2-3 | `CostCollectionService` 仅 HTTP 调用，无业务联动 | [services/cost_collection_service.rs](file:///workspace/backend/src/services/cost_collection_service.rs) | 在 `production_order_service::complete_production_order` 中调用 |
| B-P2-4 | `MrpEngineService` 仅 HTTP 调用，无业务联动 | [services/mrp_engine_service.rs](file:///workspace/backend/src/services/mrp_engine_service.rs) | 在销售订单审批/生产订单创建时调用 MRP |
| B-P2-5 | `CapacityService` 仅 HTTP 调用，无业务联动 | [services/capacity_service.rs](file:///workspace/backend/src/services/capacity_service.rs) | 在生产订单转 SCHEDULED 时调用产能校验 |
| B-P2-6 | `InventoryReservationService` 仅 HTTP 调用，销售流程未集成 | [services/inventory_reservation_service.rs](file:///workspace/backend/src/services/inventory_reservation_service.rs) | 在 `SalesService::approve_order` 中调用 |

---

## 三、财务场景闭环（16 项：8 P0 + 4 P1 + 4 P2）

### 🔴 P0 级（8 项 - 业财脱节致命问题）

| # | 问题描述 | 文件位置 | 影响范围 | 修复方案 |
|---|---------|---------|---------|---------|
| F-P0-1 | 凭证科目余额回写链路断裂：`account_balance` → `account_subject` 未回写 | [services/voucher_service.rs](file:///workspace/backend/src/services/voucher_service.rs)、[services/account_subject_service.rs](file:///workspace/backend/src/services/account_subject_service.rs) | 科目余额与凭证不一致；账簿数据失真 | 凭证 post 时回写科目余额 |
| F-P0-2 | 库存桥接凭证只 create 不 post：`inventory_finance_bridge_service` 创建凭证后不调用 `post` | [services/inventory_finance_bridge_service.rs](file:///workspace/backend/src/services/inventory_finance_bridge_service.rs) | 库存类凭证全部处于草稿状态；科目余额不更新 | 创建凭证后调用 `voucher_service.post` |
| F-P0-3 | 销售出库缺收入凭证：销售出库仅生成库存凭证，不生成收入凭证 | [services/so/delivery.rs](file:///workspace/backend/src/services/so/delivery.rs) | 主营业务收入凭证缺失；利润表数据失真 | 在销售出库时同步生成收入凭证 + 成本凭证 |
| F-P0-4 | AR 收款未生成凭证：`ar_service::create_payment` 不调用 `voucher_service` | [services/ar_service.rs#L264-L279](file:///workspace/backend/src/services/ar_service.rs) | 收款核销凭证缺失；现金/银行/应收科目不平衡 | 在 `create_payment` 中调用 `voucher_service` 生成核销凭证 |
| F-P0-5 | AP 付款未生成凭证：`ap_payment_service::confirm` 不调用 `voucher_service` | [services/ap_payment_service.rs#L313-L322](file:///workspace/backend/src/services/ap_payment_service.rs) | 付款核销凭证缺失；现金/银行/应付科目不平衡 | 在 `confirm` 中调用 `voucher_service` 生成核销凭证 |
| F-P0-6 | 销售→应收链路断开：销售订单/发货不生成应收发票 | [services/so/delivery.rs](file:///workspace/backend/src/services/so/delivery.rs) | 应收账款与销售数据不一致 | 在销售发货后生成应收发票 |
| F-P0-7 | 采购→应付链路断开：采购入库不生成应付发票 | [services/po/receipt.rs](file:///workspace/backend/src/services/po/receipt.rs) | 应付账款与采购数据不一致 | 在采购入库后生成应付发票 |
| F-P0-8 | AR/AP 核销未生成凭证：核销操作不调用 `voucher_service` | [services/ar/recon.rs](file:///workspace/backend/src/services/ar/recon.rs)、[services/ap_payment_service.rs](file:///workspace/backend/src/services/ap_payment_service.rs) | 核销凭证缺失；应收/应付明细账不平衡 | 在核销时生成核销凭证 |

### 🟠 P1 级（4 项 - 期间与报表闭环不完整）

| # | 问题描述 | 文件位置 | 修复方案 |
|---|---------|---------|---------|
| F-P1-1 | `close_period` 未做试算平衡校验、未做期末结转 | [services/accounting_period_service.rs](file:///workspace/backend/src/services/accounting_period_service.rs) | 增加 `trial_balance` 校验 + 期末结转逻辑 |
| F-P1-2 | 三大报表不走凭证体系：利润表硬编码 70% 成本比例（违反禁止硬编码规则） | [services/report/](file:///workspace/backend/src/services/report/) | 报表从凭证/科目余额取数，移除硬编码 |
| F-P1-3 | 明细账查询的 `assist_accounting_record` 表凭证侧未写入 | [services/](file:///workspace/backend/src/services/) | 凭证 post 时同步写入辅助核算记录 |
| F-P1-4 | `account_subject_service` 缺 `refresh_balance` 方法 | [services/account_subject_service.rs](file:///workspace/backend/src/services/account_subject_service.rs) | 新增 `refresh_balance` 方法 |

### 🟡 P2 级（4 项 - 缺失功能/增强项）

| # | 问题描述 | 修复方案 |
|---|---------|---------|
| F-P2-1 | 无期末调整（暂估/摊销/预提）机制 | 新增期末调整 service |
| F-P2-2 | 报表无穿透追溯功能 | 报表增加穿透到凭证/明细账 |
| F-P2-3 | 销售成本按 `product.cost_price` 计算未与采购实际单价联动 | 销售成本按移动加权平均法计算 |
| F-P2-4 | AR/AP 对账单生成不触发凭证 | 对账单确认后生成凭证 |

---

## 四、运行逻辑环流程闭环（45 项：0 P0 + 6 P1 + 14 P2 + 25 P3）

### 业务流程闭环（10 项）

| # | 问题描述 | 文件位置 | 优先级 | 修复方案 |
|---|---------|---------|--------|---------|
| L-1 | CLI 子命令分发吞错：`backup`/`restore` 命令返回的 `Result` 被直接丢弃 | [cli/util/mod.rs#L145-L146](file:///workspace/backend/src/cli/util/mod.rs#L145-L146) | P1 | 改为 `if let Err(e) = ... { eprintln!(...); std::process::exit(1); }` |
| L-2 | 升级脚本清理失败吞错：10 处 `let _ = run_cmd("rm", ...)` | [cli/util/upgrade.rs#L213-L312](file:///workspace/backend/src/cli/util/upgrade.rs#L213-L312) | P2 | 改为 `tracing::warn!` 记录 |
| L-3 | 备份脚本清理失败吞错：7 处 `let _ = run_cmd("rm", ...)` | [cli/util/backup.rs#L38-L242](file:///workspace/backend/src/cli/util/backup.rs#L38-L242) | P2 | 同上 |
| L-4 | 错误路径回滚失败吞错：`let _ = txn.rollback().await;` | [services/fixed_asset_service.rs#L305](file:///workspace/backend/src/services/fixed_asset_service.rs#L305) | P2 | 改为 `if let Err(rb_err) = ... { tracing::error!(...); }` |
| L-5 | 上传文件清理失败吞错：3 处 `let _ = std::fs::remove_file(...)` | [handlers/system_update_handler.rs#L227-L239](file:///workspace/backend/src/handlers/system_update_handler.rs#L227-L239) | P3 | 同上 |
| L-6 | `event_bus.publish` 失败吞错：`let _ = state.local_tx.send(...)` | [services/event_bus.rs#L206](file:///workspace/backend/src/services/event_bus.rs#L206) | P2 | 改为 `if ...is_err() { tracing::warn!(...); }` |
| L-7 | WebSocket 推送失败吞错 | [websocket/notifications.rs#L113](file:///workspace/backend/src/websocket/notifications.rs#L113) | P3 | 区分 `is_err()` 后 warn 日志 |
| L-8 | admin CLI 读取密码吞错 | [cli/admin.rs#L100](file:///workspace/backend/src/cli/admin.rs#L100) | P3 | 改为 `?` 传播或 warn 日志 |
| L-9 | 健康检查启动时间初始化吞错 | [main.rs#L270](file:///workspace/backend/src/main.rs#L270) | P3 | 改为 warn 日志 |
| L-10 | `init_service.test_database` 查询结果丢弃 | [services/init_service.rs#L132-L138](file:///workspace/backend/src/services/init_service.rs#L132-L138) | P3 | 简化逻辑，移除冗余 `let _ =` |

### 异常路径闭环（10 项）

| # | 问题描述 | 文件位置 | 优先级 | 修复方案 |
|---|---------|---------|--------|---------|
| L-11 | 静态正则 expect：`Regex::new(...).expect(...)` 每次调用时编译 | [services/supplier_service.rs#L18](file:///workspace/backend/src/services/supplier_service.rs#L18) | P2 | 改 `OnceLock<Regex>` 或 `lazy_static!` |
| L-12 | HMAC new_from_slice expect 在 spawn 任务内 | [services/email_service.rs#L691](file:///workspace/backend/src/services/email_service.rs#L691) | P3 | 改 `?` 传播或返回 `Result` |
| L-13 | Argon2id 参数 expect 在 bin 中 | [bin/hash_password.rs#L19](file:///workspace/backend/src/bin/hash_password.rs#L19) | P3 | 改 `OnceLock<Params>` 或返回 `Result` |
| L-14 | 时间工具静态 expect：3 处 | [utils/date_utils.rs#L9-L26](file:///workspace/backend/src/utils/date_utils.rs#L9-L26) | P3 | 改 `const` 计算或 `OnceLock` |
| L-15 | 中间件超时 fallback `Response::builder` expect | [middleware/timeout.rs#L30](file:///workspace/backend/src/middleware/timeout.rs#L30) | P3 | 改 `unwrap_or_else` 兜底 |
| L-16 | CSRF 中间件测试代码 expect：9 处 | [middleware/csrf.rs#L239-L339](file:///workspace/backend/src/middleware/csrf.rs#L239-L339) | P3 | 改用 `dec!/decs!` 宏 |
| L-17 | `ap_reconciliation_service` 测试中 `let _ = result;`：4 处无效测试 | [services/ap_reconciliation_service.rs#L1068-L1100](file:///workspace/backend/src/services/ap_reconciliation_service.rs#L1068-L1100) | P3 | 至少 `assert!(result.is_err())` |
| L-18 | `mrp_engine_service` 测试中 `let _ = result;`：3 处无效测试 | [services/mrp_engine_service.rs#L1485-L1521](file:///workspace/backend/src/services/mrp_engine_service.rs#L1485-L1521) | P3 | 同上 |
| L-19 | `bom_service`/`customer_credit_limit` 测试中 `let _ = result;` | [services/bom_service.rs#L1019-L1034](file:///workspace/backend/src/services/bom_service.rs#L1019-L1034) | P3 | 同上 |
| L-20 | `production_order_service.rs:1634` `let _ = service;` 占位抑制 dead_code | [services/production_order_service.rs#L1634](file:///workspace/backend/src/services/production_order_service.rs#L1634) | P3 | 删除占位变量或接入业务 |

### 状态机闭环（5 项）

| # | 问题描述 | 文件位置 | 优先级 | 修复方案 |
|---|---------|---------|--------|---------|
| L-21 | `MatchStatus` 枚举缺终态：无 `Disputed`/`Cancelled` | [models/ar_reconciliation_item.rs#L36-L46](file:///workspace/backend/src/models/ar_reconciliation_item.rs#L36-L46) | P1 | 增加 `Disputed` 终态 |
| L-22 | `BorrowStatus` 缺主动取消状态：无 `Cancelled` | [services/color_card_borrow_service.rs#L40-L45](file:///workspace/backend/src/services/color_card_borrow_service.rs#L40-L45) | P2 | 增加 `Cancelled` 终态 + `cancel_borrow` 方法 |
| L-23 | `DyeBatchStatus` 缺失回退/异常态：无 `Failed`/`OnHold` | [handlers/dye_batch_handler.rs#L22-L31](file:///workspace/backend/src/handlers/dye_batch_handler.rs#L22-L31) | P2 | 增加 `Failed` + `OnHold` 状态 |
| L-24 | `InitTaskStatus` 缺终态完整性文档 | [services/init_service.rs#L23-L30](file:///workspace/backend/src/services/init_service.rs#L23-L30) | P3 | 文档说明失败后恢复路径 |
| L-25 | `process_state_machine.rs` 完整闭环（已验证通过） | [utils/process_state_machine.rs#L12-L65](file:///workspace/backend/src/utils/process_state_machine.rs#L12-L65) | P3 | 已闭环，无需修复 |

### 资源生命周期闭环（10 项）

| # | 问题描述 | 文件位置 | 优先级 | 修复方案 |
|---|---------|---------|--------|---------|
| L-26 | 5 个后台定时任务缺 cancellation token | [main.rs#L478-L485](file:///workspace/backend/src/main.rs#L478-L485) 等 | P1 | 引入 `tokio_util::sync::CancellationToken` |
| L-27 | Kafka 消费桥接 JoinHandle 显式丢弃 | [services/event_bus.rs#L278](file:///workspace/backend/src/services/event_bus.rs#L278) | P1 | 保存到 `EventBusState`，shutdown 时 abort |
| L-28 | 主事件监听器 spawn 句柄丢失 | [services/event_bus.rs#L346](file:///workspace/backend/src/services/event_bus.rs#L346) | P1 | 同上 |
| L-29 | 库存财务桥接监听器 spawn 句柄丢失 | [services/inventory_finance_bridge_service.rs#L112](file:///workspace/backend/src/services/inventory_finance_bridge_service.rs#L112) | P1 | 同上 |
| L-30 | OmniAudit 异步引擎 spawn 句柄丢失 | [services/omni_audit_service.rs#L86](file:///workspace/backend/src/services/omni_audit_service.rs#L86) | P2 | 同上，且 channel sender 释放策略需文档化 |
| L-31 | WebSocket 长连接 recv_task/send_task 句柄未保存 | [websocket/notifications.rs#L353](file:///workspace/backend/src/websocket/notifications.rs#L353) | P2 | 句柄保存到 `ConnectionEntry` |
| L-32 | 审计日志异步落库 spawn 句柄丢失 | [services/audit_log_service.rs#L331-L335](file:///workspace/backend/src/services/audit_log_service.rs#L331-L335) | P3 | 改为 mpsc channel + 单消费者模式 |
| L-33 | 数据库事务 commit/rollback 路径完备（已验证通过） | 多文件 | P3 | 已闭环 |
| L-34 | `Arc<Mutex<T>>` 锁中毒处理已统一降级（已验证通过） | 多文件 | P3 | 已闭环 |
| L-35 | 文件句柄 Drop 闭环（已验证通过） | 全项目 | P3 | 已闭环 |

### 配置依赖闭环（10 项）

| # | 问题描述 | 文件位置 | 优先级 | 修复方案 |
|---|---------|---------|--------|---------|
| L-36 | `AUTH_CHECK_USER_ACTIVE` silent default | [middleware/auth.rs#L123](file:///workspace/backend/src/middleware/auth.rs#L123) | P2 | 启动期通过 `AppSettings` 显式读取并打印当前值 |
| L-37 | `AUDIT_RETENTION_DAYS` silent default 365 | [main.rs#L445-L449](file:///workspace/backend/src/main.rs#L445-L449) | P3 | 改为通过 `AppSettings` 读取，缺失时 warn |
| L-38 | `BINGXI_SLOW_QUERY_MS` silent default 100 + 与 AppSettings 重复 | [middleware/slow_query.rs#L28-L31](file:///workspace/backend/src/middleware/slow_query.rs#L28-L31) | P2 | 统一走 `AppSettings`，移除环境变量直读 |
| L-39 | `ELASTICSEARCH_URL` silent default 空 | [main.rs#L548](file:///workspace/backend/src/main.rs#L548) | P3 | 启动期 warn 提示 |
| L-40 | `telemetry.rs` 多个 silent default | [telemetry.rs#L53-L67](file:///workspace/backend/src/telemetry.rs#L53-L67) | P3 | 生产环境若未设置 warn 提示 |
| L-41 | `SERVER__HOST`/`SERVER__PORT` silent default + 与 AppSettings 重复 | [cli/util/service.rs#L12-L17](file:///workspace/backend/src/cli/util/service.rs#L12-L17) | P3 | CLI 工具也读 `AppSettings` |
| L-42 | `RATE_LIMIT_REDIS_URL` silent default 走本地 | [middleware/rate_limit.rs#L140-L143](file:///workspace/backend/src/middleware/rate_limit.rs#L140-L143) | P2 | 生产环境若未配置 Redis warn 提示 |
| L-43 | `.env.example` 缺失 `INIT_TOKEN` 显式声明 | [.env.example#L58-L59](file:///workspace/backend/.env.example#L58-L59) | P2 | 增加显式占位行 |
| L-44 | `.env.example` 缺失 `BINGXI_SYSTEMD_DIR`/`BINGXI_ENV_FILE` 显式声明 | [.env.example#L101-L104](file:///workspace/backend/.env.example#L101-L104) | P3 | 增加显式声明行 |
| L-45 | `COOKIE_SECRET`/`WEBHOOK_SECRET`/`AUDIT_SECRET_KEY` fail-fast 已完备（已验证通过） | [main.rs#L389-L426](file:///workspace/backend/src/main.rs#L389-L426) | P3 | 已闭环 |

---

## 五、修复队列（按优先级排序）

### 第一批（P0 级，14 项 - 阻塞，立即修复）

**业务场景闭环 P0（6 项）**：B-P0-1 ~ B-P0-6
**财务场景闭环 P0（8 项）**：F-P0-1 ~ F-P0-8

### 第二批（P1 级，19 项 - 高优先级）

**业务场景闭环 P1（9 项）**：B-P1-1 ~ B-P1-9
**财务场景闭环 P1（4 项）**：F-P1-1 ~ F-P1-4
**运行逻辑环流程闭环 P1（6 项）**：L-1, L-21, L-26, L-27, L-28, L-29

### 第三批（P2 级，24 项 - 中优先级）

**业务场景闭环 P2（6 项）**：B-P2-1 ~ B-P2-6
**财务场景闭环 P2（4 项）**：F-P2-1 ~ F-P2-4
**运行逻辑环流程闭环 P2（14 项）**：L-2, L-3, L-4, L-6, L-11, L-22, L-23, L-30, L-31, L-36, L-38, L-42, L-43

### 第四批（P3 级，25 项 - 低优先级）

**运行逻辑环流程闭环 P3（25 项）**：L-5, L-7, L-8, L-9, L-10, L-12, L-13, L-14, L-15, L-16, L-17, L-18, L-19, L-20, L-24, L-25, L-32, L-33, L-34, L-35, L-37, L-39, L-40, L-41, L-44, L-45

### 第五批（baseline 警告清零，213 摘要 / 89 位置 / 135 文件）

按警告最多的文件优先修复：
1. tests/bi_analysis_test.rs（18 个警告）
2. src/services/bi_analysis_service.rs（11 个警告）
3. src/services/auth/password_policy_service.rs（3 个警告）
4. src/search/elastic.rs（3 个警告）
5. 其余 48 个文件（各 1 个警告）

---

## 六、执行策略

1. **修复顺序**：P0 → P1 → P2 → P3 → baseline 清零
2. **每批 5-6 文件**：按规则 13 流程执行（建分支 → 修改 → commit → push → PR → CI → merge → 下一批）
3. **CI 全绿后自动进入下一批**：无需用户确认
4. **修复完成后同步清理 baseline 对应条目**
5. **每批修复后更新 doto.md 进度 + CHANGELOG.md 一句话总结**

---

## 七、项目已有的良好实践（保持）

- 大部分历史 `let _ =` 已通过批次修复改为 `?` 传播或 `tracing::warn!`
- 几乎所有 `tokio::spawn` 都有 `AssertUnwindSafe + catch_unwind` panic 隔离
- `COOKIE_SECRET`/`WEBHOOK_SECRET`/`AUDIT_SECRET_KEY` 启动期 fail-fast
- 锁中毒统一 `unwrap_or_else(|e| e.into_inner())` 优雅降级
- 测试夹具通过 `utils/unwrap_safe.rs` 宏统一管理
- `process_state_machine.rs` 状态机闭环完备
- 采购入库闭环完整：handler → `receive_order` → `record_transaction_txn` → `inventory_finance_bridge_service` 生成凭证 ✓
- 收款/付款事件驱动完整：`CollectionCompleted`/`PaymentCompleted` → 财务指标更新 ✓
- 低库存/缺料预警闭环完整 ✓
