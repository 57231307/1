# 未完成任务

> 本文件**只记录未完成任务**（任务队列、待修复项、剩余清单）。
> 已完成任务见 [doto-su.md](file:///workspace/.monkeycode/doto-su.md)，一句话总结见 [CHANGELOG.md](file:///workspace/.monkeycode/CHANGELOG.md)，规则见 [MEMORY.md](file:///workspace/.monkeycode/MEMORY.md)。
> 最近整理：2026-07-13（精简归档批次详细记录，重组结构）。

---

## 一、当前任务：v13 复审 + 业务/财务/运行逻辑闭环修复

> **v13 复审报告**：[v13-review-2026-07-13.md](file:///workspace/.monkeycode/docs/audits/v13-review-2026-07-13.md)
> **执行策略**：规则 13+14+15 联动，CI 全绿后自动进入下一批。
> **已完成批次**：356-388（详见 [CHANGELOG.md](file:///workspace/.monkeycode/CHANGELOG.md)）

### 进度总览

| 维度 | 总数 | 已完成 | 剩余 | 状态 |
|------|------|--------|------|------|
| baseline 警告清零 | 213 | 11 | 202 | 🔄 批次 357 完成 11 项 |
| 业务场景闭环 | 21 | 21 | 0 | ✅ **全部完成**（P0 6/6 + P1 9/9 + P2 6/6）（批次 386 完成 B-P2-4/5/6） |
| 财务场景闭环 | 16 | 11 | 5 | 🔄 **P0 8/8 ✅** + **P1 6/6 ✅** + P2 2/4（批次 387 完成 F-P2-2/4，F-P2-1/3 待后续批次） |
| 运行逻辑环闭环 | 45 | 45 | 0 | ✅ 全部完成（P1 6 + P2 13 + P3 26） |
| v14 中风险遗留 | 3 大类 | 0 | 3 大类 | ⏳ 待修复 |
| v14 低风险遗留 | 74 | 0 | 74 | ⏳ 后续迭代 |
| v13 前端/后端 P2 | 9 | 3 | 6 | 🔄 批次 388 完成 FE-P2-1/FE-P2-2/P2-1，FE-P2-3/P2-2/P2-3 待批次 389 |
| **合计** | **~378** | **82** | **~296** | — |

---

## 二、任务重新规划（每批 5-8 文件，完成后新一轮复审）

> **规划原则**：每个批次修复 5-8 个文件，按优先级分阶段推进，所有阶段完成后进行 v14 新一轮复审。
> **执行策略**：规则 13+14+15 联动，CI 全绿后自动进入下一批，无需用户确认。

### 阶段 1：P1 级闭环修复（批次 384，1 批，约 7 文件）

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| B-P1-3 | event_bus.rs / customer_service.rs / supplier_service.rs | 客户/供应商主数据变更事件发布+监听器异步刷新关联单据 |
| B-P1-7 | event_bus.rs / 新建 dead_letter_service.rs / 新建 alert_service.rs | 事件重试（指数退避）+ 死信队列 + 告警 |
| F-P1-1 | accounting_period_service.rs / account_subject_service.rs | close_period 新增期末结转，本期期末余额写入下期期初 |

### 阶段 2：业务场景 P2 闭环修复（批次 385-386，2 批，约 12 文件）

**批次 385（业务场景 P2 前 3 项，约 6 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| B-P2-1 | ar_service.rs | create_payment 与 mark_as_paid 状态更新重复，合并为单一入口 |
| B-P2-2 | customer_credit_evaluate_service.rs + mod.rs | 孤岛 service 评估后删除或接入业务 |
| B-P2-3 | cost_collection_service.rs + handler + routes | 仅 HTTP 调用，接入业务联动 |

**批次 386（业务场景 P2 后 3 项，约 6 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| B-P2-4 | mrp_engine_service.rs + handler + routes | 仅 HTTP 调用，接入业务联动 |
| B-P2-5 | capacity_service.rs + handler + routes | 仅 HTTP 调用，接入业务联动 |
| B-P2-6 | inventory_reservation_service.rs + handler + routes | 仅 HTTP 调用，销售流程集成 |

### 阶段 3：财务场景 P2 闭环修复（批次 387，1 批，约 7 文件）

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| F-P2-1 | accounting_period_service.rs + 新建 period_adjustment_service.rs | 期末调整机制（暂估/摊销/预提） |
| F-P2-2 | finance_report_service.rs + handler | 报表穿透追溯功能 |
| F-P2-3 | inventory_finance_bridge_service.rs | 销售成本与采购实际单价联动 |
| F-P2-4 | ar_service.rs / ap_invoice_service.rs + voucher_service.rs | AR/AP 对账单生成触发凭证 |

### 阶段 4：v13 前后端 P2（批次 388-389，2 批，约 14 文件）

**批次 388（前端类型+后端错误处理，约 7 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| FE-P2-1 | frontend/src/types/*.ts（3-4 文件） | unknown 类型细化，完善类型定义 |
| FE-P2-2 | frontend/src/components/*.vue（2 文件） | 组件 props 类型强化 |
| P2-1 | backend/src/handlers/*.rs（1-2 文件） | 后端错误处理统一，handler 返回 AppError |

**批次 389（i18n+后端日志+配置，约 7 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| FE-P2-3 | frontend/src/locales/*.ts + views（3 文件） | i18n 覆盖率提升（首批核心视图） |
| P2-2 | backend/src/services/*.rs（2 文件） | 后端日志规范，日志级别修正 |
| P2-3 | backend/config.yaml.example + .env.example（2 文件） | 后端配置项完善 |

### 阶段 5：useTableApi 接入（批次 390-391，2 批，约 10 文件）

**批次 390（前 5 个 view，5 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| useTableApi-1 | frontend/src/views/finance/voucher/VoucherListTab.vue | 财务凭证列表 |
| useTableApi-2 | frontend/src/views/finance/voucher/VoucherDetailTab.vue | 财务凭证明细 |
| useTableApi-3 | frontend/src/views/data-import/DataImportListTab.vue | 数据导入列表 |
| useTableApi-4 | frontend/src/views/data-import/DataImportTaskTab.vue | 数据导入任务 |
| useTableApi-5 | frontend/src/views/inventory/tabs/InventoryStockTab.vue | 库存明细（1-based 分页） |

**批次 391（后 5 个 view，5 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| useTableApi-6 | frontend/src/views/inventoryAdjustment/AdjustmentListTab.vue | 库存调整 |
| useTableApi-7 | frontend/src/views/inventoryTransfer/TransferListTab.vue | 库存调拨 |
| useTableApi-8 | frontend/src/views/barcodeScanner/index.vue | 条码扫描（0-based 分页特殊处理） |
| useTableApi-9 | frontend/src/views/assistAccounting/index.vue | 辅助核算（0-based 分页特殊处理） |
| useTableApi-10 | 待扫描发现的遗漏文件 | 其他遗漏 |

### 阶段 6：测试覆盖补测（批次 392-394，3 批，约 18 文件）

**批次 392（核心 service 测试 - 认证/用户/订单，约 6 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| 测试-1 | backend/src/services/auth_service.rs + tests | auth_service 单元测试 |
| 测试-2 | backend/src/services/user_service.rs + tests | user_service 单元测试 |
| 测试-3 | backend/src/services/so/order.rs + tests | 销售订单 service 测试 |
| 测试-4 | backend/src/services/po/order.rs + tests | 采购订单 service 测试 |

**批次 393（核心 service 测试 - 库存/财务，约 6 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| 测试-5 | backend/src/services/inventory_stock_service.rs + tests | 库存 service 测试 |
| 测试-6 | backend/src/services/voucher_service.rs + tests | 凭证 service 测试 |
| 测试-7 | backend/src/services/ar_service.rs + tests | AR service 测试 |
| 测试-8 | backend/src/services/ap_invoice_service.rs + tests | AP service 测试 |

**批次 394（handler 测试，约 6 文件）**：

| 任务 | 涉及文件 | 说明 |
|------|----------|------|
| 测试-9 | backend/tests/auth_handler_test.rs | auth handler 集成测试 |
| 测试-10 | backend/tests/order_handler_test.rs | 订单 handler 集成测试 |
| 测试-11 | backend/tests/inventory_handler_test.rs | 库存 handler 集成测试 |
| 测试-12 | backend/tests/finance_handler_test.rs | 财务 handler 集成测试 |

### 阶段 7：baseline 清零（批次 395-424，约 30 批，约 202 项）

> **目标**：213 条 baseline 警告全部清零，每批 5-8 文件清理 7-10 项警告。
> **分类**：dead_code 193 + unused_import 15 + 其他 5（批次 357 已清理 11 项 unused import）。
> **执行方式**：按文件分组扫描，每批选取 5-8 个文件集中清理，CI 全绿后自动进入下一批。
> **完成后**：移除 baseline 机制，改为 `cargo clippy -- -D warnings`。

| 批次范围 | 文件数 | 警告清理数 | 说明 |
|----------|--------|-----------|------|
| 395-404 | 50-80 | 70-100 | dead_code 前 100 项（按文件分组） |
| 405-414 | 50-80 | 70-100 | dead_code 中 100 项 |
| 415-424 | 30-50 | 30-50 | dead_code 后 93 项 + 其他 5 项 |

### 阶段 8：v14 低风险修复（批次 425-435，约 11 批，74 项）

> **目标**：74 项低风险问题全部修复，每批 5-8 文件。

| 批次范围 | 任务类别 | 项数 | 说明 |
|----------|----------|------|------|
| 425-427 | 占位符/Mock 存根 | 21 | 逐个评估，合理保留加注释，不合理的真实实现 |
| 428-429 | 项目规则符合性 | 11 | 评估是否符合规则 0-13 |
| 430-431 | 死代码补充清理 | 8 | 与 baseline 清零合并处理后的遗漏 |
| 432-435 | 其他 | 34 | 命名规范/注释完善/代码风格等 |

### 阶段 9：其他遗留（批次 436-438，3 批，约 15 文件）

| 批次 | 任务 | 涉及文件 | 说明 |
|------|------|----------|------|
| 436 | FE-P2-6 | frontend/src/components/Table*.vue（5-8 文件） | 大列表虚拟化（el-table-v2 引入） |
| 437 | P2-8 | backend/src/services/*.rs + tests（5-8 文件） | 剩余无测试 service 补测 |
| 438 | E2E 失败排查 | e2e/*.spec.ts + 修复代码（5-8 文件） | E2E 失败用例分析与修复 |

### 阶段 10：v14 新一轮复审（批次 439+）

> **触发条件**：阶段 1-9 全部完成后自动触发。
> **复审目标**：在 v13 复审全部维度基础上，新增 17 个审计维度（通用 3 + 面料行业特性 7 + 面料行业模块专项 7），全面覆盖面料行业 ERP 业务场景。
> **核心特性**：**一个面料有多个颜色**（面料→颜色→缸号→批号四层级联关系），所有维度均围绕此核心特性展开。
> **复审流程**：扫描 → 生成 v14 复审报告 → 按优先级排序修复队列 → 自动开始修复（每批 5-8 文件）。

#### 10.0 面料行业核心数据模型（所有维度的基础）

> **核心特性：一个面料有多个颜色**。这是面料行业 ERP 的基础数据模型，所有业务模块均围绕此模型展开。

**四层级联关系**：
```
面料（Fabric/Product）
  └── 颜色（Color）              ← 一个面料有多个颜色
       ├── 色号（ColorNo）        ← 颜色唯一编码，如 C001
       ├── 色卡（ColorCard）      ← 颜色实物样卡
       └── 缸号（DyeLotNo）       ← 每个颜色每批次染色有独立缸号
            ├── 批号（BatchNo）    ← 同一缸号下可分多个批次（匹号）
            ├── 库存数量          ← 按缸号/批号独立核算
            ├── 质检结果          ← 按缸号/批号独立检验
            ├── 成本单价          ← 按缸号/批号独立核算（实际成本）
            └── 库位              ← 按缸号分区存放
```

**关键业务约束**（必须强制执行）：
- **匹号唯一约束**：**同一缸号（dye_lot_no）下不能有相同的匹号（batch_no）**
  - DB 层：`UNIQUE(dye_lot_no, batch_no)` 唯一索引
  - Service 层：创建/更新时校验 (dye_lot_no, batch_no) 组合唯一，冲突时返回业务错误
  - 全局适用：库存表、入库表、发货表、退货表、盘点表等所有含 batch_no 的表均需校验
  - 业务语义：一个缸号代表一次染色，同一缸内分多匹出布，每匹有独立匹号；匹号重复会导致库存和成本核算混乱
- **面料-颜色关联约束**：一个面料可有多个颜色，但同一面料+颜色组合唯一（`UNIQUE(product_id, color_id)`）
- **缸号-颜色关联约束**：缸号必须挂靠 color_id，缸号在颜色范围内唯一（`UNIQUE(color_id, dye_lot_no)`）
- **库存四维标识**：库存唯一标识 = product_id + color_id + dye_lot_no + batch_no（联合唯一索引）

**数据模型检查要点**：
- `product` 表：面料基础信息（品名、成分、规格、门幅、克重、纱支）
- `product_color` 表：面料-颜色关联（product_id ↔ color_id 多对一，UNIQUE(product_id, color_id)）
- `color` 表：颜色主数据（色号、颜色名称、RGB/潘通色号）
- `color_card` 表：色卡管理（挂靠 color_id，非 product_id）
- `dye_lot` 表：缸号管理（挂靠 color_id，UNIQUE(color_id, dye_lot_no)）
- `inventory_stock` 表：库存按 product_id + color_id + dye_lot_no + batch_no 四维度核算，UNIQUE 四字段联合索引
- `inventory_transaction` 表：库存流水按四维度记录
- 所有业务单据（订单/发货/入库/退货）明细必须包含 color_id + dye_lot_no + batch_no

#### 10.0.1 复用现有功能原则（v14 复审最高准则）

> **核心准则：v14 复审的修复必须与现有功能优化结合，禁止重复实现类似功能，尽最大可能复用现有前后端功能。**

**强制执行要求**：
- **修复前必须调研现有实现**：每个维度修复前，必须先扫描现有代码，评估是否已有类似功能可复用
- **禁止重复造轮子**：如已有 service/composable/component 实现类似功能，必须复用而非新建
- **优化而非新建**：对已有功能进行优化完善，而非新建并行实现
- **合并同类项**：发现多个 service/composable 实现相同逻辑时，必须合并为单一实现
- **共享工具优先**：优先使用 `utils/` 下的共享工具（如 dual_unit_converter、number_generator、pagination 等）
- **前端 composable 优先**：优先使用 `useTableApi`、`useFormApi` 等已封装的 composable
- **后端 service 优先**：优先使用已封装的 `voucher_service`、`audit_log_service`、`event_bus` 等
- **DB 模型扩展优先**：新增字段优先扩展已有表，而非新建表（除非有明确的业务隔离需求）
- **API 契约兼容**：修复时优先保持 API 返回格式兼容，避免破坏前端已有调用

**复用评估清单**（每个维度修复前必须逐项核对）：
1. 现有 service 是否已实现类似业务逻辑？→ 复用并扩展
2. 现有 composable 是否已封装类似前端逻辑？→ 复用并扩展
3. 现有组件是否已实现类似 UI 交互？→ 复用并扩展
4. 现有 utils 工具是否已提供类似能力？→ 直接使用
5. 现有 DB 表是否已有类似字段？→ 扩展而非新建表
6. 现有 API 是否已暴露类似端点？→ 扩展参数而非新建端点
7. 现有事件类型是否已覆盖类似业务事件？→ 复用而非新建事件

**禁止的反模式**：
- 为实现"缸号管理"新建 `dye_lot_service.rs`，而现有 `color_card_borrow_service.rs` 已有缸号逻辑 → 必须复用并扩展
- 为实现"双单位换算"新建前端 composable，而 `utils/dual_unit_converter.rs` 已有后端实现 → 前端直接调用 API
- 为实现"色卡管理"新建 `color_card_service.rs`，而现有已有同名 service → 扩展现有
- 为实现"按缸号核算成本"新建 `dye_cost_service.rs`，而现有 `cost_service.rs` 可扩展 → 扩展现有

#### 10.1 通用审计维度（3 项）

##### 10.1.1 业务功能完整性

**检查要点**：
- 所有规划功能是否真实实现，无占位符/stub/`let _ =` 吞错
- 功能闭环：输入→处理→输出→反馈，无断点
- 验收标准 100% 达成（对照规划文档逐项核对）
- 每个功能模块的 CRUD 完整：创建→查询→更新→删除→列表分页
- 批量操作支持（批量删除/批量审核/批量导出）
- 软删除 vs 硬删除策略一致（已删除数据不可恢复 vs 可恢复）
- 并发安全：乐观锁/悲观锁/状态机门控
- 幂等性保证：重复请求不产生副作用

**涉及范围**：全项目

##### 10.1.2 逻辑完整性

**检查要点**：
- 业务逻辑无断点：状态流转必须连贯（如 DRAFT→SUBMITTED→REVIEWED→POSTED）
- 状态机闭环有终态：所有状态机必须有最终态，禁止"孤儿状态"
- 异常路径有恢复/降级/告警：所有 `Result<T, E>` 必须有错误处理，禁止 `unwrap()`/`expect()` 无上下文
- 分支覆盖完整：if/else/match 所有分支必须有处理，禁止 `_ => {}` 吞分支
- 边界条件处理：零值/负值/空字符串/最大值/精度溢出
- 并发冲突处理：乐观锁失败重试/悲观锁超时降级
- 事务边界正确：跨表操作必须原子化，失败必须回滚
- 事件发布时机：commit 后发布（避免幻事件），失败仅 warn 不阻断

**涉及范围**：全项目

##### 10.1.3 数据流转性

**检查要点**：
- 跨模块数据流转连贯无断点：销售→库存→财务→报表全链路
- 业财数据一致性：业务表金额与凭证金额一致，库存数量与流水一致
- 主数据变更同步关联单据：客户/供应商/面料/颜色变更后关联单据冗余字段同步刷新
- 报表数据可追溯到底层凭证：报表数字 → 凭证汇总 → 业务单据 → 库存流水
- 面料-颜色数据流转：订单明细的 color_id 从订单→发货→出库→成本→凭证全链路传递
- 缸号/批号数据流转：库存的 dye_lot_no/batch_no 从入库→存储→出库→退货全链路可追溯
- 双单位数据流转：米/公斤换算在所有模块一致（换算率统一管理）
- 辅助核算数据流转：客户/供应商/面料/颜色辅助核算项在凭证中完整记录

**涉及范围**：全项目

#### 10.2 面料行业特性审计维度（7 项）

##### 10.2.1 面料行业转项特性优化

**检查要点**：
- **色号管理**：每个颜色有唯一色号（如 C001），色号规则可配置
- **批号管理**：每批次生产有独立批号，批号含日期/序号/缸号信息
- **缸号管理**：每次染色有独立缸号，缸号关联染色工艺参数
- **一个面料多颜色**：`product` 表与 `color` 表多对一关系，UI 支持按面料查看所有颜色
- **色差等级**：A 级（合格）/ B 级（让步接收）/ C 级（降级处理），影响定价和销售
- **门幅**：面料宽度（如 150cm），影响用料计算和定价
- **克重**：单位面积重量（g/m²），影响成本核算和定价
- **纱支**：纱线粗细（如 40S），面料规格核心指标
- **面料属性**：成分（棉/涤/混纺）、工艺（梭织/针织）、后整理（防水/抗皱/涂层）
- **双单位换算**：米↔公斤，换算率 = 克重 × 门幅 / 100（统一管理，禁止各模块独立计算）
- **面料编码体系**：面料编码 + 颜色编码 + 缸号 + 批号 组成唯一库存标识

**涉及范围**：`models/product` + `models/color` + `models/dye_lot` + `services/fabric` + `utils/dual_unit_converter`

##### 10.2.2 面料行业术语

**检查要点**：
- 代码变量名使用行业术语：`dye_lot_no`（缸号）、`color_no`（色号）、`batch_no`（批号）
- 代码注释使用行业术语：解释"染色批次"、"色卡"、"门幅"等
- UI 文本使用行业术语：列表表头、表单标签、报表标题
- 数据库字段名使用行业术语：`fabric_width`（门幅）、`gram_weight`（克重）、`yarn_count`（纱支）
- API 响应字段使用行业术语：前端类型定义与后端一致
- 无外行表述：禁止"颜色编号"代替"色号"、"染色批次号"代替"缸号"
- 术语配套使用：缸号↔染色批次、色号↔颜色编码、批号↔生产批次（全项目一致）
- 面料属性术语：成分（composition）、工艺（weaving_type）、后整理（finishing）
- 质检术语：色差（color_difference）、纬斜（skewness）、缩水率（shrinkage）
- 染整术语：染料（dye）、助剂（auxiliary）、固色（fixing）、皂洗（soaping）

**涉及范围**：全项目文本（代码变量名 + 注释 + UI + DB 字段 + API 字段）

##### 10.2.3 面料行业流程完善情况

**检查要点**：
- **打色流程**：客户来样→实验室打色→客户确认→色号生成→色卡制作
- **试样流程**：小样试织→客户确认→大货生产准备
- **大货生产流程**：生产订单→领料→染色→印花→后整理→质检→入库
- **染整工艺流程**：前处理→染色→固色→皂洗→烘干→定型→验布
- **印花工艺流程**：制版→调浆→印花→烘干→汽蒸→水洗→定型
- **面料检验流程（十项指标）**：色差、纬斜、缩水率、克重偏差、门幅偏差、色牢度、疵点、强力、起毛起球、手感
- **质检结果分级**：A 级（合格）/ B 级（让步接收，降级销售）/ C 级（不合格，返工或报废）
- **入库流程**：按缸号/批号分批入库，每批独立质检结果
- **发货流程**：按客户指定缸号/批号发货，先进先出或指定批次
- **一个面料多颜色在流程中的体现**：同一面料的多个颜色可并行生产，每个颜色独立缸号

**涉及范围**：`services/dye` + `services/production` + `services/inspection` + `services/color_card`

##### 10.2.4 面料行业交易流程

**检查要点**：
- **询价流程**：客户询价（指定面料+颜色+数量）→ 内部核算成本 → 报价
- **报价流程**：报价单（含面料规格、颜色、单价、色卡费、打样费）→ 客户确认
- **打样确认流程**：打样订单（指定面料+颜色）→ 打色 → 寄色卡 → 客户确认 → 转大货
- **大货订单流程**：销售订单（面料+颜色+缸号要求+数量+交期）→ 生产 → 发货
- **面料定价**：按米计价 / 按公斤计价 / 按码计价（可配置，含税/不含税）
- **色卡费**：打色/制卡费用，独立计费或转大货后冲销
- **打样费**：小样试织费用，独立计费或转大货后冲销
- **对账流程**：发货明细按缸号/批号汇总 → 客户对账 → 确认收款
- **收款流程**：按面料+颜色+缸号追溯收款，支持部分收款
- **一个面料多颜色在交易中的体现**：报价单一个面料多行颜色，每颜色独立单价；订单一个面料多行颜色，每颜色独立交期

**涉及范围**：`services/so` + `services/quotation` + `services/color_card` + `services/ar`

##### 10.2.5 面料行业调货流程

**检查要点**：
- **厂际调拨**：总厂→分厂调拨，按面料+颜色+缸号指定
- **仓际调拨**：原料仓→成品仓调拨，按面料+颜色+缸号+批号指定
- **借条流程**：借出面料（指定颜色+缸号）→ 借条单 → 还条 → 冲销
- **还条流程**：归还面料（原缸号或新缸号）→ 还条单 → 冲销借条
- **调货计价**：成本价（内部调拨）/ 协议价（关联公司交易）/ 市场价（独立公司交易）
- **在途库存管理**：调拨在途数量独立核算，不影响可用库存
- **调拨损耗处理**：调拨过程损耗单独记录，影响成本核算
- **一个面料多颜色在调货中的体现**：调拨单一个面料多行颜色，每颜色独立缸号和数量

**涉及范围**：`services/inventory_transfer` + `services/color_card_borrow`

##### 10.2.6 面料行业专用词汇/术语配套使用

**检查要点**：
- **缸号 ↔ 染色批次**：代码中统一用 `dye_lot_no`，注释/UI 用"缸号"，DB 字段用 `dye_lot_no`
- **色号 ↔ 颜色编码**：代码中统一用 `color_no`，注释/UI 用"色号"，DB 字段用 `color_no`
- **批号 ↔ 生产批次**：代码中统一用 `batch_no`，注释/UI 用"批号"，DB 字段用 `batch_no`
- **门幅 ↔ 规格**：代码中用 `fabric_width`，注释/UI 用"门幅"，DB 字段用 `fabric_width`
- **克重 ↔ 单位重量**：代码中用 `gram_weight`，注释/UI 用"克重"，DB 字段用 `gram_weight`
- **纱支 ↔ 纱线粗细**：代码中用 `yarn_count`，注释/UI 用"纱支"，DB 字段用 `yarn_count`
- **成分 ↔ 原料构成**：代码中用 `composition`，注释/UI 用"成分"，DB 字段用 `composition`
- **染整 ↔ 染色整理**：代码中用 `dyeing_finishing`，注释/UI 用"染整"
- **印花 ↔ 图案印刷**：代码中用 `printing`，注释/UI 用"印花"
- **色差 ↔ 颜色偏差**：代码中用 `color_difference`，注释/UI 用"色差"
- **色卡 ↔ 颜色实物样**：代码中用 `color_card`，注释/UI 用"色卡"
- **术语一致性**：同一术语在代码/DB/前后端/UI/报表中完全一致，无混用
- **术语翻译规范**：英文变量名 + 中文注释/UI，禁止拼音命名

**涉及范围**：全项目术语一致性（代码 + DB + 前后端 + UI + 报表）

##### 10.2.7 面料行业业务流转

**检查要点**：
- **接单流转**：客户询价 → 报价 → 打样确认 → 大货订单（面料+颜色+缸号要求）
- **打色流转**：订单触发放色 → 实验室打色 → 色号生成 → 色卡制作 → 客户确认
- **采购流转**：按订单面料+颜色 → 采购原料（染料/助剂/坯布）→ 入库
- **生产流转**：生产订单（面料+颜色+缸号）→ 领料 → 染色 → 印花 → 后整理
- **染色流转**：按缸号投染 → 染色工艺参数记录 → 出缸 → 质检
- **印花流转**：按颜色制版 → 调浆 → 印花 → 烘干 → 定型
- **质检流转**：按缸号/批号检验 → 十项指标 → 分级（A/B/C）→ 入库
- **入库流转**：按缸号/批号分批入库 → 库位分配（染缸区/成品区）
- **销售流转**：发货（指定缸号/批号）→ 出库 → 成本结转 → 收入确认
- **退货流转**：按缸号/批号退货 → 质检 → 入库（或报废）→ 红字凭证
- **跨模块事件贯通**：每个流转节点发布事件，下游订阅处理，失败有重试+死信
- **一个面料多颜色在流转中的体现**：同一面料的多个颜色可并行流转，每颜色独立缸号和状态

**涉及范围**：`services/*` 全链路 + `event_bus`

#### 10.3 面料行业模块专项审计维度（7 项）

##### 10.3.1 面料行业人事管理

**检查要点**：
- **员工档案**：岗位分类（染色工/印花工/质检员/仓管员/销售员/采购员/打色员）
- **考勤排班**：三班倒支持（早班/中班/夜班），染色车间 24 小时连续生产
- **计件工资**：按米计件 / 按公斤计件，不同工序不同单价
- **染色工计件**：按缸号产量计件，A 级品全额，B 级品折扣，C 级品不计
- **印花工计件**：按印花米数计件，含套色数加权
- **质检员计件**：按检验批次计件，含检验指标数加权
- **绩效与产量挂钩**：A 级品率奖励，C 级品率扣罚
- **技能矩阵**：员工技能等级（初级/中级/高级），影响工序分配
- **培训记录**：染色工艺/印花工艺/质检标准培训记录
- **一个面料多颜色在人事中的体现**：按颜色/缸号统计员工产量，计算计件工资

**涉及范围**：`services/hr` + `handlers/hr` + `services/payroll`

##### 10.3.2 面料行业仓库管理

**检查要点**：
- **面料分类入库**：按面料品类（梭织/针织/无纺）分区存放
- **按色号/缸号分区**：同一面料不同颜色分库位，同色不同缸号分批次
- **批次先进先出**：按入库时间先后发货，避免库存积压
- **指定批次发货**：客户指定缸号/批号发货（如复购指定同缸号）
- **库存双单位**：米/公斤双单位核算，换算率统一管理
- **库位管理**：染缸区（在制品）/ 印花区（在制品）/ 成品区 / 原料区 / 次品区
- **盘点差异处理**：按缸号/批号盘点，差异按色差/短码/疵点分类
- **库存预警**：按面料+颜色预警（某颜色库存不足）
- **库龄分析**：按缸号/批号分析库龄，超期面料降价处理
- **一个面料多颜色在仓库中的体现**：库存查询按面料展开所有颜色，每颜色展开所有缸号，每缸号展开所有批号

**涉及范围**：`services/inventory` + `models/inventory` + `services/warehouse`

##### 10.3.3 面料行业销售管理

**检查要点**：
- **面料报价**：含色卡费/打样费/大货单价，按颜色独立报价
- **客户色卡管理**：客户专属色卡库，历史色卡可追溯
- **大货订单与打样订单关联**：打样订单转大货，关联色号和缸号要求
- **销售退货**：按缸号/批号退货，可追溯原始发货单
- **客户信用额度**：面料行业账期长（30/60/90 天），信用额度按客户+面料品类控制
- **色差纠纷处理**：客户投诉色差，按缸号追溯质检记录
- **复购管理**：客户复购指定同缸号（颜色一致性），系统提示库存
- **价格波动管理**：面料价格随原料波动，历史报价可追溯
- **订单分配**：一个面料多颜色订单，按颜色分配生产任务
- **一个面料多颜色在销售中的体现**：订单一个面料多行颜色，每颜色独立单价/数量/交期/缸号要求

**涉及范围**：`services/so` + `services/customer` + `services/color_card` + `services/quotation`

##### 10.3.4 面料行业公司管理

**检查要点**：
- **多公司/多工厂架构**：总公司下多个分厂，独立核算
- **公司间调货/交易**：分厂间调拨按成本价，关联公司交易按协议价
- **组织架构**：总厂/分厂/车间/班组/岗位五级架构
- **公司级配置**：染整工艺参数/印花工艺参数/质检标准按公司独立配置
- **独立核算**：每个分厂独立成本核算，独立财务报表
- **合并报表**：总公司合并所有分厂报表
- **产能管理**：各分厂染缸数量/印花机数量/产能独立管理
- **一个面料多颜色在公司管理中的体现**：同一面料在不同分厂生产不同颜色，按分厂+颜色统计产能

**涉及范围**：`services/company` + `services/organization` + `services/production_capacity`

##### 10.3.5 面料行业权限细化

**检查要点**：
- **按岗位授权**：染色工/印花工/质检员/仓管员/销售员/采购员/打色员
- **按车间授权**：染色车间/印花车间/成品车间，员工只能操作本车间数据
- **按面料品类授权**：梭织组/针织组，销售员只能销售授权品类
- **按颜色授权**（高级）：某销售员只负责某颜色系（如暖色系）
- **敏感操作独立权限**：调价/退货/盘点/报废需独立授权
- **缸号级权限**（高级）：质检员只能检验本车间缸号
- **数据隔离**：分厂员工只能查看本厂数据
- **操作日志**：所有敏感操作记录审计日志，按岗位/车间/品类过滤
- **一个面料多颜色在权限中的体现**：按颜色授权销售员（某销售员只销售某颜色）

**涉及范围**：`middleware/permission` + `services/auth` + `services/role`

##### 10.3.6 面料行业财务专项优化

**检查要点**：
- **面料成本核算**：染料成本 + 助剂成本 + 人工成本 + 水电费分摊 + 设备折旧
- **按缸号核算成本**：每缸独立核算实际成本（非按面料平均成本）
- **染整加工费核算**：外发染整加工费按缸号归集
- **色卡/打样费用归集**：打色/制卡费用归集到客户或转大货后摊销
- **面料库存估值**：按批次实际成本（移动加权平均/先进先出），非按面料平均
- **色差降级处理**：B 级品降价销售，成本按实际售价倒推；C 级品报废，成本转损失
- **面料行业税票**：染整加工费税率（6% 服务）/ 面料销售税率（13% 货物）分开
- **成本结转**：销售出库按缸号实际成本结转主营业务成本
- **期末调整**：面料库存跌价准备（按库龄和品级）
- **一个面料多颜色在财务中的体现**：成本按面料+颜色+缸号三级核算，同一面料不同颜色成本不同

**涉及范围**：`services/voucher` + `services/cost` + `services/ar` + `services/ap` + `services/accounting_period`

##### 10.3.7 面料行业 CRM 专业优化

**检查要点**：
- **客户面料偏好**：品类偏好（梭织/针织）/ 色系偏好（暖色/冷色）/ 规格偏好（门幅/克重）
- **客户色卡历史**：客户所有色卡记录，按面料+颜色+时间检索
- **客户打样记录追踪**：打样→客户反馈→修改→再打样→确认全流程追踪
- **客户报价历史**：面料价格波动历史，按面料+颜色+时间检索
- **客户投诉与色差纠纷管理**：投诉记录（缸号/批号/色差等级）→ 处理（退货/补偿/让步接收）
- **客户复购预测**：按面料品类+颜色预测复购周期和数量
- **客户价值分析**：RFM 模型 + 面料品类维度
- **客户信用管理**：信用额度按面料品类分配（高价值面料低额度）
- **客户联系人管理**：采购/质检/财务多联系人
- **一个面料多颜色在 CRM 中的体现**：客户偏好按面料展开颜色偏好，每颜色独立色卡历史和报价历史

**涉及范围**：`services/crm` + `services/customer` + `services/color_card` + `services/quotation`

#### 10.4 v14 复审执行流程

1. **复审阶段**（自动执行）：
   - 扫描 v13 复审全部维度（baseline 清零 + 业务/财务/运行逻辑闭环回归验证）
   - 扫描 17 个新增维度（通用 3 + 面料行业特性 7 + 面料行业模块专项 7）
   - 重点核查"一个面料多颜色"核心特性在所有模块的实现
   - 重点核查"同一缸号下匹号唯一"业务约束在所有含 batch_no 的表的实现
   - 生成 v14 复审报告（保存到 `.monkeycode/docs/audits/v14-review-YYYY-MM-DD.md`）
   - 按优先级排序修复队列（P0 阻塞 → P1 高 → P2 中 → P3 低）

2. **修复阶段**（复审完成后自动开始）：
   - 严格按规则 13 流程执行：建分支 → 修改 → commit → push → PR → CI → merge → 下一批
   - 每批 5-8 文件，CI 全绿后自动进入下一批，无需用户确认
   - 所有警告视为错误，必须真实修复（规则 14）
   - **复用现有功能原则**（10.0.1 节）：修复前必须调研现有实现，禁止重复造轮子，优化而非新建
   - **匹号唯一约束**：所有涉及 batch_no 的修复，必须同步校验 (dye_lot_no, batch_no) 组合唯一

3. **闭环验证**：
   - 每批修复后更新 doto.md 进度 + CHANGELOG.md 一句话总结
   - 所有批次完成后进行 v14 复审回归验证（确认无新增问题）
   - 通过后触发 v15 复审（如需）

---

## 三、批次执行计划总览

| 阶段 | 批次范围 | 批次数 | 文件数 | 任务类别 |
|------|----------|--------|--------|----------|
| 1 | 384 | 1 | 7 | P1 级闭环（B-P1-3/7 + F-P1-1） |
| 2 | 385-386 | 2 | 12 | 业务场景 P2（B-P2-1~6） |
| 3 | 387 | 1 | 7 | 财务场景 P2（F-P2-1~4） |
| 4 | 388-389 | 2 | 14 | v13 前后端 P2（FE-P2-1~3 + P2-1~3） |
| 5 | 390-391 | 2 | 10 | useTableApi 接入（10 个 view） |
| 6 | 392-394 | 3 | 18 | 测试覆盖补测（核心 service + handler） |
| 7 | 395-424 | 30 | 200+ | baseline 清零（202 项） |
| 8 | 425-435 | 11 | 74 | v14 低风险修复（74 项） |
| 9 | 436-438 | 3 | 15 | 其他遗留（虚拟化+补测+E2E） |
| 10 | 439+ | - | - | v14 新一轮复审（v13 全维度 + 17 个新增维度：通用 3 + 面料行业特性 7 + 面料行业模块专项 7） |
| **合计** | **384-438** | **55** | **~357** | **所有未完成任务** |

---

## 四、规则节点提醒

- **规则 5（E2E 独立工作流，每 30 批次）**：批次 330 已到期需触发（403 权限不足，需用户手动触发）；批次 390、420、450 到期需触发
- **规则 10（每 15 批次记忆整理）**：批次 375 已完成，下次整理批次 390，后续 405/420/435
- **规则 13（修复流程自动化）**：CI 全绿后自动开始下一批，无需用户确认
- **规则 14（移除所有警告抑制）**：所有警告视为错误需修复
- **规则 15（v13 复审严格规范 + v14 面料行业特性复审）**：v13 业务/财务/运行逻辑闭环 + 阶段 1-9 完成后触发 v14 复审，新增 17 个维度（通用 3 + 面料行业特性 7 + 面料行业模块专项 7）

---

## 五、历史任务（全部完成）

- v8 复审（批次 290-308）：21 项问题全部修复 ✅
- v9 复审（批次 317-323）：16 项问题全部修复 ✅
- sea-orm 版本调研（批次 324）：确认使用 1.1.20 稳定版 ✅
- 规则 14 新增（批次 324）：移除所有警告抑制 ✅
- v10 复审（批次 325-339）：53 项问题全部修复 ✅
- v11 复审（批次 340-346）：27 项问题全部修复 ✅
- v12 复审（批次 347-355）：15 项问题全部修复 ✅
- v13 复审批次 356-383：详见 [CHANGELOG.md](file:///workspace/.monkeycode/CHANGELOG.md)

> 详细记录已归档到 [doto-su.md](file:///workspace/.monkeycode/doto-su.md)。
